# SLICE BRIEF — WBS 2.1 · Register WH1, its zones and its 8 space blocks (proof slice)

Task: 2.1 — Register **WH1**, its zones and its **8 space blocks** — doc 19 §4 + `019-Warehouse-WH1-Setup.sql`
      (the file generates exactly what 19 §4 describes; nothing is entered by hand)
Lane: 2          Lock: wms (tasks/LANE_LOCKS.md)          Owner: WH_MGR
Model: session (sonnet-tier work) · Delegated: pg-tester (sonnet) → pg-reviewer (opus) → pg-scribe (haiku)
Type of slice: **proof-only**, same shape as WBS 0.9 (`aa46787`) and 0.18 — the schema file already
delivers the data; the slice is the permanent, re-runnable proof that doc 38's acceptance numbers hold
against a live database. There is no domain/application/api code to build; pg-backend is NOT used.

## Read ONLY
- CLAUDE.md
- .claude/briefs/wms.brief.md (§2 tables; note its obj_description column carries pre-existing mojibake — ignore it)
- docs/package/19-PST-Warehouse-Setup.md: lines 111-119 (§3-1 zones by type), 93-102 (§2-4 the six operational
  zones and their location counts 6·8·6·4·4·2), 127-135 (§3-2 space-block table: block · type A/A1/B/C ·
  positions · m³ — THE source of positions and m³), 224-240 (zones insert: code · name_ar · zone_type) and
  256-263 (block insert tuples: code · zone letter · block_type pallet_rack/shelf · positions · sqm · m³ —
  THE source of zone and block_type), §3-4 (verify_wh1 expectations)
- docs/package/40-Build-Specification-EN.md line 239 (WH1 fixed data — 30 operational: RCV 6 STG 8 SHP 6 QRT 4
  RTN 4 DMG 2; 147 structural `X-`)
- modules/identity/package.json (the second scaffold precedent, CHANGELOG 0.16)
- eslint.config.mjs · pnpm-workspace.yaml · root package.json (only to confirm MODULES discovery / turbo pickup)
- database/schema/019-Warehouse-WH1-Setup.sql — only to find `wms.verify_wh1()` and the block/zone inserts
- modules/platform/tests/integration/schema-invariants.test.ts (style + connection precedent for a proof slice)
- modules/platform/{package.json, tsconfig.json, tsconfig.test.json, vitest.config.ts, index.ts} — the scaffold
  `modules/wms` must replicate EXACTLY (WBS 0.16 did the same for modules/identity)
- docs/package/38-WBS.md row 2.1 (acceptance, verbatim below)

## Write ONLY
- modules/wms/{package.json, tsconfig.json, tsconfig.test.json, vitest.config.ts, index.ts}  (scaffold, copied from modules/platform with the name changed to `@pg-eos/wms`; index.ts is the same empty shell with an M04 comment)
- modules/wms/tests/integration/wh1-setup.feature
- modules/wms/tests/integration/wh1-setup.test.ts
- pnpm-lock.yaml (regenerated by `pnpm install` — never hand-edited)
Forbidden: database/**, packages/**, apps/**, any other module, scripts/**, docs/**. NO migration.
`scripts/new-slice.sh` is a no-op until 2.9 — this is not a use-case slice, so no hexagonal tree is created;
the scaffold mirrors modules/platform and modules/identity exactly (the standing substitute, CHANGELOG 0.16).

## Acceptance criterion (doc 38 row 2.1, verbatim)
"Blocks are `P-A` 288 · `P-A1` 12 · `G-B` 906 · `G-C` 45 · `M-B` 906 · `M-C` 45 · `T-B` 906 · `T-C` 45;
storage capacity 3,153; 3,301.641 m³"

## Source of truth for every number: doc 19 §3-2 (lines 127-135) — copy them, never derive them from the DB
| block | type | zone | positions (locations) | m³ |
|---|---|---|---|---|
| P-A  | A  | P | 288 | 507.384 |
| P-A1 | A1 | P | 12  | 21.141 |
| G-B  | B  | G | 906 | 880.632 |
| G-C  | C  | G | 45  | 43.740 |
| M-B  | B  | M | 906 | 880.632 |
| M-C  | C  | M | 45  | 43.740 |
| T-B  | B  | T | 906 | 880.632 |
| T-C  | C  | T | 45  | 43.740 |
| **Σ** | | | **3,153** | **3,301.641** |
**Citation discipline (review finding 3):** `positions` and `m³` come from doc 19 §3-2 lines 127-135 (which have
type A/A1/B/C and NO zone column); `zone` letter and `block_type` (pallet_rack / shelf) come from doc 19 lines
256-263 (the `019` insert tuples). Cite each literal from the line it actually appears on.
**Numbers are doc literals, not display formats (finding 2):** doc 19 says `288` and `3,153` — never write
`'288.000'` / `'3153.000'`; compare in SQL with `= $n::numeric` and return the actual text only for the failure
message. m³ text `'507.384'` IS the doc literal and may be compared as text or numeric.

### Zones — doc 19 §3-1 (line 111-119), §2-4 (93-102), lines 224-240; doc 40 line 239
| code | zone_type | locations |
|---|---|---|
| P | storage | 300 (P-A 288 + P-A1 12) |
| G · M · T | storage | 951 each (B 906 + C 45) |
| RCV | receiving | 6 |
| STG | staging | 8 |
| SHP | shipping | 6 |
| QRT | quarantine | 4 |
| RTN | returns | 4 |
| DMG | damaged | 2 |
| X | structural | 147 |
Eleven zones exactly; operational total 30; structural 147; storage 3,153 (doc 40 line 239).
Locations join blocks via `wms.locations.space_block_id`. Warehouse `WH1` belongs to entity `PST`
(`wms.warehouses.entity_id → platform.entities.code = 'PST'`). Zones present: DMG G M P QRT RCV RTN SHP STG T X.

## Scenario (Gherkin first — modules/wms/tests/integration/wh1-setup.feature)
Feature: WH1 registered exactly as doc 19 §4 describes (WBS 2.1)
  Background: the dev database has `database/schema/apply.sh` applied (01 · 13 · 13B · 019 + migrations)
  Scenario: the warehouse exists once and belongs to PST
    Then wms.warehouses has exactly one row with code WH1, and its entity is PST
  Scenario Outline: each of the eight space blocks matches doc 19 §3-2
    Then wms.space_blocks has exactly one row with code <block> for WH1
    And that row is in zone <zone>
    And its block_type is <type>
    And its capacity_pallets equals <positions>
    And its capacity_cbm equals <m3>
    And its status is active
    And exactly <positions> wms.locations rows reference it via space_block_id
    Examples: the eight rows of the table above
  Scenario: the eight blocks are exactly the eight doc-19 codes and no other
    Then the sorted codes of wms.space_blocks for WH1 equal [G-B, G-C, M-B, M-C, P-A, P-A1, T-B, T-C]
  Scenario: totals
    Then Σ capacity_pallets over WH1's blocks equals 3,153 (doc 19 line 135, numeric equality)
    And Σ capacity_cbm equals 3,301.641 (doc 19 line 135, numeric equality)
  Scenario Outline: each of the eleven zones matches doc 19 §3-1 / §2-4
    Then wms.zones has exactly one row with code <zone> for WH1
    And its zone_type is <type>
    And exactly <locations> wms.locations rows belong to it
    Examples: the eleven rows of the zones table above
  Scenario: the zones are exactly the eleven doc-19 codes and no other
  Scenario: operational and structural totals — 30 operational locations (RCV 6 STG 8 SHP 6 QRT 4 RTN 4 DMG 2)
    and 147 structural (zone X) — doc 40 line 239
  Scenario: the schema's own verification agrees — wms.verify_wh1() returns 21 rows, all passed = true
    (assert the count AND that every row's passed is true; print the failing check_name on failure)
  Scenario: nothing was entered by hand — every wms.zones / wms.space_blocks / wms.locations row in the
    database carries WH1's warehouse_id (a whole-table check; it will legitimately go red the day a second
    warehouse is registered — Master decision (finding 5): keep it whole-table now, and the slice that
    registers a second warehouse narrows it to "for WH1" as part of its own acceptance)
    (each clause above is its own Gherkin step — no keyword-less continuation lines; review finding 6)

## Test rules
- Connect exactly like modules/platform/tests/integration/schema-invariants.test.ts (pg Pool, PG* env, same defaults).
  Read-only: this suite must not INSERT/UPDATE/DELETE anything — it proves seeded state.
- Numbers are literals copied from the doc-19 line they appear on (§3-2 lines 127-135 for positions/m³; lines
  256-263 for zone/block_type; §3-1/§2-4 for zones) with a comment citing that line; never computed from a query.
- Compare numeric columns by exact numeric equality in SQL (`= $n::numeric` with the doc literal bound), never by
  float arithmetic and never by the database's display text (`'288.000'` is a numeric(14,3) format, not a doc
  literal — review finding 2). m³ literals like `'507.384'` may also be compared as text, since that text is the doc's.
- Expect this suite to be GREEN immediately (the schema is delivered) — report RED or GREEN honestly; a RED
  here is a real finding about 019 or doc 19, not something to fix in the test.
- Run: `pnpm --filter @pg-eos/wms test` · `pnpm --filter @pg-eos/wms typecheck` · `npx eslint modules/wms`.
  Then confirm `pnpm typecheck` (root, turbo) still 11/11 and `pnpm lint:boundaries` still A–F.
- Constraints (CLAUDE.md · AGENT CONSTRAINTS): no any / @ts-ignore / eslint-disable / console.log / magic numbers
  (the doc-19 literals are named constants with the citation) / fabricated values.

Contract: none (proof slice — derive nothing).      Screen/Board spec: none.
Migration number: none.
Stop-and-ask if: any table/column/rule needed is not in 01 / 13 / 13B / 019 / 40, or if doc 19 §3-2 and
`019` disagree with each other — report both numbers, do not pick one.

## Report back (§5 REPORT format)
Files changed · Files read outside list · Tests: x/y with exact vitest output · Guards: n/a (no schema change;
G7/G14 unaffected) · Open questions · Model · Tokens (approx).

## Review round 1 — pg-reviewer FAIL(9), 2026-09-23
1–6 → pg-tester (zones unproven against doc literals; `.000` display format used as a literal; wrong citation for
zone/type; "exactly 8" without the codes and with a magic `'8'`; feature narrower than the test; invalid Gherkin
continuation lines). 7 → this brief (Read ONLY widened above to what was legitimately read). 8 → the wip
checkpoint `deb75d5` is squashed into the single closing commit (not on main). 9 → work moved to branch `lane/2`.
