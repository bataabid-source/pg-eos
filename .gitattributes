# PG-EOS — line-ending policy (D-122, GM 2026-09-23). Windows Git Bash tooling (sed -i, some
# editors) has repeatedly introduced CRLF into files this repo keeps as LF; this is the fix.
* text=auto eol=lf
*.ps1 text eol=crlf
