# MIGRATION-REQUEST-3 — lane 3 (imile → fleet, 2026-09-25)

| # | module | slug | purpose (one line) | RED test paths | requested | issued by Master |
|---|---|---|---|---|---|---|
| 1 | imile | `shipments-version` | `imile.shipments.version int not null default 1` (+ column_classification row) — WBS 3.14 part 2's `PullShipments` command updates existing rows (iMile-sourced columns only) and CLAUDE.md · ARCHITECTURE requires an optimistic-lock column on every mutable aggregate; verbatim replica of 0008/0013/0014/0017/0019/0020 | `modules/imile/tests/pull-shipments/pull-shipments.feature`, `modules/imile/tests/pull-shipments/pull-shipments.test.ts` | 2026-09-25 | **0024** — issued in the Master's commit `3eb4465` 2026-09-25 ("0024 = imile.shipments.version (3.14 part 2)"); file `database/migrations/0024_3_shipments-version.sql`; pre-migration pg-reviewer APPROVED WITH CHANGES; applied |
| 2 | hr | `commission-daily-rls-version` | `hr.commission_daily` (a) internal-only INSERT+UPDATE policies, entity-scoped, both USING and WITH CHECK — the table has RLS enabled but only one SELECT policy, confirmed live against `pg_policies` by lane 3 and the Master independently; (b) the existing `own_commission` SELECT policy is recreated with `entity_id = any(platform.allowed_entities())` AND-ed onto its existing condition (a `read_all` holder currently sees every entity's rows, not just their own); (c) `version int not null default 1` + column_classification row (mutable aggregate, no optimistic-lock column existed) — same class of gap as SCR-SALES-ACCT-01 / migration 0021 (`sales.accounts` missing internal write policy); (d) `imile.shipments_attributed` (doc 38's own attribution mechanism) had no GRANT to `pgeos_app` at all (0007's role-grant loop excludes views by design) — fixed with `security_invoker = true` FIRST (verbatim structure of migration 0012), then the GRANT, never a bare GRANT: the view's owner is `postgres`, so without the invoker option an owner-rights view would bypass RLS on `imile.shipments`/`driver_ids`/`driver_id_assignments` entirely, a cross-entity leak (Master's own catch before this shipped) | `modules/hr/tests/calculate-daily-commission/calculate-daily-commission.feature`, `modules/hr/tests/calculate-daily-commission/calculate-daily-commission.test.ts`, `tests/isolation/tests/shipments-attributed-invoker-rls.test.ts` (RED confirmed 2026-09-26 — module/contract not found; the RLS-gap RED layer triggers once pg-backend's implementation exists, item (d) added after the Master's review) | 2026-09-26 | **0027** — issued by the Master 2026-09-26 (WBS 3.13 part 1); file `database/migrations/0027_3_commission-daily-rls-version.sql`; pre-migration pg-reviewer review mandatory before the file is written (SECOND pass covers item (d), added after the first APPROVED WITH CHANGES round); P6 batch moves to 0028+ (0028 reserved, not consumed by this slice) |
Row 2 (`tms.vehicles` "missing" entity_scope RLS) is **WITHDRAWN** (2026-09-25, Master's correction) — the static `01-Data-Model.sql` grep that found it missed 13B's dynamic policy-generation loop (ق-9), which grants `entity_scope` to every `entity_id`-carrying table not otherwise excepted. Verified live: `select policyname, qual from pg_policies where schemaname='tms' and tablename='vehicles'` returns `entity_scope | (entity_id = ANY (platform.allowed_entities()))`. Lesson recorded: verify a claimed RLS gap against `pg_policies` on the live DB, not static schema files, before writing a request row (same method that confirmed SCR-RLS-03).

Row 1 is closed (applied, part of the 3.14 part 2 commit already merged). No open migration request for WBS 3.1 — 13B already has every column the slice needs.

**G-01 candidate filed, not built this slice (Master directive, 2026-09-25, WBS 3.1):** DB-level enforcement of doc 40 INV-C4-1 (vehicle half) on the assignment columns — `tms.delivery_tasks.vehicle_id`, `tms.routes.vehicle_id` (01-Data-Model.sql:890, 907) — is not yet in the database. WBS 3.1 delivers the application-level gate (`fleet.AssertVehicleAssignable`, throws `VehicleNotAssignableError` on any expired document), which is the correct enforcement point for now since the delivery module that will WRITE those columns doesn't exist yet and cannot import `fleet` (cross-module import is a lint-enforced boundary, CLAUDE.md ARCHITECTURE — not a D-179 use-case-lock matter). This is **required, not merely "should be", before WBS 3.4** (or whichever slice first writes `vehicle_id` on `tms.delivery_tasks`/`tms.routes`) — a trigger on those two tables, or a `fleet.assert_vehicle_assignable(uuid)` SQL function the delivery module's own migration/trigger calls, same pattern as `wms.check_space_available()`.

**Pre-existing gap this note must also record (pg-reviewer, round 1 of 3.1's final scope, finding 11a):** `hr.shift_groups.vehicle_id` (migration `0016_2_hr-shifts-groups-assignments.sql:97`, already applied) is an EXISTING, WRITABLE assignment column this gate does not cover and never will retroactively — WBS 3.1 does not touch `hr.shift_groups`, and whether a shift-group's vehicle link counts as an "assignment" under INV-C4-1 the way a delivery task's `vehicle_id` does is a GM/Master call, not decided here. Recorded as a known gap, not silently missed.

Not a migration request today (no schema change needed for WBS 3.1 itself); recorded here so the slice that first writes `tms.delivery_tasks.vehicle_id`/`tms.routes.vehicle_id` finds this note.
