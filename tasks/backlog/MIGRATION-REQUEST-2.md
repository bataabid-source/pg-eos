# MIGRATION-REQUEST-2 — lane 2 (wms)

| # | module | slug | purpose (one line) | requested | issued by Master |
|---|---|---|---|---|---|
| 1 | wms | `inventory-counts-version` | `wms.inventory_counts.version int not null default 1` (+ column_classification row) — optimistic-lock column for WBS 2.13's StartCount/CountLocation/Recount/AdjustCount, shape of 0008/0014/0015/0016. pg-reviewer pre-migration review: **APPROVED WITH CHANGES (3 findings, comment-only, all applied)**. | 2026-09-25 | **0018** (`f070f62`, applied, verified) |
| 2 | wms | `schedule-inbound` | `wms.inbound_orders` + `scheduled_by uuid`, `scheduled_at timestamptz`, `cancel_reason text`, `dock_code text`, `handover_point text`, `transport_by text`, `labour_by text`, `labour_count int`, `delivery_task_id uuid` (nullable, no FK — mirrors `wms.outbound_orders.delivery_task_id`, 01:765, also no FK; never written this slice, D-180/D5), check on `vehicle_type` (closed list), all classified — WBS 2.9b, `tasks/backlog/2.9b-schedule-inbound.md`, SCR-WMS-INB-01 §7-8. RED test paths naming the columns before the file exists: `modules/wms/tests/schedule-inbound/schedule-inbound.test.ts`, `modules/wms/tests/receive-inbound/receive-inbound.test.ts` (extended — approve-with-slot / cancel-reason, under the scoped `wms/receive-inbound` grant, PR #53). Pre-migration pg-reviewer review: round 1 FAIL(5: 1 classification, 1 column removed [schedule_note], 3 comment) → fixed → round 2 FAIL(2, header-wording only) → fixed → round 3 **APPROVED**, applied live. | 2026-09-25 | **0023** (issued by Master, D-180 batch) |
| 5 | billing | `dimensions` (part 1) | `database/migrations/0030_2_dimensions.sql` — **ONE new entity-scoped table** (SCR-ACC-01 row 9, part 1 only after pre-migration round-1 finding 1/Master ruling): `billing.dimension_types` (entity_id, code, name_ar, name_en, is_active, version, `kind` ∈ {list,reference}, `source_table` nullable/CHECK-whitelisted when kind='reference' — D-190 hybrid-design ruling). `entity_scope` RLS (not `reference_write` — avoids 4.1a's `platform.reference.manage` no-holder gap), `identity.column_classification` rows for every new column. No dimension type seeded (D3). `billing.line_dimensions` (+ `dimension_values` for list-kind, `value_id`/constraint-trigger for reference-kind, `value_ref` DROPPED entirely) is deferred to `4.1b part 2` — a half-record line-tag table with no value column is the wrong migration order. RED test paths (existed before the migration file, as required): `modules/billing/tests/dimensions/dimensions.feature`, `modules/billing/tests/dimensions/dimensions.test.ts`, `modules/billing/tests/dimensions/invariants.property.test.ts` (brief `docs/notes/slice-briefs/_slice-4.1b.brief.md`, SCR-ACC-01 §3). Pre-migration + slice-close pg-reviewer review: 29 findings across 5 rounds total (pre-migration round 1 FAIL(12, scope split ruled) → round 2 FAIL(11, incl. a real CHECK three-valued-logic bug: `kind='reference' AND source_table IS NULL` silently ACCEPTED without an explicit `source_table is not null`) → confirmation FAIL(1) → CLOSED; slice-close round 1 FAIL(5) → round 2 PASS(0)). **Scope this part: `billing.dimension_types` ONLY — `billing.line_dimensions` NOT in this migration.** Applied and verified live on `pgeos_lane2`, 46/46 tests green. | 2026-09-26 | **0030** (applied, verified 46/46 green) |
| 6 | billing | `gl-account-change-requests` (4.1a part 2) | `database/migrations/0034_2_gl-account-change-requests.sql` — new entity-scoped `billing.gl_account_change_requests` table (maker/checker for `billing.gl_accounts` writes, D-190 hybrid ruling): `change_kind ∈ {create,update}` (deactivate/reactivate deferred to 4.1a part 3, needs its own `is_active` column — G-01), proposed_* columns, `status ∈ {draft,pending_approval,approved,rejected,cancelled}` (XState machine in domain/), `approved_by <> requested_by` CHECK (also structurally guaranteed by the existing `identity.sod_rules (CFO,ACCOUNTANT)` row, 13B:632), a partial-unique-index pair (one pending request per target_account_id / per proposed create code), `doc_no` via `platform.next_doc_no` (new `GLC` counter type, seeded per entity). A shared `immutable` SQL function validates code-format/account_type on BOTH `gl_accounts` (0028's two CHECKs refactored to call it) and this table's proposed_* columns. Parent-in-same-entity is a **composite FK**, not a function/trigger (Master ruling, precedent 0026): `unique (id, entity_id)` added to `gl_accounts`, `gl_account_change_requests(proposed_parent_id, entity_id) → gl_accounts(id, entity_id)`, AND a retrofit `gl_accounts(parent_id, entity_id) → gl_accounts(id, entity_id)` closing a pre-existing gap (safe, nothing seeded in gl_accounts). New permission `billing.gl_accounts.manage` → ACCOUNTANT (role_permissions seed); CFO approval via `platform.approval_chains` (`gl_account_change`, step 1, CFO) read as data, never hard-coded. The actual `gl_accounts` write happens ONLY inside Approve, same transaction, with `requested_by`+the request id in the audit row's `new_value` alongside the approving CFO's own `user_id`/`actor_type` (no schema change to `platform.audit_log`). **Approver mechanism (SCR-PLAT-APPR-01, round-1 finding 2's resolution, D-190):** `platform.is_approval_chain_approver(request_type, step)` — `stable security definer`, pinned search_path (0009 style) — the first consumer of a generic primitive 4.19/4.20/4.22/5.11b will reuse; approver SELECT/UPDATE RLS policies on `gl_account_change_requests` (pending→approved|rejected only, decision columns required); a `gl_accounts` INSERT/UPDATE policy OR-ed with `reference_write`; a trigger `billing.assert_gl_account_change_approved()` binding every `gl_accounts` write to an approved request whose EVERY proposed_* column matches what was actually written (`is not distinct from`, round-2 finding 1) and whose `decided_at = now()` (replay prevention — the match must have been decided in the SAME transaction as the write, round-2 finding 2, Master's exact mechanism) — bypassed only when `current_user <> 'pgeos_app'` (seed/apply.sh/import); a SECOND trigger `billing.assert_gl_account_change_immutable_fields()` on the request table itself blocking `requested_by`/`proposed_*`/`change_kind` edits once a request is past `draft` (round-2 finding 3, closes a cross-policy self-approval hole). RED test paths naming the columns before the file exists: `modules/billing/tests/gl-account-change-requests/{gl-account-change-requests.feature,gl-account-change-requests.test.ts,machine.unit.test.ts,invariants.property.test.ts,invariants.unit.test.ts,contract.test.ts,approver-mechanism.test.ts}` (brief `docs/notes/slice-briefs/_slice-4.1a-part2.brief.md`). Pre-migration pg-reviewer review (opus, Master reads the diff first-hand — touches `platform.*`): round 1 FAIL(17, incl. the core finding that no approver write-path existed at all) → Master/evaluation-session ruling (SCR-PLAT-APPR-01) → round 2 FAIL(13, incl. 3 real security gaps: trigger didn't check written content matched the proposal, an approved request could be replayed indefinitely, and RLS policies could be combined across the maker/approver split to self-approve) → D-117 last round FAIL(8, incl. a NULL-clearing gap on nullable proposed columns, the replay guard not actually enforced by RLS itself, and a maker draft-rewind loophole) → confirmation pass (2 mechanical findings — a test needing to isolate each nullable column, a brief-wording correction — fixed in place) → PASS. Two additional bugs found and fixed via live testing (not review): a statement-ordering bug (the approver function was referenced before its own definition) and a policy-naming collision (a RESTRICTIVE policy literally named `entity_scope` collided with migration 0007's self-healing loop — renamed to `glc_entity_scope`, same precedent as `idem_entity_scope`/0010, registered in the P5 isolation matrix's `NAMED_ENTITY_SCOPE_EXCEPTIONS`). Catalog events `billing.gl_account_change.{requested,approved,rejected}` — Master adds to `packages/events/catalog.ts` before merge, not built by the lane. Slice-close review (separate from the pre-migration gate above): round 1 FAIL(8) → round 2 FAIL(3, 1 fixed in place — a migration header comment, 2 deferred to `4.1a part 2c`, no round 3 per REVIEW CAP). Applied and verified: 198/198 module tests green, 207/207 `tests/isolation` matrix green (`glc_entity_scope` registered in `NAMED_ENTITY_SCOPE_EXCEPTIONS`), `pnpm guards:run` all blocking guards green. | 2026-09-26 | **0034** (issued by the Master; 0032 is lane 1's, 0033 is lane 3's; applied, verified) |
| 3 | billing | `record-billable-event` | **NO MIGRATION NEEDED.** Investigated per D-186 item 3 before requesting a number: `billing.billable_events` (table, all 21 columns, RLS `entity_scope`, `chk_billable_events_status` CHECK) already exists in full (`01-Data-Model.sql:1048-1073`), every column already classified (verified live), and the unique index `(source_table, source_id, service_id)` that IS doc 38 line 152's acceptance criterion ("Same event cannot bill twice") already exists live — `13B-Schema-Reference-Consolidation.sql:4129-4130` (ق-38) explicitly says this index is NOT to be recreated by any migration. WBS 4.2 is a pure build task, no schema change. **RESCOPED (round-1 review finding 1, Master ruling):** `01:1047` says the table is "generated automatically from domain events, no manual entry" — 4.2 delivers domain + infrastructure ONLY (`modules/billing/{domain,infrastructure}/record-billable-event/**`), a repository insert port for WBS 4.3's future subscribers, no application command, no api layer. RED test path: `modules/billing/tests/record-billable-event/record-billable-event.test.ts` (brief `docs/notes/slice-briefs/_slice-4.2.brief.md`). | 2026-09-25 | **none requested** |
| 7 | billing | `gl-accounts-is-active` (4.1a part 3) | `database/migrations/0036_2_gl-accounts-is-active.sql` — delta over migration `0034` (SCR-ACC-01 row 1 part 3, doc 38 row 4.1a part 3): (1) `alter table billing.gl_accounts add column is_active boolean not null default true` + one `identity.column_classification` row (separate axis from `is_postable` — lifecycle vs. header/leaf). (2) Widen `chk_glc_requests_change_kind` to `change_kind in ('create','update','deactivate','reactivate')`. (3) Widen `chk_glc_requests_change_kind_target`: `create` still requires a null `target_account_id`; `update`/`deactivate`/`reactivate` all require a non-null one. (4) Extend `billing.assert_gl_account_change_approved()` (the existing plain BEFORE INSERT OR UPDATE trigger on `gl_accounts`): a NEW unconditional check (runs BEFORE the `current_user <> 'pgeos_app'` bypass — a data-integrity invariant, not a maker/checker rule, so seed/import must obey it too) rejects (`23514`) any UPDATE setting `is_active` true→false when a child row (`parent_id = new.id`) is still `is_active = true`; the existing UPDATE-branch `exists` subquery gains two new disjuncts (`change_kind = 'deactivate'`/`'reactivate'`, each requiring every OTHER mutable column unchanged and `is_active` flipping the correct direction against `old.is_active`), and the existing `update` disjunct gains `new.is_active is not distinct from old.is_active` (an `update` request must never touch `is_active`); the INSERT branch gains `new.is_active is not distinct from true` (defense-in-depth, matches the column default). The three `update`/`deactivate`/`reactivate` disjuncts are each keyed on `r.change_kind` (0034's own UPDATE-branch subquery had no such key, matching on `target_account_id` alone — this is a real tightening, not just an additive change). The UPDATE-mismatch rejection message's prefix changed from `0034:` to `0036:` since this migration reworded that message's text — this forced 6 pre-existing `approver-mechanism.test.ts` assertions to update their expected prefix, and 2 negative checks there to additionally exclude `0036:`, both fixed within this slice. Domain: `isValidChangeKindTargetPairing`/`isProposedCodeOnlyForCreate` TS type widens to the 4-value union (bodies unchanged — already generalize); new pure invariant `isValidDeactivateReactivateDirection`/`assertValidDeactivateReactivateDirection` (no I/O, `currentIsActive` passed as a plain parameter) + new error `InvalidDeactivateReactivateDirectionError`; slice-close review round 1 added a second pure pair, `isDeactivationAllowed`/`assertDeactivationAllowed` (no I/O, `hasActiveChild` a plain parameter — the domain-level counterpart of the DB trigger's active-children check) + new error `ActiveChildBlocksDeactivationError` (renamed by the Master from an initially-backwards draft name). Contract: `packages/contracts/billing/gl-account-change-requests.ts` `CHANGE_KIND` enum widens to 4 values; the `targetAccountId` pairing `.refine` widens from `changeKind === 'update'` to `changeKind !== 'create'`; the `proposedCode` immutability `.refine` widens from `changeKind !== 'update'` to `changeKind !== 'create'`. No new `proposed_*` column — direction is implied entirely by `change_kind` + the target's current `is_active`. No application/api/infrastructure layer this part (same budget precedent as part 1/part 2, deferred to `4.1a part 2b`). Non-zero-balance enforcement explicitly OUT of scope (belongs to 4.20/4.23, per the Master's prior ruling carried in MASTER_BACKLOG row 151). Brief: `docs/notes/slice-briefs/_slice-4.1a-part3.brief.md` (7 files / 723 lines, builder: pg-backend-core). RED test paths (already exist, extending the 4.1a part 2 use case — no new tree): `modules/billing/tests/gl-account-change-requests/{gl-account-change-requests.feature,gl-account-change-requests.test.ts,invariants.property.test.ts,invariants.unit.test.ts,contract.test.ts}` — pg-tester confirmed RED (26 new cases across the 5 files: 8 unit + 5 property + 4 contract + 9 integration, all failing for the expected reason — missing domain export, un-widened enum, or DB CHECK/column not yet existing; `approver-mechanism.test.ts` originally untouched at RED time, later updated in the pre-migration fix round (6 assertions re-prefixed `0034:`→`0036:`, 2 negative checks widened) once the migration's own reworded message forced it — still 44/44 green after that update). Applied and verified live on `pgeos_lane2`: 258/258 tests green across all 6 files in `modules/billing/tests/gl-account-change-requests/` (412/412 across the whole billing module at slice-close); `pnpm guards:run` all blocking guards green (G1-G14/G18/G-SEED; G15-G17 not runnable/non-blocking). Pre-migration pg-reviewer (opus) review: round 1 FAIL(6, all separable — test gaps/isolation/mislabels + one stale `0034:` message prefix) → round 2 PASS(0). Slice-close review (separate gate, doc 36 §5-4): round 1 FAIL(5: a missing real-`pgeos_app`-session active-children rejection test, a missing domain-level `isDeactivationAllowed` invariant, two rule-wording messages phrased as violations rather than rules, two stale comments, and this row's own incompleteness) → round 2 FAIL(2, both separable/non-security: this row still omitted the fix-round's own new `isDeactivationAllowed`/`ActiveChildBlocksDeactivationError` pair — now added above — and `isCreateRequestComplete`/`assertCreateRequestComplete` in `invariants.ts` still type `changeKind` as the narrow `'create'\|'update'` union rather than the widened 4-value type, a comment/type-only inconsistency with no behavior impact) → no round 3 per REVIEW CAP (D-186/P7); the type-widening finding is deferred to `4.1a part 3 part 2` (MASTER_BACKLOG). | 2026-09-26 | **0036** (issued by the Master as `database/migrations/0036_2_gl-accounts-is-active.sql`; 0035 is lane 3's, 3.13 part 5a; next free 0037) |
| 4 | billing | `chart-of-accounts` | `database/migrations/0028_2_chart-of-accounts.sql` — ships ONLY the two CHECK constraints: `billing.gl_accounts.code` for the `X-XX-XXX-XXX` format with class 1–9 from the first segment (01:1181, SCR-ACC-01 #1), and `billing.gl_accounts.account_type` restricted to the existing 5 values plus the SCR-ACC-01 #2 additions (`cost_of_revenue`, `other_income_expense`, `tax`, `control_memorandum`). No `column_classification` row (no new column — both constraints are on existing columns). RLS unchanged (`reference_read` / `reference_write`, 13B:3078). No account row seeded (ADR-0004 D3, synthetic pilot chart D-127 — fixtures only, never in the migration). **A `platform.reference.manage` → SYSADMIN role-permission grant was proposed by the Master (round 1) to unblock a write path, then WITHDRAWN in round 2**: that permission spans every reference table (not just `gl_accounts`), letting SYSADMIN self-escalate with no second approver (doc 22:186, SoD finding) — and the cited source, SCR-ACC-01 row 29, actually asks for `identity.roles` seed rows, not a `role_permissions` grant (mis-applied citation). Ruling: part 1 ships domain + contract + the two CHECKs only — no write path at all (no `insertGlAccount`, no repository, no api layer). The write path + a narrow `billing.gl_accounts.manage` permission (four-eyes via `platform.approval_chains`, CFO approval, requester ≠ approver) is deferred to WBS `4.1a part 2` (MASTER_BACKLOG). RED test paths (existed before the migration file, as required): `modules/billing/tests/chart-of-accounts/chart-of-accounts.feature`, `modules/billing/tests/chart-of-accounts/chart-of-accounts.test.ts`, `modules/billing/tests/chart-of-accounts/invariants.property.test.ts` (brief `docs/notes/slice-briefs/_slice-4.1a.brief.md`). Pre-migration pg-reviewer review: round 1 FAIL(10) → round 2 FAIL(8, incl. the SoD/citation correction above) → confirmation pass PASS(0). | 2026-09-26 | **0028** (issued by the Master, applied, verified — 74/74 green) |
