# SLICE BRIEF — WBS 0.4

```
Task: 0.4 — Initialise monorepo (pnpm, Turborepo, TypeScript strict, ESLint boundaries)
Lane: B          Lock: repo root tooling · modules/platform · modules/identity (shells only)
Read ONLY: CLAUDE.md · docs/package/36-Technical-Architecture-Audit.md §1-2 and §8
           · tasks/MASTER_BACKLOG.md (row 0.4) · docs/PROJECT_STATE.md · scripts/new-slice.sh
Write ONLY: package.json · pnpm-workspace.yaml · pnpm-lock.yaml · turbo.json · tsconfig.base.json
            · tsconfig.json · eslint.config.mjs · .gitignore · scripts/check-boundaries.sh
            · modules/{platform,identity}/** · apps/.gitkeep · packages/.gitkeep
            · tests/{scenarios,load,guards}/.gitkeep · database/{migrations,seeds}/.gitkeep
Scenario:
  Given a workspace that contains more than one module
  When a file in one module imports another module
  Then lint fails with the boundary rule — by package name, by relative path, and when the
       relative path does not resolve; and an `eslint-disable` comment does not rescue it
  And when no such import exists, `pnpm build`, `pnpm typecheck` and `pnpm lint` are green
  And a file at modules/<m>/domain/ is type-checked, so `pnpm build` is never vacuously green
Contract: none (infra task — no domain surface)
Screen/Board spec: none
Deliver: root workspace config · shared strict tsconfig · flat ESLint config (boundaries v7
         policies + per-module no-restricted-imports + noInlineConfig) · two module shells
         · scripts/check-boundaries.sh (the executable form of the acceptance criterion)
Migration number: none
Stop-and-ask if: any table/column/rule not in 01 / 13 / 13B / 019 / 40.  (Not reached — no schema touched.)
```

## Amendments made during the slice (recorded, not back-dated)

- **Write ONLY** originally omitted `tsconfig.json`, `pnpm-lock.yaml`, `database/seeds/.gitkeep`
  and `.gitignore`; all four were written. pg-reviewer raised it as finding 5 and the list above
  is the corrected one. `pnpm-lock.yaml` is an unavoidable byproduct and must be committed.
- **Read ONLY** gained `docs/package/36 §1-2` and `scripts/new-slice.sh` — needed to establish the
  mandated module layout after finding 2.

## Process deviations recorded by the Master

1. **Tier.** `docs/MODEL_ROUTING.md` §4 routes a build of this size to **pg-backend (sonnet)**, and
   `docs/PROJECT_STATE.md` names pg-backend for 0.4. It was built **Master-direct on opus** instead,
   because the session opened on `Desktop/New sys` — one level above the repo root — so
   `.claude/agents`, `.claude/commands` and `.claude/hooks/lane-guard.sh` were never registered by
   Claude Code and no worker agent existed to delegate to. pg-reviewer raised this as finding 6.
   **It is not retro-fixable inside this session.** Every later session must start inside
   `claude-kit/`; `/resume` and `/slice` being offered as commands is the check that it did.
2. **Review.** The ten-point review ran on opus with `.claude/agents/pg-reviewer.md` loaded as its
   role, for the same reason. Verdict: FAIL (9 findings) → all nine closed → see CHANGELOG.
3. **RED first** held: `scripts/check-boundaries.sh` was written and run before any config existed
   (it failed), and each fix was proved by a negative control — reverting the fix makes the
   corresponding assertion fail again. Controls run: finding 1 (remove `noInlineConfig` → E fails),
   finding 2 (restore the `src/`-scoped tsconfig → F fails).
4. **Guards G1–G18 NOT RUN** — no database on this machine (psql 16 absent) and no SQL in the diff,
   so none can have regressed. Not recorded as green.
