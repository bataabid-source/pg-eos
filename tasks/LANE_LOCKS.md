# LANE_LOCKS — module ownership, one row per claimed module

| module | lane | task | claimed_at | worktree |
|---|---|---|---|---|
| pda | 1 | 2.16 | 2026-09-26 | ../pg-eos-lane-1 (first-ever `pda` slice, whole-module lock — same standing as `admin`'s own 0.19 bootstrap; 2.16 part 1a DONE-in-part @ `5e71c96` — PDA PWA shell: nine placeholder routes, kiosk mode, i18n in all six locales, PASS(7 findings, 2 rounds), 1 test-coverage item deferred; **2.16 part 1a-2 DONE-in-part @ `67ef627`** — offline queue (72-h IndexedDB) + PWA manifest/service-worker + kiosk-mode test coverage, 50/50 tests green, PASS(9 findings, 2 rounds); part 1a-3 next (OTP login), same lock) |
| wms/receive-inbound | 1 | 2.11 | 2026-09-25 | ../pg-eos-lane-1 (2.11 part 2 DONE-in-part @ `156b042` — test-files-only grant, item 0 complete: `pickFreshWh1Location`/`nextFixtureLocationCode()` now create their own M9 locations instead of selecting shared/orphan rows; row kept open into part 3 per the Master's instruction; no domain/application/api file) |
| billing | 2 | 4.1a | 2026-09-25 | ../pg-eos-lane-2 (WBS 4.2 DONE (all three parts) @ `4ba65a4`. WBS 4.1a part 1 DONE @ `b31fe44`. **WBS 4.1a part 2 DONE-in-part @ `4585fce`** — `billing.gl_account_change_requests` maker/checker write path for `billing.gl_accounts` (D-190 hybrid ruling), the new generic `platform.is_approval_chain_approver()` approval-chain primitive (SCR-PLAT-APPR-01, reused by 4.19/4.20/4.22/5.11b), migration `0034` applied and verified (198/198 module tests, 207/207 isolation matrix, guards green). Combined review PASS(49 findings fixed, 2 deferred to `4.1a part 2c`, 6 rounds: pre-migration round 1 FAIL(17) → ruling → round 2 FAIL(13) → D-117 round 3 FAIL(8) → confirmation FAIL(2) → confirmation PASS(0); slice-close round 1 FAIL(8) → round 2 FAIL(3, 1 fixed in place, 2 deferred)). Open: `4.1a part 2b` (Submit/Approve/Reject/Cancel application/api layer, deferred for budget, same precedent as part 1/4.1b part 1). **`4.1a part 2c` DONE @ `<this commit>`** — both deferred slice-close test findings closed, test-only, one file (`approver-mechanism.test.ts`): stale header comments rewritten to GREEN state, 2 new tests (UPDATE-side cross-entity negative + same-entity positive control) added to the existing §9(i) block, 44/44 green, PASS(0 findings, 1 round). Next open item: **`4.1a part 3`** (`is_active` + deactivate/reactivate lifecycle). WBS 4.1b part 1 DONE-in-part @ `921d923` (`billing.dimension_types`, migration `0030` applied, 46/46 green); `billing.line_dimensions` deferred to `4.1b part 2` (design ruled, D-190, not yet built). Row NOT released — lane 2 continues in `billing`; next is **`4.1a part 3`** (`is_active` + deactivate/reactivate lifecycle, per the Master's handoff instruction) — `4.1a part 2b`/`4.1b part 2` remain open backlog items) |
| imile | 3 | 3.12 | 2026-09-26 | ../pg-eos-lane-3 (`imile.driver_ids` + time-bounded assignments + termination trigger — GM-delegated decision, doc-38 Lane column moves 2 → 3 in v4.7 bookkeeping, D-190; whole-module lock, existing `modules/imile`; `AssignDriverId` command only — the termination/suspension triggers already exist at the schema level, not rebuilt; brief ≤ 8 files / 1,000 lines, two-round review cap; 3.12 part 1 committed `b9c3b1d`, part 2 backlog open (8 round-2 findings, MASTER_BACKLOG, no round 3 per REVIEW CAP) — lock held, part 2 continues in this lane; then 3.13, then 3.4 once 2.12 is DONE) |
| hr | 3 | 3.13 | 2026-09-26 | ../pg-eos-lane-3 (3.13 parts 1-4 DONE/DONE-in-part (`6c16c41`/`016cce9`/`fdebfc7`/`05fa568`); **3.13 part 5a DONE-in-part @ `<this commit>`** — dispute-side DB backstop, migration `0035` applied, PASS(6 findings, 2 rounds, accepted as-is per Master ruling); part 5b/5c backlog open (race-test barrier, audit-content assertions, carried from part 4's slice-close review), lock held) |

`wms/process-outbound` (lane 1, task 2.12) released 2026-09-26 — doc-38 row 2.12 is now fully DONE (all 4 parts, `830bfaf` and earlier); lane 1 re-claimed above as the new whole-module lock `pda` for 2.16 part 1a.
`fleet` (lane 3, task 3.1) released 2026-09-26 — 3.1 both use cases DONE-in-part: part 1 `RegisterVehicle` @ `e3137d5` (PASS, 0 open round 2), part 2 `AssertVehicleAssignable` (the real INV-C4-1 gate) @ `8d2ead9` (PASS(1 finding, 2 rounds)). Judgment call recorded in CHANGELOG/MASTER_BACKLOG: the gate mechanism is complete/tested/proven but not yet called by any real assignment write path — that wiring is a future delivery/TMS slice's job (G-01, `tasks/backlog/MIGRATION-REQUEST-3.md`), not this row's own scope. Lane 3 idle, next task pending the Master's assignment (3.4, once 2.12 DONE).
`wms/integration` (lane 1, task 2.11 part 2/3) released 2026-09-25 — its only item, `modules/wms/tests/integration/location-limits.test.ts` `pickWh1Location` (WBS 2.4), is fixed to the collision-safe dedicated-`M9-`-location pattern in 2.11 part 3 @ `f30f7c7` (tests-only grant, no domain/application/api file touched); `wms/process-outbound` + `wms/receive-inbound` stay held for 2.11 part 4 (2 deferred naming items; condition 10 stays BLOCKED, `docs/notes/SCR-WMS-OUT-02-order-quantity-limit.md`).
`wms/integration` (lane 1, task 2.11) released 2026-09-26 — the 2.4 picker fix landed in 2.11 part 3 @ `f30f7c7`.
`wms/schedule-inbound` + `wms/receive-inbound` (lane 2, task 2.9b) released 2026-09-25 — 2.9b DONE @ `716bf8e` (5 review rounds, 38 findings fixed, round 3 escalated to opus per D-117); migration `0023_2_schedule-inbound.sql` applied; lane 2 idle, next task pending Master assignment.
`imile` (lane 3, task 3.14 part 2) row above updates in place, not released — 3.14 part 2 DONE-in-part @ `e667821` (pull loop mechanism, PASS(27 findings/7 rounds)); lane 3 continued in the same module for 3.17 next (D-184).
`imile` (lane 3, task 3.17 part 1) row above updates in place, not released — 3.17 part 1 DONE-in-part @ `ca7f6f1` (`EvaluateDtlProblem` gate evaluation mechanism, PASS(27 findings/4 rounds), D-186 two-round cap hit at round 3, Master fixed at session tier sonnet not opus, deviation recorded); DEL_MGR further parts/auditor screen/nightly job not yet scheduled; lane 3 next: awaiting the Master's `tms` claim for 3.1 (D-184) — the Master claims `tms`, not the lane.

WBS 1.11 is doc-38 Lane **M**, corrected 2026-09-25 from an earlier Master misassignment to lane 1, then investigated on GM instruction and found **BLOCKED** (D-178, `docs/notes/2026-09-25-wbs-1.11-premature.md`) — not a lane-1 row, not buildable today.
`imile` (lane 3, tasks 3.14 / 3.17) released 2026-09-25 — 3.14 part 2 DONE-in-part @ `e667821`, 3.17 part 1 DONE-in-part @ `ca7f6f1`; lane 3 re-claimed above for `tms` (3.1, D-184). Former row note: ../pg-eos-lane-3 (part 1, DTL gate evaluation mechanism, DONE-in-part @ `19646f1`; 3.14 part 2 DONE-in-part on main; lane 3 next awaits the Master's `tms` claim for 3.1 per D-184)
`wms` (lane 1, task 2.10) released 2026-09-25 — 2.10 DONE @ `95a33b8`; re-claimed above as the use-case lock `wms/process-outbound` (D-179/D-180) so lane 2 can build 2.9b in the same module at the same time.

- `0013_1_price-lists-version.sql` — lane 1, task 1.2, applied 2026-09-24 (pg-reviewer pre-migration APPROVED WITH CHANGES; number issued by the Master, MIGRATION-REQUEST-1.md).
- `0019_1_contracts-version.sql` — lane 1, task 1.7, applied 2026-09-25 (pg-reviewer pre-migration APPROVED WITH CHANGES; number issued by the Master, MIGRATION-REQUEST-1.md).

`wms` (lane 2, task 2.15) released 2026-09-25 — Phase 2 complete for lane 2 (2.13 DONE @ `ca9a109`, 2.14 DONE @ `eec1618`, 2.15 DONE @ `758a5fe`); lane 2 now builds `wms/schedule-inbound` (2.9b, D-180) instead of idling. `wms` also freed for lane 1 — claimed above as `wms/process-outbound` for 2.10/2.11.
`sales` + `admin` (lane 1, task 1.9) released 2026-09-25 — 1.9 DONE @ `90faea3` (PR #43).
`hr` (lane 2, task 5.5a) released 2026-09-25 — 5.5a DONE in full @ `ffab3bf` (part 1 `3679b70` + part 2 `ffab3bf`, PRs #19/#30); re-claimed above (`wms`) for Phase 2 (D-176).
`admin` (lane 1, task 0.19) released 2026-09-25 — 0.19 DONE @ `80e827f` (PR #24); re-claimed above (`sales`) for Phase 1 (D-176).
`platform` (lane 2, task 5.5a part 1) released 2026-09-24 — `platform.sites` DONE @ `3679b70` (PR #19); free for lane 3 / 5.18 now that lane 2 no longer holds it.
`hr` (lane 2, task 3.3) released 2026-09-24 — 3.3 DONE @ `9616422` (PR #14); re-claimed above for 5.5a.
`catalog` (lane 1, task 1.2) released 2026-09-24 — 1.2 DONE @ `6a53fc8` (PR #12).
`platform` (lane 3, task 5.13) released 2026-09-24 — 5.13 part 1 committed (alert evaluation mechanism), NOT DONE (part 2: delivery, escalation, dynamic recipients, reports, job).
`wms + packages/db (idempotency) + database/migrations (0010)` (lane M, task 2.9-part2) released 2026-09-24 — GM sheet-3 answers applied (idempotency store, cancel/close rules, variance photo, shared logger); NOT DONE (blocked on SCR-WMS-INB-01 §6, Q10/D-159 GM acceptance).
`wms + packages/contracts/wms + packages/events (catalog)` (lane M, task 2.9) released 2026-09-24 — 2.9 built and reviewed, PASS round 4; NOT DONE (blocked on SCR-PLAT-IDEM-01, SCR-WMS-INB-01 §1–4, observability, GM acceptance).
`wms` (lane 2, task 2.4) released 2026-09-24 — 2.4 DONE.
`packages/db + CI (.github)` (lane M, task 0.6a-2) released 2026-09-24 — 0.6a IN PROGRESS pending GM ruleset decision (first green CI run: #2 @ d8dc887).

## Migrations issued

Not a lock table — deliberately not a `|`-prefixed markdown table, since
`.claude/hooks/lane-guard.sh` parses every such line in this file as a
module-lock row (see Rules below). One line per migration, newest first:

- `0036` — next free number (`0031` applied by P6b-2, Master; `0035` applied by lane 3, WBS 3.13 part 5a).
- `0035_3_commission-daily-dispute-sod.sql` — lane 3, WBS 3.13 part 5a (`hr.guard_commission_daily_status()` extended with a DB-level backstop on the `calculated -> disputed` edge — bound to `old.employee_id`, not `new.employee_id` — mirroring the confirm side's own self-review check; deferred finding from 3.13 part 4's slice-close review), issued by the Master 2026-09-26 on MIGRATION-REQUEST-3 row 4; RED paths `modules/hr/tests/dispute-commission/dispute-commission.test.ts` (two new tests) + fixture-helper fixes in that file, `modules/hr/tests/confirm-commission/{confirm-commission.test.ts,handlers.test.ts}`. Pre-migration pg-reviewer (opus) round 1 FAIL(7, fixed: old.employee_id binding, message/header text, fixture session-actor binding); slice-close round 2 FAIL(6 NEW findings — stale bookkeeping/test comments, a misleading post-rollback sanity-check assertion, an error-code-only assertion, no NULL-session-actor test; none security/RLS/audit-chain) → Master ruling: commit as-is, the 6 findings logged as a non-blocking follow-up (`3.13 part 5a — polish`, MASTER_BACKLOG), no round 3 taken. Applied and verified.
- `0034_2_gl-account-change-requests.sql` — lane 2, task 4.1a part 2 (D-190 hybrid ruling: `billing.gl_account_change_requests` maker/checker write path for `billing.gl_accounts`; new generic primitive `platform.is_approval_chain_approver()`, SCR-PLAT-APPR-01, reused by 4.19/4.20/4.22/5.11b; RLS `glc_entity_scope` + five permissive maker/approver policies; two triggers `assert_gl_account_change_approved()`/`assert_gl_account_change_immutable_fields()`; composite FKs closing the `gl_accounts.parent_id` cross-entity gap), issued by the Master 2026-09-26 on MIGRATION-REQUEST-2 row 6 (0032 lane 1's, 0033 lane 3's, migration renumbered 0032→0034 after the D-117 round closed). Pre-migration pg-reviewer (opus) review: round 1 FAIL(17) → SCR-PLAT-APPR-01 ruling → round 2 FAIL(13, 3 real security gaps) → D-117 round 3 FAIL(8, further real gaps) → confirmation FAIL(2) → confirmation PASS(0). Slice-close review: round 1 FAIL(8) → round 2 FAIL(3, 1 fixed in place, 2 deferred to `4.1a part 2c`). Applied and verified on `pgeos_lane2`, 198/198 module tests green, 207/207 isolation matrix green, guards all green.
- `0033_3_commission-daily-status-lifecycle.sql` — lane 3, WBS 3.13 part 4 (`hr.commission_daily` status-lifecycle correction — `chk_commission_daily_status` 'approved'→'confirmed' per D-190 ruling — + `hr.guard_commission_daily_status()` transition/immutability/SoD-backstop trigger + `hr.commission.dispute_window_hours=48` threshold + SCR-HR-COMM-01's `hr.driver_commission.read_all`/`hr.commission.confirm` permission split), issued by the Master 2026-09-26; RED paths in MIGRATION-REQUEST-3 row 3; pre-migration pg-reviewer (opus) 5 rounds total (all FAIL→fixed, no round returned a bare PASS verdict — the migration's SQL body was independently re-confirmed to carry no open finding at the slice-close review's own round 2), applied and stable.
- `0032_1_order-lines-picked-by.sql` — lane 1, task 2.12 part 3 (D-190 option a, closes SCR-WMS-OUT-03: `wms.order_lines.picked_by uuid` + `picked_at timestamptz`, additive/nullable, pairing CHECK, no new RLS statement — table already RLS'd via 13B's `internal_only` auto-policy — 2 `identity.column_classification` rows, `sensitivity='public'`), issued by the Master 2026-09-26 on MIGRATION-REQUEST-1 row 8. Pre-migration pg-reviewer review: round 1 FAIL(4) → fix round → round 2 PASS(0). Applied and verified on `pgeos_lane1`, 247/247 tests green.
- `0030_2_dimensions.sql` — lane 2, task 4.1b part 1 (SCR-ACC-01 #9, D-190 hybrid design: `billing.dimension_types` — `kind` ∈ {list,reference}, `source_table` nullable/CHECK-whitelisted (8-value closed list) when kind='reference', `entity_scope` RLS, `identity.column_classification` rows for every new column), issued by the Master 2026-09-26 on MIGRATION-REQUEST-2 row 5; `billing.line_dimensions` NOT in this migration (deferred to `4.1b part 2`). Pre-migration pg-reviewer review: round 1 FAIL(12, scope split ruled: `dimension_types` this part, `line_dimensions`+values deferred) → round 2 FAIL(11, incl. a real CHECK three-valued-logic bug caught: `kind='reference' AND source_table IS NULL` silently ACCEPTED without an explicit `source_table is not null`) → confirmation FAIL(1, stale comment) → CLOSED; slice-close review round 1 FAIL(5) → round 2 PASS(0). Applied and verified on `pgeos_lane2`, 46/46 tests green.
- `0029_M_hr-employee-checks.sql` — Master, P6a (SCR-HR-EMP-01, three hr CHECKs); merges after 0028.
- `0028_2_chart-of-accounts.sql` — lane 2, task 4.1a part 1 (SCR-ACC-01 #1-2: `billing.gl_accounts` CHECK constraints on `code` format X-XX-XXX-XXX/class 1-9 and `account_type` closed list), issued by the Master 2026-09-26 on MIGRATION-REQUEST-2 row 4; pre-migration pg-reviewer review round 1 FAIL(10) → round 2 FAIL(8, SoD/citation correction — a proposed `platform.reference.manage` → SYSADMIN role-permission grant withdrawn) → confirmation PASS(0); applied and verified on `pgeos_lane2`, 74/74 tests green.
- `0027_3_commission-daily-rls-version.sql` — lane 3, WBS 3.13 part 1 (`hr.commission_daily` internal write policy + entity-scoped SELECT fix + `version` column, plus `imile.shipments_attributed` security_invoker + grant added after round 1 on the Master's own catch), issued by the Master 2026-09-26; RED paths in MIGRATION-REQUEST-3 row 2; pre-migration pg-reviewer (opus) review round 1 APPROVED WITH CHANGES (8 findings, applied) + round 2 on the added item (4) APPROVED WITH CHANGES (7 findings, applied).
- `0026_1_contract-sku-limits.sql` — lane 1, WBS 2.11 part 5 (condition 10, D-189 / SCR-WMS-OUT-02 §6: new `sales.contract_sku_limits`), issued by the Master 2026-09-26; RED paths in MIGRATION-REQUEST-1 first, pre-migration pg-reviewer (opus) review mandatory (new table + RLS), shape fixed there.
- `0025_M_client-portal-scope-internal-bypass.sql` — lane M, SCR-RLS-03 / D-181 (seven `client_portal_scope` policies rewritten to `NOT is_internal() AND client_id = current_client_id()`; internal reads now rest on `entity_scope` alone), pg-reviewer (opus) pre-migration APPROVED WITH CHANGES (5 findings applied), RED first (`tests/isolation` entity-boundary tests rewritten by pg-tester), applied 2026-09-25.
- `0024_3_shipments-version.sql` — lane 3, task 3.14 part 2 (`imile.shipments.version int not null default 1` + classification row; shape of 0008/0013/0014/0017/0019/0020/0022/0023), issued by the Master 2026-09-25 on MIGRATION-REQUEST-3 row 1; the row names the RED tests `modules/imile/tests/pull-shipments/{pull-shipments.feature,pull-shipments.test.ts}` and the pre-migration pg-reviewer run is mandatory before the file is written.
- `0023_2_schedule-inbound.sql` — lane 2, task 2.9b (SCR-WMS-INB-01 §8 G-01: `wms.inbound_orders` + `handover_point`, `transport_by`, `labour_by`, `labour_count`, `delivery_task_id` nullable, check on `vehicle_type`; N-23 alert seed), issued by the Master 2026-09-25 ahead of MIGRATION-REQUEST-2 (D-179 batch); the request row must name the RED test paths before the file is written, and the pre-migration pg-reviewer run is mandatory. `delivery_task_id` is a nullable column only — no `tms.delivery_task` row is created until a `tms` slice exists to own it (Master default, same class as D-178).
- `0022` — **WITHDRAWN 2026-09-25** (lane 1, 2.11 part 1, pg-reviewer round 1: `wms.outbound_orders.version` already exists at 13B-Schema-Reference-Consolidation.sql:164-166; the number stays consumed, no file is written). Original issue: `0022_1_outbound-orders-version.sql` — lane 1, task 2.11 (`wms.outbound_orders.version` + classification row; shape of 0008/0013/0017/0019/0020), issued by the Master 2026-09-25 on MIGRATION-REQUEST-1 (#6, pre-migration review to be run by the lane before the file is written).
- `0021_1_accounts-internal-write-policy.sql` — lane 1, task 1.8, G-01 schema-change (SCR-SALES-ACCT-01, `docs/notes/`): `sales.accounts` had no internal write RLS policy at all (silent no-op UPDATE for every role). Fix APPROVED by the Master: `internal_only for all using (platform.is_internal())`, additive, matches every other non-entity-scoped internal table's policy. Issued 2026-09-25; RLS-touching, pre-migration review mandatory, run by the lane before the file is written. Applied 2026-09-25 (pg-reviewer pre-migration APPROVED WITH CHANGES; number issued by the Master).
- `0020_1_accounts-version.sql` — lane 1, task 1.8 (`sales.accounts.version` + classification row; shape of 0008/0013/0017/0019), issued by the Master 2026-09-25 on MIGRATION-REQUEST-1 (#4, pre-migration review to be run by the lane before the file is written). Applied 2026-09-25 (pg-reviewer pre-migration APPROVED WITH CHANGES; number issued by the Master, MIGRATION-REQUEST-1.md).
- `0019_1_contracts-version.sql` — lane 1, task 1.7 (`sales.contracts.version` + classification row; shape of 0008/0013/0017), issued by the Master 2026-09-25 on MIGRATION-REQUEST-1 (pre-migration review to be run by the lane before the file is written, same as 1.2/1.6).
- `0018_2_inventory-counts-version.sql` — lane 2, task 2.13 (`wms.inventory_counts.version` + classification row; shape of 0008/0013), issued by the Master 2026-09-25 on MIGRATION-REQUEST-2 (pre-migration review APPROVED WITH CHANGES, 3 comment-only findings applied; applied and verified by the lane).
- `0017_1_quotes-version.sql` — lane 1, task 1.6 (`sales.quotes.version` + classification row; shape of 0008/0013), issued by the Master 2026-09-25 on MIGRATION-REQUEST-1; applied and verified by the lane (pg-reviewer pre-migration APPROVED WITH CHANGES).
- `0016_2_shifts-groups-assignments.sql` — lane 2, task 5.5a part 2 (`hr.shifts` / `hr.shift_groups` / `hr.shift_assignments`, exclusion constraint + composite FK), issued by the Master on MIGRATION-REQUEST-2, applied and verified by the lane (35/35 columns classified, RLS active, exclusion + FK functionally tested).
- `0015_2_platform-sites.sql` — lane 2, task 5.5a part 1 (`platform.sites` + `hr.employees.default_site_id` FK + `platform.thresholds` `att.geofence_radius_m=500`, SCR-HR-SHIFT-01 §2.4 / D-144 item 4), issued by the Master 2026-09-24 on MIGRATION-REQUEST-2 (pre-migration review APPROVED WITH CHANGES, 7 findings applied).
- `0014_2_employees-version.sql` — lane 2, task 3.3 (`hr.employees.version` + classification row; shape of 0008), issued by the Master 2026-09-24 on MIGRATION-REQUEST-2 (pre-migration review APPROVED); applied, merged in `9616422`.
- `0013` — RESERVED for lane 1, task 1.2 (catalog), pending its MIGRATION-REQUEST-1.md.
- `0012_M_space-dashboard-invoker-grant.sql` — lane M, task 5.13 part 1, applied 2026-09-24 (pg-reviewer pre-migration APPROVED WITH CHANGES, header text).
- `0011_M_alert-log-version-seed-rules.sql` — lane M, task 5.13 part 1, applied 2026-09-24 (pg-reviewer pre-migration FAIL(11) → PASS round 2).
- `0010_M_idempotency-keys-variance-photo.sql` — lane M, task 2.9-part2, applied 2026-09-24 (pg-reviewer pre-migration APPROVED WITH CHANGES).
- `0009_M_next-doc-no-definer.sql` — lane M, task 2.9, applied 2026-09-24 (pg-reviewer pre-migration APPROVED WITH CHANGES).
- `0008_M_inbound-orders-version.sql` — lane M, task 2.9, applied 2026-09-24 (pg-reviewer pre-migration APPROVED WITH CHANGES).
- `0007_M_pgeos-app-role-entity-scope.sql` — lane M, task 0.6a (part 1, D-133/D-140), applied 2026-09-24 (pg-reviewer pre-migration APPROVED WITH CHANGES; slice PASS round 4).
- `0006_M_possible-duplicates-ge.sql` — lane M, task 1.5 (GM directive phase D: 13B §13B-19 `> 0.85` → `>= 0.85`), applied 2026-09-23 (pg-reviewer migration gate PASS, round 3).
- `0005_M_balance-integrity-batch.sql` — lane M, task 2.8 fix (SCR-WMS-01), applied 2026-09-23 (pg-reviewer migration gate PASS, round 2).
- `0004_M_audit-chain-seq.sql` — lane M, task 0.9 fix (SCR-AUDIT-01, ADR-0002), applied 2026-09-23 (pg-reviewer migration gate PASS, round 3).
- `0003_M_rls-scr-01-02.sql` — lane M, task 0.18 (SCR-RLS-01 B+C, SCR-RLS-02 A+B+C), applied 2026-09-23.
- `0002_M_classify-columns.sql` — lane M, task 0.16, applied 2026-09-22.
- `0001_B_fix-is-internal-empty-guc.sql` — lane B, task 0.11, applied 2026-09-22.

## Rules (CLAUDE.md · PARALLEL LANES)

1. A lock is a module (`wms`) or a module/use-case pair (`wms/put-away`, D-179); each appears at most once, and a whole-module row and a use-case row of the same module never coexist for two lanes. A lane writes only inside its lock and `tests/`; a use-case lock never writes a module-wide file (index.ts, package.json, router, i18n) — a worker needing a file outside its lock STOPS and reports.
2. Only the Master claims and releases; pg-scribe writes this file. Lanes never edit it — max three lanes. The `worktree` column of a lane row is `../pg-eos-lane-<lane>` — never the shared `claude-kit`; `scripts/check-locks.sh` (pre-commit gate ⓐ, CI gate ①, `pnpm check:locks`) refuses the table otherwise.
3. Frozen for every lane: `packages/*`, `database/schema/*`, `packages/contracts/_shared/*`, `CLAUDE.md`, `.claude/*` — changes there are single-lane Master tasks merged before lanes resume.
4. Migrations: the lane requests — every migration of its whole task list in ONE table at lane start, each row naming its RED test paths — the Master issues the numbers in one batch and records them here; the file is `database/migrations/NNNN_<lane>_<slug>.sql`, refused by `lane-guard.sh` until the named RED tests exist, and migrations merge first, in number order (D-179).
5. Merge queue: pg-reviewer PASS → `git rebase main` → gates ①–③ green → `pnpm guards:run` green → the Master merges via linear history (rebase merge, D-173); lanes never merge lanes.

`lane` is `A` (GM manual) · `B` · `C` · `1` · `2` · `3` · `M` (Master), taken from the `Lane` column of `docs/package/38-WBS.md` — never invented. `.claude/hooks/lane-guard.sh` reads this table on every Edit/Write when `PG_LANE` is set.
