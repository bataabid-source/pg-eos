// M01 identity — package shell created by WBS 0.4 (monorepo skeleton).
// The module's own surface is built by WBS 0.17; its hexagonal file tree is copied from the
// golden slice (WBS 2.9) by scripts/new-slice.sh — never hand-made (CLAUDE.md).
export {};
