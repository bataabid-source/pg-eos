// M02 sales — package shell created by WBS 1.5 (proof slice, ADR-0001).
// Proof slice: data model + seed only (fixtures created/removed by the proof suite), no UI, no
// workflow, no public API — docs/adr/ADR-0001-1.5-proof-slice.md. The full 1.5 slice (accounts,
// contacts, leads, opportunities, activities) is replicated later from the golden slice (WBS 2.9)
// via scripts/new-slice.sh — never hand-made (CLAUDE.md).
export {};
