# CHANGELOG — PG-EOS v3 → v4
**21/09/2026 · التفصيل الكامل (ملف:سطر، قبل→بعد، أوامر التحقق ونواتجها) في `_changelog/CHANGELOG-{GOV-A,GOV-B,OPS,ADM,PLT,SCH-2}.md` و`_changelog/VERIFY-v4.md`. سجل القرارات الحاكمة للتغييرات: `_changelog/../AUDIT-REPORT-v4.md` §3 وEXECUTION-MASTER-v4 §1.16.**

## 1. بنية الحزمة
| ما تغيّر | التفصيل |
|---|---|
| إزالة النسخ المكررة | `files/BOOTSTRAP-v3.md` و`files/EXECUTION-MASTER.md` كانتا نسختين مطابقتين (md5) لما داخل A-governing — أُزيل التكرار |
| إزالة التداخل | `PG-EOS-Package/PG-EOS-Package/` → مستوى واحد `PG-EOS-v4/` |
| تقاعد أربع وثائق حاكمة | BOOTSTRAP-v3 → **BOOTSTRAP-v4** · EXECUTION-MASTER v1 + DECISIONS-ADDENDUM + 43 → **EXECUTION-MASTER-v4** (سجل قرارات واحد، خطة مسارات مشتقة، أوامر، خريطة وثائق، حالة ما قبل 0.1، سؤال المعايرة) |
| مجلد `database/` | الملفات SQL الأربعة + `guards.sql` + `apply.sh` في مكان واحد (40 Part I) |
| ملفات جديدة | `13B-Schema-Reference-Consolidation.sql` · `019-Warehouse-WH1-Setup.sql` (بديل 019-Main-Abdullah) · `00-README-v4.md` · `AUDIT-REPORT-v4.md` · `CHANGELOG-v4.md` · `_changelog/` (13 ملفاً) |
| ترويسة موحّدة | كل وثيقة تحمل «الإصدار 4.0 · 21/09/2026» وكتلة v4 (الحالة · الحاكم عند التعارض · معرّفات التصحيحات المطبّقة) |

## 2. المخطط (database/)
| الملف | التغيير |
|---|---|
| 01-Data-Model.sql | نقل `platform.has_perm()` و`platform.allowed_entities()` إلى ما بعد جداول identity (كانا يفشلان فتفشل كل سياسات RLS) · إزالة `platform.domain_events` (الحاكم `platform.outbox` في 13B) · حذف السلسلة اليتيمة `RCP` · تعليق نطاقات الأدوار الثمانية |
| 13-Schema-Additions.sql | العمود المولَّد `is_overdue` (يستدعي `now()` → غير قابل للثبات → الجدولان `hr.recruitment_cases` و`admin.gov_transactions` لم يكونا يُنشآن) → `due_at` بمُشغِّل `platform.set_stage_due_at()` |
| 13B (جديد، 219 ك.ب) | 39 جدولاً مرفوعاً حرفياً من الوثائق 14 · 15 · 17 · 22 · 23 · 25 · 27 · 29 · 31 · 35 · 36 مع `-- المصدر` فوق كل جدول · `platform.audit_log` مجزّأ شهرياً بسلسلة SHA-256 (`prev_hash/row_hash` + `verify_audit_chain()`) · `platform.outbox` مع `entity_id` وقيد `outbox_business_needs_entity` · جداول قرارات GM: `imile.dtl_rule_autonomy` · `dispatch_autonomy` · `driver_trust` · `coverage_areas` · `driver_zone_exclusions` · SCR-1…7 (POD `sha256/legal_hold/retain_until` + مشغّل الحجز · `penalty_schedule.is_fraud/min_degree` · `disciplinary_cases.signed_by/routed_to` + مشغّل السلطة الهرمية + قيدا م.35/م.37 · قيد الـ17 مرحلة · عرضا 22) · 54 قيد `check` على كل عمود حالة (من STATE-REGISTER) · **RLS على 154 جدولاً / 188 سياسة** (G7 = 0) · مشغّل SoD على insert **or update** · قيد `valid_scope` الثماني · البذور: 26 دوراً · 4 أزواج SoD · 92 خدمة · 77 بند جزاء · 17 مرحلة · 32 سبب فشل (3 على السائق) · 22 تنبيهاً بـ`source_query` · 44 عتبة (منها 16 لـADR-27 والعمولة 0.300/0.050/23/5/12) · عرض `identity.unclassified_columns` (لمهمة 0.16) |
| 019-Warehouse-WH1-Setup.sql (جديد) | أُعيد بناؤه من وثيقة 19 §4: WH1 (لا MAB) · 11 منطقة · 8 كتل · كود سباعي `P3-14-5` عبر `wms.generate_locations` · مستويات 1–6 · 147 إنشائي `X-B101-1` · 30 تشغيلي · `wms.verify_wh1()` = 21/21 · الأرقام مطابقة إلى آخر خانة (3,330 · 3,301.641 · 3,830.895 · 2,439,750) · الملف القديم كان بخطأ نحوي (`join` قبل `cross join`) ولم يُشغَّل قط |
| guards.sql / apply.sh (جديدان) | G1–G13 + G18 + `G-SEED` قابلة للتشغيل بنص 40 Part F v4 · `apply.sh --recreate` يعيد البناء ويطبع ملخصاً |

**نتيجة التحقق النهائي:** `apply.sh --recreate` = exit 0 على ثلاث قواعد مستقلة · 155 جدولاً · G1–G5, G7–G13, G18, G-SEED = 0 · G13 = 100/100 · G6 = 2,136 (مهمة 0.16، غير مانع).

## 3. الوثائق الحاكمة (A-governing/)
| الوثيقة | أهم التغييرات |
|---|---|
| BOOTSTRAP-v4 | سلّم أسبقية واحد (R-01) + «تعليمات تشغيل لا مصدر قواعد» · قائمة المخطط «01/13/13B/019» · ثماني مراحل · G1–G18 · 20/20 سيناريو · opus للجلسة كاملة في 2.9 · «AGENT CONSTRAINTS of BOOTSTRAP-v4 §6» بدل الإحالة إلى v2 المحذوف · `docs/package` = A+B+C · الهجرات `database/migrations` · 7 بوابات نشر |
| EXECUTION-MASTER-v4 | Part 0 السلّم + المانيفست + 11 صفاً للمتقاعد بسببه · Part 1 سجل القرارات الكامل (§1.1–§1.4 من v1 مصحّحة + §1.5–§1.15 = DECISIONS §1–§11 حرفياً + §1.16 قرارات تدقيق v4) مع الإضافات: 22 تنبيهاً · الأسباب الثلاثة مسمّاة · الفرز A3 · RPO/النسخ بالطبقة · 4 OCPU/24 GB · 26 دوراً · 4 SoD · 77 جزاءً · 16 عتبة بأسمائها · 2% مطابقة الشريك · عهدة 24 س تنبيه / 48 س تصعيد · Part 2 خطة مسارات **مشتقة من عمود Lane في 38** · Part 3 أوامر تعمل مع بنية v4 · Part 5 خريطة الوثائق · Part 6 الحالة قبل 0.1 · Part 7 المعايرة |
| 40 | السلّم · §A5 · §B1 (26 دوراً · 4 SoD · نموذجا الجهاز · تجزئة PIN) · §B2/§B3 (audit_log الإحدى عشرة · outbox بـentity_id) · §B5 · §B6 (22/24 · read replica = Tier 1/2) · §C3 (الكود السباعي · WH1 · 3,330) · §C4 (3 أسباب · 25 فرعياً · سلّم 90/45/30/15 · POD) · §C7 (77 · السلطة الهرمية · العمولة · 17 مرحلة · due_at) · §C8 (A3 · ADR-27/28 · 16 عتبة · coverage_areas) · Part D · Part E (**S19/S20 مضافان من محتوى 12 → 20 سيناريو**) · Part F (كل حارس سطر SQL؛ G3 بالتوقيع الصحيح؛ G8/G9/G10 معرَّفة؛ G18) · Part G بالطبقتين · Part H ثماني مراحل · **Part I خريطة ملفات المخطط** |
| 36 | «الحاكم في حدود السلّم» · §2-4 R-03 · §3-1 outbox · §4-3 سبع بوابات (وُحِّد §7) · §5-2 يذكر WBS 2.9 بمعياره · §8 المسارات · §9 إحالة إلى EXEC-v4 |
| 38 | صفّا الملخص للمرحلتين 0 و3 مصحّحان بالعدّ الآلي · ثماني مراحل · عمود `Lane` (بعد تصحيح 10 مخالفات اعتمادية و5 مهام غائبة) · 0.6 سبع بوابات · 2.1 → 19 §4 + 019 · 2.5 = 3,330 ملصقاً · 4.17/7.4 بـS19/S20 · G6/G7 مدمجان في 0.16/0.18 (العدد 132 ثابت) |
| 42 | صفر منافذ واردة فعلاً (NSG/ufw/compose/nginx/SSH عبر Cloudflare Tunnel) · 4 OCPU/24 GB · 14/8/6 · RPO 24 س/RTO 4 س بجدول الطبقتين · Tier 1 بتعريف جديد · deploy.sh بـG1–G18 · `apps`/`ssh` في DNS · §10 يشمل 26 |
| 22 | المطابقة البنكية CFO (زال التناقض الداخلي) · 4 SoD · 26 دوراً · DEPUTY_SYSADMIN وأربع عيون · المناصب غير المشغولة · DDL → 13B · §9 قرارات مُغلقة |

## 4. الوثائق المرجعية (B-reference/) — ملخص
00 (PGH · 20 سيناريو) · 02 (26 دوراً · حذف CLIENT_USER · 4 SoD · CFO) · 03 (**سجل الحالات المرجعي** 13 آلة/19 عموداً · دور وحدث لكل انتقال · سلّم 90/45/30/15) · 04 (الخمس المعلَّمة · ×1.15) · 05 (بيانات القبول التنفيذية من 40) · 06 (CFO · PGH · 500) · 07 (**ADR-27/28 مدمجان** · A3 · حذف «الذكاء الاصطناعي يقترح ولا يقرّر» · coverage_areas) · 09 (سقف المسؤولية · 2% · الشريك لا يُسمّى) · 10 (17 مرحلة بأكواد · عتبة 11 يوماً · العمولة قرار مغلق) · 11 (مستويات 100/500/2,000 · GOV · لا اعتماد ذاتي فعلي) · 12 (20 سيناريو بلا تناقض) · 14 (وردية→فريق · إعفاء GM · الإشغال من bed_assignments) · 15 (77 · قيدا م.35/م.37 · signed_by/routed_to · is_fraud) · 17 (space_block_id · دالتا الإشغال بلا تضخيم · §10 مغلق) · 18 (سجل تدقيق يحيل إلى 19 §3-4) · 19 (**الحاكم للمستودع**: قاعدة الكود · الجدول الرقمي الوحيد · إصلاح join) · 23 (Twilio · CFO · config/queue/runs · N-19…22) · 25 (**22 تنبيهاً بـsource_query/action** · حريق ليس تنبيه نظام · NFR بالطبقتين) · 26 (RPO/النسخ بالطبقة · الوضع اليدوي · نماذج ورقية ومسار إدخال) · 27 (فترات الصيانة · legal_hold) · 28 (77/26/20/57 · 5 لغات · خريطة بلا متقاعد · ربط 16 بنداً بمهام 38) · 29 (49 عملية · 93.9% · 84→57 · GM يضبط حد الفاتورة) · 30 (G-07 · الكود السباعي) · 31 (مطابق لـ13B · outbox · legal_hold) · 35 (3 أسباب · 25 فرعياً · العمولة · N7 · G-07 · on conflict مصحّح) · 41 (الطبقات الثلاث · الجزء الثاني → BOOTSTRAP-v4).

## 5. الأدوات (C-tools/)
pst-warehouse-3d: حارس three.js · الكود السباعي وكود الإنشائي `X-B101-1` (كان باگاً) · عرض 30 تشغيلية + 147 إنشائية · العدّ 3,330 مُعاد تنفيذه في Node. app-lab-instrument: الجلسات الست من 34 · المعايير 1–19 · يعمل بلا إنترنت. premium-apps-download: Premium WH كـPWA · حذف الشارات غير المسنَدة. 32/33/34: تاريخ الاطّلاع · 89.7 ميجا غير مسنَد · إعادة توجيه التعديلات من 16/21 إلى 35/40.

## 6. ما لم يتغيّر عمداً
كتالوج الخدمات الـ92 وأسبقية السعر · بنود الجزاءات الـ77 نصاً · التحفّظ القانوني في 15 · الأرقام التنفيذية للسيناريوهات · قرارات 42 §11 الخمسة · ADR-27/28 بمعادلاتهما · حدود المصادقة · مصفوفة التوجيه · قائمة المراجعة العشر · هندسة الأداة ثلاثية الأبعاد · سطر «Premium Ops v123 لا يُمس».

## 7. إضافة لاحقة (21/09/2026 مساءً) — أطلس النظام وإغلاق فجوات المخطط
- **D-blueprints/** جديد: 9 وثائق خرائط هيكلية (01–09) + `PG-EOS-System-Atlas.html/.docx` + 122 خريطة Mermaid مصيَّرة + مولّدات (`gen_erd.py` · `render.py` · `build.py`).
- **SCH-4 (13B):** إغلاق 15 فجوة مخطط كشفتها الخرائط: قيد `occupancy_snapshots` بالكتلة · 8 سلاسل ترقيم مفقودة · `gov_transactions.fee_voucher_no` · `trg_no_holding_invoice` على insert/update · SoD على الإنابة · `hr.employees_basic` · 12 قيداً تعدادياً · 11 مفتاح `settings` · عزل طوابير `cc` (S5) · سلسلة الإشراف في السلطة الهرمية · **مخطط `governance` بـ14 جدولاً (M13)** · `variance_pct` مولَّد · أعمدة تكلفة الشريك/السكن/الاستقدام في `profitability` مع إعادة تعريف `margin` · بذور `domain_owners` 12 · `approval_chains` 8 · `automation_rules` 9. الجداول 155 → **169**؛ `apply.sh` بصفر أخطاء، G7 = 0.
- **09-Gap-Register.md:** 46 بنداً مفتوحاً (18 قرار GM · 14 مهمة WBS · 14 بلا نص يغطيها).

## 8. إضافة (21/09/2026) — الوثائق 10–12 وطبقة أوامر العمل
- **D-blueprints 10** سيناريوهات دورة تنفيذ الطلبات (10 سيناريوهات) · **11** الربط مع متاجر العملاء (تقييم + 3 خيارات + تصميم I-12) · **12** الخدمات التشغيلية داخل المستودع كأوامر عمل (8 سيناريوهات + مواصفة).
- **13B (SCH-5 · SCR-WO-01):** `wms.work_orders` · `work_order_tasks` · `work_order_events` · `work_order_task_types` (35 صفاً لـ33 خدمة) · مشغّلا checker≠picker وحدّ المهام النشطة · مشغّل تجميع `qty_done` · سلسلة `WO` · 7 عتبات `wo.*` · RLS. الجداول 169 → **173**. قيد `movement_type` صار اتحاد القائمتين.
- **09-Gap-Register:** +19 بنداً (53–71) من الوثائق 10–12؛ بندان أُغلقا.

## 9. إضافة (21/09/2026) — الوثائق 13–15 وSCH-6
- **D-blueprints 13** مؤشرات إشغال المساحات (5 حالات · 3 لوحات: GM · المبيعات · العميل) · **14** راتب وعمولة المبيعات (4 نماذج · قواعد الحساب · أمثلة 12 شهراً · 8 قرارات GM) · **15** لوحات التركيز (المهمة الموحّدة · 14 مجموعة × 26 دوراً · تشريح اللوحة).
- **13B (SCH-6):** `13B-SP` (4 عروض مساحة + حارس الحجز + `convert_reservation()` + RLS العميل + تصحيح `space_dashboard`) · `13B-SC` (تعميم `hr.commission_rules` 13→29 عموداً · `hr.sales_commission_events` · `sales.account_ownership_history` · 13 عتبة · عدّاد `SCM` · RLS مقيِّدة) · `13B-FB` (`platform.my_work` · `my_roles()` · `my_employee_id()`). الجداول 173 → **175** · العروض 11 → **16** · العتبات 51 → **65**. `apply.sh` بصفر أخطاء، G7 = 0.
- **09-Gap-Register:** +8 بنود (72–79)؛ 3 أُغلقت.

## 10. إضافة (21/09/2026) — تعليمات البناء v5 وعدة التشغيل
- `A-governing/BOOTSTRAP-v5.md` يحلّ محل v4 كتعليمات تشغيل: آلية توازٍ بلا تعارض (`tasks/LANE_LOCKS.md` · أرقام هجرات من الرئيسي · مسارات مجمَّدة · طابور دمج واحد) · انضباط الحصة (موجزات الموديولات بدل قراءة الحزمة · مهمة لكل جلسة · ميزانية رموز لكل نوع عمل) · السرعة والجودة (مولّد الشريحة الذهبية · اختبارات RED أولاً · بوابات ما قبل الالتزام ①–③ · CI ①–⑦) · D-blueprints مدخلات بناء ملزمة · المهام المقترحة في `tasks/proposed/`.
- `A-governing/PROJECT-SETUP-GUIDE.md`: إعداد المستودع والقاعدة المحلية وأول جلسة والمسارات المتوازية ومشروع الدردشة للمدير العام وقواعد الحصة.
- `A-governing/claude-kit/` (45 ملفاً جاهزاً للنسخ): CLAUDE.md · settings.json + hooks (حارس المسارات) · 5 وكلاء مثبّتون · 6 أوامر · 15 موجز موديول مولَّداً من القاعدة + المولّد · scripts (new-slice · guards-run) · LANE_LOCKS · 4 مهام مقترحة · PROJECT_STATE/MODEL_ROUTING/AGENT_WORKFLOW · docker-compose.

## 11. قيد بأثر رجعي (23/09/2026) — تعديلان على الحزمة لم يُقيَّدا في وقتهما

- **تعديل 8d62747 / SCR-I18N-01 / D-001 (21/09/2026):** الوثائق التالية عشر: `19-PST-Warehouse-Setup.md` (لافتات الممرات A5 بست لغات — الأمهرية `am` سادسةً؛ لوحة الإدارة تبقى ar/en)، `28-Adoption-Compliance-Launch.md`، `30-PDA-App.md`، `38-WBS.md` (الصف 3.22: "5 لغات" → "6 لغات")، `40-Build-Specification-EN.md` (ملاحظة الإغلاق: `am` في قائمة i18n)، `BOOTSTRAP-v4.md` (قيد الوكيل يكسب `am`)، `EXECUTION-MASTER-v4.md` (§1.4 صف مبدِّل اللغة: 6 لغات في تطبيقات الميدان، يشير إلى D-001؛ قيد i18n يكسب `am`)، `D-blueprints/03-Operations-Warehouse.md`، `D-blueprints/06-Administrative-HR-Housing.md`، `D-blueprints/15-Focus-Boards-UX.md`. الإصدارات المرفوعة: `19` · `28` · `30` · `38` · `BOOTSTRAP-v4` · `EXECUTION-MASTER-v4` من 4.0 → 4.1؛ الوثيقة `40` → 4.1 بهذا القيد ثم → 4.2 بالقيد التالي؛ `D-blueprints` 03/06/15 بلا حقل إصدار (لم تُرفع). التفصيل في `docs/notes/SCR-I18N-01-amharic.md`.

- **تعديل f03e160 / D-002 / SCR-RLS-02 (23/09/2026):** الوثيقة 40 Part F صف G7 تُحدَّث من `relkind in ('r')` إلى `relkind in ('r','p')` (الجداول المُقسَّمة الأب، كالحالة `platform.audit_log`)، والنص الشارح يشمل الأب المُقسَّم. 13B الحلقة الآلية لإنشاء السياسات: `relkind in ('r','p')` + ملاحظة 13B-22. `database/schema/guards.sql` و`apply.sh` (G7) محدَّثة. الإصدارات: 40 → 4.2، 13B → 4.1.

## 12. إضافة (23/09/2026) — توحيد نوع المبلغ وقواعد الالتزام

- **13B `tms.contact_log.cost`:** من `numeric(10,4)` إلى `numeric(14,3)` (قرار GM: كل المبالغ النقدية `numeric(14,3)` بالدينار الكويتي د.ك). ملف المخطط فقط (لا ترحيل جديد) — ينتج ترحيل من المهمة المالكة للجدول (WBS 3.8، طبقة التواصل بتطبيق السائق). 13B → **4.2**.

- **قاعدة GIT في CLAUDE.md (B3): استبدال قاعدة GIT الحالية:** لا تُنشأ التزام منفصل لـ"تسجيل الهاش المُتحقَّق منه"؛ الرابط الوحيد بين مهمة WBS وشيفرتها هو سجل الالتزام `git log` بنص نموذج `type(WBS): description` (حيث WBS معرّف من 38 أو `X`)؛ هاش المهمة السابقة يُسجَّل هنا في `PROJECT_STATE.md` ضمن التزام المهمة اللاحقة.

- **قاعدة hook التزام (B4): `.githooks/commit-msg` نسخة مُصرَّحة:** يرفض أي رسالة أول سطر فيها لا يطابق `type(WBS): description` حيث WBS موجود في `docs/package/38-WBS.md` أو `X`. يُفعَّل عبر `git config core.hooksPath .githooks` بآلية البرنامج النصي `prepare` الجذري.

- **تأكيدات B7:** `platform.test_probe` و`identity.test_probe` ليستا جداول — هما قيم نصية (`aggregate_type` في طلبات `platform.outbox` في اختبارات `packages/events`؛ رمز إذن في `identity.permissions` في اختبارات `packages/identity`)، تُنشآ وتُحذفان ضمن الاختبارات (`afterAll` أو اختبار فردي). مقبول من GM. `modules/wms` مبني يدويّاً مقبول (موثَّق منذ 0.16).

## 13. إضافة (23/09/2026) — تثبيت نموذج pg-scribe على sonnet

- **توجيه المدير العام 2026-09-23 (لا haiku؛ `pg-scribe` = sonnet):** `A-governing/BOOTSTRAP-v5.md` §3 السطر 69 (`pg-scribe     model: haiku` → `model: sonnet`) و§4 جدول التوجيه السطر 84 (`pg-scribe | haiku | ≤ 10k` → `pg-scribe | sonnet | ≤ 10k`)، الإصدار 5.0 → 5.1؛ و`41-Cloud-and-AI-Efficiency.md` §1 السطر 93 (`pg-scribe` haiku → `pg-scribe` sonnet)، الإصدار 4.0 → 4.1. `CLAUDE.md` و`docs/MODEL_ROUTING.md` كانا قد طابقا هذا التثبيت مسبقاً في الالتزام `dc3cc1b`.

## 14. إضافة (23/09/2026) — نطاقات كتابة الوكلاء وتسلسل الشريحة الثابت

- **توجيه المدير العام 2026-09-23 (بلا وكيل جديد):** `pg-tester` يكتب ملفات الاختبار فقط (`tests/**` · `**/tests/**` · `*.test.*` · `*.spec.*` · `features/**` · `*.feature`) ويُستدعى ثانيةً للتحقق بعد البناء؛ `pg-backend` لا يمسّ أي ملف اختبار وأي خلل في اختبار يعود إلى `pg-tester`؛ `pg-reviewer` يُستدعى عند إغلاق الشريحة **وقبل** كتابة أي ترحيل يمسّ المخطط أو RLS أو سلسلة التدقيق. التسلسل الثابت: tester ← backend ← tester (تحقق) ← reviewer ← scribe ← commit `feat(<WBS>)` واحد.
- **`A-governing/BOOTSTRAP-v5.md` 5.1 → 5.2:** §2 الخطوات 6–13 (أُعيد ترقيمها: خطوة تحقق pg-tester وبوابة المراجع قبل الترحيل)، §3 أسطر أدوار pg-reviewer وpg-backend وpg-tester، §6 كتلة SPEED AND QUALITY (بند SLICE SEQUENCE مطابق حرفياً لـCLAUDE.md). **§4 (جدول التوجيه) لم يتغيّر** — التسلسل والأدوار في §2 و§3 لا في §4.
- **`41-Cloud-and-AI-Efficiency.md` §1:** لا تعديل — لا يصف نطاقات الكتابة ولا تسلسل الشريحة.
- خارج الحزمة (بلا إصدار): `CLAUDE.md` BUILD METHOD · `.claude/agents/pg-{tester,backend,reviewer}.md` · `.claude/commands/slice.md` · `docs/AGENT_WORKFLOW.md`.

## 15. إضافة (23/09/2026) — SCR-AUDIT-01: ترتيب سلسلة التدقيق بـ`chain_seq` (ADR-0002)

- **قرار GM 23/09/2026 (الخيار A بحلّ §6):** `chain_seq` = السابق + 1 يُحسب داخل المشغّل بعد القفل الاستشاري في استعلام جلب التجزئة السابقة، بلا كائن تسلسل؛ فهرس فريد على `chain_seq` في كل قسم (الافتراضي ضمناً)؛ `verify_audit_chain()` ترتّب بـ`chain_seq` فقط وتكشف عدم تطابق التجزئة والتكرار والفجوات وقسماً بلا الفهرس. مراجعة بوابة الترحيل (pg-reviewer، opus): FAIL(14) → FAIL(4) → **PASS**.
- **`13B-Schema-Reference-Consolidation.sql` 4.2 → 4.3:** عمود `chain_seq bigint not null` · خمسة فهارس `<القسم>_chain_seq_key` · المشغّل والتحقق `security definer` مع `search_path` و`TimeZone = UTC` و`DateStyle = ISO, YMD` مثبّتة · رفض غير READ COMMITTED · معاملات ارتكاز `(p_anchor_seq, p_anchor_prev_hash)` · سحب EXECUTE من PUBLIC · ملاحظتا مهمة القسم الشهري (الفهرس + RLS + التصنيف). الصيغة (31) دون تغيير.
- **ترحيل `0004_M_audit-chain-seq.sql`** (قابل لإعادة التشغيل؛ إعادة بناء السلسلة لمرة واحدة ممنوعة إلا بتفعيل صريح `pgeos.audit_chain_rebuild=allow`).
- **`40-Build-Specification-EN.md` 4.2 → 4.3:** §B2 (ترتيب `chain_seq`، الصلاحية، التمثيل الزمني، READ COMMITTED، مهمة القسم الشهري) · Part F صف G8 ونص الارتكاز.
- **`31-Audit-and-Traceability.md` 4.0 → 4.1:** §3-3 — اقتباس المشغّل الجديد ورأس دالة التحقق، والتمثيل القانوني لـ`occurred_at` في الصيغة، وإجراء الاستعادة.
- **`D-blueprints/08-Data-Model-Maps.md`:** صفّا توقيع `verify_audit_chain` (بلا حقل إصدار — لم يُرفع).
- **النتائج:** اختبار 8 كتّاب × 500 — قبل: 2008 · 2009 · 2004 صفاً مكسوراً؛ بعد: 0 · 0 · 0. بند G-01 مفتوح لدى GM: مكان حفظ نقطة الارتكاز قبل أول فصل لقسم (≥ 03/2028).

## 16. إضافة (23/09/2026) — SCR-WMS-01: الحارس G1 بمفتاح الرصيد الكامل وفي الاتجاهين

- **قرار GM 23/09/2026 (المرحلة H):** `wms.verify_balance_integrity()` تجمّع الدفتر بمفتاح `wms.stock_balance` الكامل `(client_id, sku_id, location_id, batch_no)` — `batch_no` الفارغ في الدفتر يطابق `''` في الرصيد. **الجزء الثاني (حكم pg-reviewer ضمن SCR-WMS-01، مستند إلى 40 INV-C3-2 وP4):** المقارنة في الاتجاهين — صف رصيد بلا حركات وكميته ≠ 0 يُبلَّغ أيضاً. نوع الإرجاع يكسب `batch_no`. الدالة بحقوق المستدعي: نتيجتها صالحة فقط لدور يتجاوز RLS (مذكور في تعليقها).
- **`01-Data-Model.sql` 1.0 → 1.1** (كتلة الدالة فقط) · **ترحيل `0005_M_balance-integrity-batch.sql`** (حذف ثم إنشاء؛ قابل لإعادة التشغيل؛ لا جدول ولا عمود).
- **`D-blueprints/08-Data-Model-Maps.md`:** صفّا الدالة (بلا حقل إصدار).
- **الاختبارات:** دفعتان في الموقع نفسه ⇒ 0 انحراف (كانت 2)؛ اختبار الخاصية 1,000 حركة بدفعات متعددة أحمر على الدالة القديمة وأخضر على الجديدة. مراجعة بوابة الترحيل: FAIL(8) → PASS.

## 17. إضافة (23/09/2026) — إعداد لغة القاعدة للعربية (SCR-TRGM-01) وحدّ التشابه `>= 0.85` (WBS 1.5)

- **SCR-TRGM-01، الخيار A (قرار GM 23/09/2026):** كل القواعد تُنشأ بـ UTF8 و`lc_collate = C` و`lc_ctype = C.UTF-8`. تحت `lc_ctype = C` لا تُولِّد pg_trgm أي trigram للحروف العربية (التشابه بين اسمين عربيين متطابقين = 0) فلا يعمل كشف التكرار على `name_ar` أبداً. `database/schema/apply.sh` ينشئ القاعدة بهذا الإعداد ويرفض غيره؛ `infra/docker/docker-compose.yml`؛ **`42-Oracle-Cloud-Deployment.md` 4.0 → 4.1** (§4.2 الإعداد وفحص سلوكي عند 0.5 و0.8، §6.3 `restore.sh`، §9 تكافؤ glibc/musl مرفوع للـGM).
- **`13B` 4.3 → 4.4 · الترحيل `0006_M_possible-duplicates-ge.sql`:** `sales.possible_duplicates` من `> 0.85` إلى `>= 0.85` (40 §C2 INV-C2-3)، ومؤشر فهرس SCR-7 صُحِّح إلى 13B-19. قياس: `similarity()` تُرجع `real` فكان الزوج عند 0.85 بالضبط يُبلَّغ أصلاً مع `>` (float4(0.85) = 0.8500000238)؛ التصحيح مطابقة نصية.
- **`22-Master-Data-Governance.md` 4.0 → 4.1:** نص العرض في §5 منسوخ من 13B v4.4 وملاحظة الملكية مصحَّحة (العرض معرَّف في 13B §13B-19 منذ v4) مع الشرط المسبق للغة القاعدة.
- **ADR-0001:** سطر حلّ مؤرَّخ تحت «Known discrepancy».
