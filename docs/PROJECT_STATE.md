# PROJECT_STATE — PG-EOS

**Hard limit: 40 lines.** Older history lives in `docs/CHANGELOG.md` (CLAUDE.md · OPERATING RULES · BRIEFS). Maintained by pg-scribe only, in the same commit as the task it records.

| field | value |
|---|---|
| Phase | 0 CLOSED · 1 Commercial Core (done except 1.11, BLOCKED D-178) · 2 Warehouse (9/19) · pilot-first (D-127): seed 019 + synthetic data. Doc 38 **v4.6 (154 rows, D-187/ADR-0004)**. |
| Current task | Lane 1: **2.16 part 1a-4 DONE-in-part @ `<this commit>`** (PDA OTP login screen: `OtpLoginClient` port + mock client + two-step request→verify screen, `/login` route wired to the mock client and navigating to `/home` on success, 9 new `login.*` i18n keys in all six locales; 71/71 tests, round 1 FAIL(7) → fix round → round 2 FAIL(1, process-only) → REVIEW CAP: PASS subset committed, PASS(8 findings, 2 rounds), 1 process finding deferred as `2.16 part 1a-4b`); `pda` lock held; next `2.16 part 1a-3b` (needs `identity` re-claimed by the Master) / `2.16 part 1a-4b` / `1b` (PIN, after SCR-IDN-01) — 2.12 DONE (all 4 parts) @ `830bfaf`. Lane 2: **WBS 4.1a part 3 DONE-in-part @ `fe8a9ad`** — `gl_accounts.is_active` + deactivate/reactivate maker/checker lifecycle, migration `0036` applied (active-children check + three-way keyed UPDATE branch), 258/258 tests green (module 412/412), guards green, PASS(6 findings, 2 rounds; slice-close: 5 findings fixed, 1 deferred to `4.1a part 3 part 2`, 2 rounds); part 2c DONE @ `95c9f89`; part 2 itself DONE-in-part @ `4585fce` (migration `0034`); `4.1a part 2b`, `4.1a part 3 part 2`, `4.1b part 2` (line_dimensions) still open; `billing` lock held, next **4.1b part 2** (dimension values write path). Lane 3: **3.12 part 2a DONE @ `3e3dd73`** (citation + trigger-name comment fixes) → **3.12 part 2c-i DONE @ `33a5446`** (outbox publish + INV-C4-1 driver-document gate reuse, covers terminated-employee too, PASS(4 findings, 2 rounds, accepted as-is per Master ruling matching 3.13 part 5a)); part 2b (`UUID_PATTERN`) reverted/deferred per REVIEW CAP; next **3.12 part 2c-ii** (race-condition branches, audit assertion, RLS-outsider assertion); `3.12 part 2c-i — polish` filed non-blocking. 3.13 part 5 (5a+5b+5c) fully DONE (`110d7ea`/`7ea577c`/`c507e4e`); confirm-side `new.employee_id` exposure closed by migration 0037; 3.13 overall NOT DONE (dep 3.12 part 2b/2c-ii); `hr`+`imile` locks held. One DB per lane (`bash scripts/lane-db.sh <id>`, P4a @ `436be60`). |
| Golden slice / tier / schema / session model | 2.9 ACCEPTED (`.golden-slice-accepted`, `scripts/new-slice.sh` active) · Pilot Tier 0 = local Docker `postgres:16` (D-129), Oracle DEFERRED-POST-PILOT · migrations 0001–0037 applied (0022 withdrawn), next free **0038** (0031 claimed by P6b-2) · session model (D-191 routing v2): lanes sonnet/medium, Master opus/medium until 4.20 then sonnet/medium, pg-reviewer + pg-backend-core opus, pg-backend/pg-frontend/pg-tester/pg-scribe sonnet · **P6c part 1 DONE @ `69899c5`** (identity tx-injectable OTP/session primitives, no migration) → **P6c part 2 DONE @ `b18c0ca`** (`login.ts` `requestLoginOtp`/`verifyLoginOtp`, anti-enumeration shape, barrel exports, test-isolation fix, deadlock regression) — unblocked lane 1 `2.16 part 1a-3`, now DONE-in-part · **Master security task DONE @ `1461d24`** — 3.13 confirm-side SoD DB fix, migration 0037 (`employee_id` IMMUTABLE + confirm-side self-review bound to `old.employee_id`), closing the confirm-side `new.employee_id` exposure |

## Lanes

| lane | task | module lock | worktree |
|---|---|---|---|
| 1 | 2.16 part 1a-4 DONE-in-part @ `<this commit>` — PDA OTP login screen, 71/71 tests green, PASS(8 findings, 2 rounds), 1 finding backlogged as `2.16 part 1a-4b`; next `2.16 part 1a-3b` (needs `identity` re-claimed by the Master) / `2.16 part 1a-4b` / `1b` (PIN, after SCR-IDN-01) — 2.12 DONE (all 4 parts) @ `830bfaf` | `pda` | `../pg-eos-lane-1` |
| 2 | 4.1a part 3 DONE-in-part @ `fe8a9ad` — is_active + deactivate/reactivate lifecycle, migration 0036, 258/258 green; part 2c DONE @ `95c9f89`; part 2 DONE-in-part @ `4585fce` (migration 0034); `4.1a part 2b`, `4.1a part 3 part 2`, `4.1b part 2` open; next: 4.1b part 2 | `billing` | `../pg-eos-lane-2` |
| 3 | 3.12 part 1 DONE-in-part @ `b9c3b1d`, **part 2a DONE @ `3e3dd73`** (citation + trigger-name fixes), **part 2c-i DONE @ `33a5446`** (outbox publish + INV-C4-1 driver-document gate reuse, PASS(4 findings, 2 rounds, accepted as-is per Master ruling)); part 2b (UUID_PATTERN) reverted/deferred per REVIEW CAP; next **3.12 part 2c-ii** (race-condition branches, audit assertion, RLS-outsider assertion); `3.12 part 2c-i — polish` open non-blocking. 3.13 part 5 fully DONE (5a `110d7ea`, 5b `7ea577c`, 5c `c507e4e`); confirm-side `new.employee_id` exposure closed (migration 0037); 3.13 overall not DONE (part 5a — polish open, non-blocking; dep 3.12 part 2b/2c-ii); `hr`+`imile` held | `hr`, `imile` | `../pg-eos-lane-3` |

## Last 5 DONE (newest first)
| task | commit |
|---|---|
| 2.16 part 1a-4 — PDA OTP login screen (pda, lane 1), DONE-in-part — `OtpLoginClient` port + mock client + two-step request→verify screen, `/login` route wired to the mock client, 9 new `login.*` i18n keys in all six locales; 71/71 tests green; round 1 FAIL(7) → fix round → round 2 FAIL(1, process-only) → REVIEW CAP no round 3, PASS(8 findings, 2 rounds); 1 finding backlogged as `2.16 part 1a-4b` | `<this commit>` |
| 3.12 part 2c-i — outbox publish (`imile.driver_id.assigned`) + INV-C4-1 driver-document gate reuse (covers terminated-employee too) on `AssignDriverId` (imile, lane 3), DONE — closes original findings 9/10/11 of `3.12 part 2`; 44/44 tests green, guards green; PASS(4 findings, 2 rounds, accepted as-is per Master ruling matching 3.13 part 5a) | `33a5446` |
| 3.13 — confirm-side SoD DB fix (Master security task), DONE — migration 0037 re-issues `hr.guard_commission_daily_status()` (0035's body verbatim + `employee_id` IMMUTABLE on every UPDATE, confirm-side self-review bound to `old.employee_id`), closing the confirm-side `new.employee_id` exposure; `tests/isolation/tests/hr-commission-confirm-sod.test.ts` 6/6 (4 RED before); isolation 229/229, hr confirm/dispute 62/62, guards G1-G14/G18/G-SEED green; PASS(14 findings, 2 rounds) | `1461d24` |
| 2.16 part 1a-3 — OTP login endpoint (identity, lane 1), DONE-in-part — first-ever `modules/identity` use case over P6c's `requestLoginOtp`/`verifyLoginOtp`; 46/46 tests green; round 1 FAIL(11: 9 fixed, 2 closed by P6c) → round 2 FAIL(3, non-security) → REVIEW CAP no round 3, PASS(3 findings, 2 rounds); 3 findings backlogged as `2.16 part 1a-3b` | `70754ce` |
| 3.12 part 2a — citation + trigger-name comment corrections on `AssignDriverId` (imile, lane 3), DONE — comment/citation-only, 3 files, 8 insertions/8 deletions; the attempted `UUID_PATTERN` fix (finding 3) was reverted and deferred to `3.12 part 2b` per REVIEW CAP; PASS(0 findings, 2 rounds) — the 6+2 findings across both review rounds were entirely on the reverted work | `3e3dd73` |

## Blockers

- `scripts/deploy.sh` missing (G15–G17 NOT RUNNABLE, non-blocking, 0.6b DEFERRED-POST-PILOT). Open G-01 items: G8 anchor storage (≥2028-03, D-115); 3.12/3.14/3.17 imile tables missing `entity_id`/CHECKs/outbox, row m (over-broad termination-trigger reset) (`docs/notes/2026-09-24-imile-agent-scenario.md` §4); 2.15 `space_reservations.qty` no DB CHECK; 3.1 `hr.shift_groups.vehicle_id` pre-existing assignment column the new gate doesn't cover (GM call, `tasks/backlog/MIGRATION-REQUEST-3.md`); INV-C4-1 DB-level enforcement on `tms.delivery_tasks`/`tms.routes.vehicle_id`, required before WBS 3.4, same pattern as `wms.check_space_available()`; 4.2's round-2 G14 red is external — lane 1's own uncommitted migration 0026 on the shared dev DB, not lane 2's to fix; CI's ephemeral DB is the real merge gate.
- 2.16 part 1a-3b (lane 1, `identity`, backlog, low priority): `InvalidOtpError` message needs an "(Allowed: ...)" clause; a missing test pinning `verifyOtpCode`'s response to exactly `{token, expiresAt}`; brief Read ONLY list never amended for 2 files read directly — see MASTER_BACKLOG. · 3.12 part 2b (lane 3, `imile`): `UUID_PATTERN` nil/max-UUID fix — reverted per REVIEW CAP (round 1 FAIL: over-widened match; round 2 FAIL: case-sensitivity mismatch vs zod + fabricated verification comment); backlog, see MASTER_BACKLOG. 3.12 part 2c-ii (lane 3, `imile`): two untested race-condition branches, missing audit-content assertion, weak RLS-outsider assertion (findings 4/5/6 of the original `3.12 part 2` list) — see MASTER_BACKLOG. `3.12 part 2c-i — polish` also open, non-blocking (4 round-2 test-quality findings on `3.12 part 2c-i`, accepted as-is). **3.13 part 5 (a+b+c) now fully DONE** (part 5c @ `c507e4e` closed the confirm-side race-test barrier + audit-content assertions, the last of the three part-4-deferred findings; part 5a @ `110d7ea`, part 5b @ `7ea577c`). 3.13 overall still not DONE (dep 3.12 part 2b/2c-ii open). Confirm-side `new.employee_id` exposure CLOSED by migration 0037. Still open (non-blocking, MASTER_BACKLOG): `3.13 part 5a — polish` (5 items on migration 0035's own tests). · 4.1a part 2b (lane 2, `billing`): Submit/Approve/Reject/Cancel application/api layer, deferred for budget — see MASTER_BACKLOG. 4.1a part 3 part 2 (lane 2, `billing`): `isCreateRequestComplete`/`assertCreateRequestComplete` type-widening in `invariants.ts` (type/comment-only, no behavior impact) — see MASTER_BACKLOG. · 4.1b part 2 (lane 2, `billing`): `billing.line_dimensions` + `dimension_values`/`value_id`/`assert_dimension_value()` trigger — design fully ruled (D-190, SCR-ACC-01 row 9), not GM-blocked, just not yet built — see MASTER_BACKLOG.
- The two 0.9 WIP branches (`claude/postgresql-16-254336` @ `0217e85`, `claude/resume-4c8ecc` @ `f127ab2`) are never merged, never deleted.
- Batched Master tasks (frozen paths): `entityId` on `WithContextCtx`; `packages/domain-kit` browser break (node:crypto in browser bundle); root `eslint.config.mjs` cwd bug; X part 2 — resolve-hashes attributes a placeholder to the last commit that edited its line (blame), not the one that introduced it; fix via `git log -L`. Also (P2 review round 2 #3): `scripts/new-slice.sh:11,205` + template text in `modules/{catalog,fleet,hr,imile}/index.ts` cite the retired "SPEED AND QUALITY (v5)" → "OPERATING RULES · REPLICATE"; `packages/i18n` bootstrap (never created — SCR-I18N-01 §1; 2.11/2.9b keys code-referenced only). D-125 cleanup still open (D-132 disposition, `docs/DECISION_LOG.md`): G1/G2 biometric-retention wording (doc 40:668, doc 25:429) needs a GM-set value, none invented; G3 diagram `01-08.mmd/.svg` not regenerated (needs the mermaid renderer).
- WBS 1.11 BLOCKED (D-178) — see `docs/notes/2026-09-25-wbs-1.11-premature.md`. Pre-existing, untouched: ~7 failures in `tests/evaluate-alerts/**` + `tests/integration/{schema-invariants,audit-chain-*}.test.ts`; 8 `tests/receive-inbound` failures from orphaned `wms.locations` (`T9-*`) fixtures.

## Next 3 tasks

1. Lane 1: 2.16 part 1a-4 DONE-in-part — next: `2.16 part 1a-3b` (needs `identity` re-claimed by the Master) / `2.16 part 1a-4b` (process fix-up) → 1b (PIN, after SCR-IDN-01)
2. Lane 2: 4.1b part 2 (dimension values write path) → 4.19 → 4.20 (ADR-0004) — `4.1a part 2b`/`4.1a part 3 part 2` remain open backlog items
3. Lane 3: 3.12 part 2c-ii (race-condition branches, audit assertion, RLS-outsider assertion) → 3.4 (2.12 DONE, dependency met) → 3.12 part 2c-i — polish / 3.13 part 5a — polish (non-blocking backlog)
