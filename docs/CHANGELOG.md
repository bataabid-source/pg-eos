# CHANGELOG — PG-EOS repository

One entry per completed task, newest first (CLAUDE.md · GIT · DOCUMENTATION). Each entry: WBS ID · what changed · Model · Delegated · Review · tokens (estimate). History that leaves `docs/PROJECT_STATE.md` lands here.

---

## 2.8 — Stock ledger + derived balance + verify_balance_integrity() — DONE (2026-09-23)

- **Delivered (doc 38 acceptance verbatim "Zero rows after 1,000 random movements; property test green"):** `modules/wms/src/stock-ledger` (postMovement, postTransfer, reverseMovement, rebuildBalance, domain folds), event `wms.stock.moved` (`packages/events/catalog.ts`), outbox + audit per ledger row in one transaction, audit row last (ADR-0002); mechanism slice, flat layout (0.17 precedent), no endpoint/UI/XState, no schema change in this commit (schema fixes landed as `e5bff15` SCR-AUDIT-01 and `787d9dc` SCR-WMS-01).
- **Tests:** `modules/wms` 85/85 (wh1 29, domain 12, stock-ledger 24, balance-integrity-batch 8, rebuild-balance-scope 3, rebuild-balance-scope-full 4, rebuild-balance-lock 5); property test 1,000 movements fast-check seed 20081000, several batches per location.
- **Review findings closed at close-out:** rebuild scope (RebuildScopeError, internal + all entities or RLS bypass), rebuild/posting serialisation (shared/exclusive advisory lock — the stress test reproduced a real race before the fix: 1 guard row), postTransfer audit-row-last, error messages state what is allowed, vacuous rejection assertion fixed, named test-data constants, env restore in scope tests, citations by name.
- **Deviations recorded (review findings 10–12):** pg-tester added devDependency `@pg-eos/domain-kit` (brief allowed only fast-check) — needed for Quantity/FixedClock in tests; pg-backend edited `modules/wms/tsconfig.test.json` (not in its list) to include `tests/unit`; pg-backend added dependency `drizzle-orm ^0.45.3` (not in its list) — required for `sql`/`NodePgDatabase` under pnpm strict resolution, same version as `packages/db`, `events`, `identity`; fast-check is the property-test tool named in doc 36 §5-5; the brief's Read ONLY list had ≈ 25 files, over the 12-file / 1,500-line budget (`docs/MODEL_ROUTING.md`) — the slice was not split; brief decision 2 (`insert … on conflict do update`) replaced by UPDATE-first under sorted advisory locks because PostgreSQL checks `no_negative_stock` on the INSERT candidate row before the ON CONFLICT probe (confirmed by pg-reviewer from the PostgreSQL 16 source).
- **doc 36 §5-4 point 10 (monitoring):** no board in this slice; G1 in `pnpm guards:run` is the only monitor until WBS 2.17 "Warehouse Grafana board" (and X.3 per-module board) delivers the stock board (D-blueprints/03).
- **Carried forward (open questions, not findings):** INV-C3-7 adjust approval → count/adjust slice (2.13); Idempotency-Key → 2.9 endpoint; `audit_log.device_id` not filled from input → 2.9; runtime still superuser (0.18 item 7); postTransfer from = to not rejected (no rule in schema/40); INV-C3-3 SKU/order client match → order slices; domain coverage ≥ 90% and Stryker ≥ 75% measured in CI (G16).
- Model: opus (Master session, claude-opus-5-5) · sonnet (workers) · Delegated: pg-tester (sonnet), pg-backend (sonnet), pg-reviewer (opus), pg-scribe (sonnet); Master direct: `packages/events/catalog.ts` entry, `modules/wms/vitest.config.ts` fileParallelism · Review: PASS(<N> findings fixed) · tokens: ≈ 2.2M (estimate: pg-tester ≈ 800k, pg-backend ≈ 600k, pg-reviewer ≈ 450k, pg-scribe ≈ 150k, Master ≈ 200k)

---

## 2.8 (fix) — SCR-WMS-01: G1 keyed by batch_no and compared both ways — migration 0005 (2026-09-23)

- **Decision (SCR-WMS-01, phase H of GM directive 2026-09-23):** `wms.verify_balance_integrity()` (guard G1) now folds the ledger by the full `stock_balance` key `(client_id, sku_id, location_id, coalesce(batch_no, ''))` — part 1, the GM's wording — and compares in BOTH directions, so a non-zero balance row with no ledger rows is reported — part 2, ruled within SCR-WMS-01 by pg-reviewer on doc 40 INV-C3-2 ("Rebuilt balance from ledger equals stored balance") and P4. Return type gains `batch_no`: `TABLE(client_id, sku_id, location_id, batch_no text, ledger_qty, balance_qty, diff)`. Invoker function: valid only for a role that bypasses RLS (stated in its comment).
- **Files/versions:** `database/schema/01-Data-Model.sql` 1.0 → 1.1 (function block only; the function now starts at 01:1499); migration `database/migrations/0005_M_balance-integrity-batch.sql` (drop + create, re-runnable); `D-blueprints/08-Data-Model-Maps.md` two rows; `CHANGELOG-v4.md` §16; `tasks/LANE_LOCKS.md` 0005 line; src comments and message strings (citations by name only) in `modules/wms/src/stock-ledger/{domain,errors,post-movement,rebuild-balance}.ts`; `modules/wms/tests/unit/stock-ledger.domain.test.ts` (citation by name only); `.claude/briefs/_slice-2.8.brief.md` (citations + SCR-WMS-01 decision line).
- **Tests:** new `modules/wms/tests/integration/balance-integrity-batch.{feature,test.ts}` (8; 5 were RED by behaviour on the old function: two batches → 2 rows, one-batch deviation → 2, orphan balance → 0, batch mismatch → 0, column list); `stock-ledger.test.ts` property test (1,000 movements, seed 20081000) now draws several batches per location — RED on the old function (false deviations), GREEN now; same file also fixed: fixture role prefix `pgeos_t_` (was reserved `pg_`), `CONCURRENCY_TEST_TIMEOUT_MS` 60 s, 0 skipped. `modules/wms/tests/integration/stock-ledger.test.ts` / `stock-ledger.domain.test.ts` / `stock-ledger.feature` (wording) also updated. `modules/wms` now 73/73 (wh1 29, domain 12, stock-ledger 24, balance-integrity-batch 8). `apply.sh --recreate` 0001–0005 green, G1–G14 + G18 + G-SEED green.
- **Review:** migration gate round 1 FAIL(8) → round 2 PASS with 3 new slice-close findings (9–11); close round 1 FAIL(10) → fixed; close round 2 FAIL(6) → fixed ⇒ PASS(26 findings fixed) (8 + 2 [10, 11] + 10 + 6; gate finding 9 is carried to feat(2.8) and not counted). Finding 9 (rebuildBalance under partial RLS view corrupts balances) is carried to the 2.8 close-out together with the ADR-0002 audit-row-last fix in postTransfer.
- Model: opus (Master session, claude-opus-5-5) · sonnet (workers) · Delegated: pg-tester (sonnet), pg-backend (sonnet — comments and message citations only), pg-reviewer (opus), pg-scribe (sonnet); Master direct: one-character comment fix in rebuild-balance.ts (routing row "Single-file edit ≤ 30 lines, no new logic → Master, direct", BOOTSTRAP-v5 §4) · Review: PASS(26 findings fixed) · tokens: ≈ 450k (estimate)

---

## 0.9 (fix) — SCR-AUDIT-01: audit hash chain ordered by chain_seq — ADR-0002, migration 0004 (2026-09-23)

- **Decision (ADR-0002, phase G of GM directive 2026-09-23, decision #4):** `platform.audit_log` gains `chain_seq bigint not null`, assigned by the trigger after the advisory lock as previous + 1 (no sequence); unique index `<partition>_chain_seq_key` on every partition incl. default; trigger and verifier `SECURITY DEFINER` (owner superuser/BYPASSRLS), `TimeZone` UTC / `DateStyle` ISO,YMD pinned, `READ COMMITTED` required. Verifier `platform.verify_audit_chain(p_anchor_seq bigint default 1, p_anchor_prev_hash text default null)` returns `(chain_seq, id, occurred_at, problem, detail, expected_hash, actual_hash)`, `problem` ∈ `hash_mismatch · prev_hash_mismatch · duplicate_chain_seq · chain_seq_gap · partition_missing_chain_seq_unique_index · definer_owner_cannot_bypass_rls · anchor_invalid · anchor_not_found`; `EXECUTE` revoked from `PUBLIC` on both.
- **Files/versions:** 13B v4.2 → v4.3; migration `database/migrations/0004_M_audit-chain-seq.sql` (re-runnable; one-time rebuild refused without `pgeos.audit_chain_rebuild=allow`); doc 40 → v4.3 (§B2 + Part F G8 + anchor); doc 31 → v4.1 (§3-3); `D-blueprints/08-Data-Model-Maps.md` two rows; `CHANGELOG-v4.md` §15; `modules/platform/vitest.config.ts` `fileParallelism` false.
- **Tests:** audit-chain-concurrency (G4), audit-chain-seq (15), schema-invariants (+2), isolation teardown.
- **Results — G4 8 writers × 500:** before `2008 · 2009 · 2004` broken rows; after `0 · 0 · 0` (repo build). `platform` 23/23 ×3, isolation 43/43, `guards:run` G1–G14 + G18 + G-SEED green, build 9/9. Head lookup on 24 partitions: Merge Append, 1.2 ms.
- **Review rounds:** migration gate FAIL(14) → FAIL(4) → PASS; close review FAIL(4) (doc 40 "should"→"must" audit-row-last, doc 31 §4→§3-3 citations, D-08 problem list, LANE_LOCKS "applied") → fixed. Total findings fixed: 22.
- **2.8 close-out blocker found by pg-tester verify:** `modules/wms/src/stock-ledger/post-movement.ts` `postTransfer` writes the audit row for the out-entry before the in-entry `stock_balance` update → violates ADR-0002 / doc 40 §B2 "must write the audit row as the last statement before commit"; produced one deadlock in a shared-DB full run (platform writers chosen as victims; chain stayed intact). Plus known 2.8 test items: 5 s timeout on the 1,000-movement test, reserved `pg_` fixture role name (3 skipped), cleanup FK error (delete `wms.skus` vs `stock_movements_sku_id_fkey`).
- **Open G-01 item for the GM:** where the G8 anchor is stored before the first partition detach (≥ 2028-03).
- Model: opus · Delegated: pg-tester (opus — escalation for ADR/security design, GM directive 2026-09-23), pg-reviewer (opus), pg-scribe (sonnet) · Review: PASS(22 findings fixed) · tokens: ≈ 900k (Master ≈ 250k, pg-tester ≈ 380k, pg-reviewer ≈ 480k — estimate)

---

## X — Agent write-scopes + fixed slice sequence — GM directive 2026-09-23 (2026-09-23)

- **pg-tester** writes only test files (`tests/**`, `**/tests/**`, `*.test.*`, `*.spec.*`, `features/**`, `*.feature`); runner config and `package.json` changes are requested from the Master. Called twice per slice: RED first, then VERIFY after the build.
- **pg-backend** never creates, edits or deletes a test file; a suspected test defect is reported and routed back to pg-tester.
- **pg-reviewer** is called at slice close (after pg-tester's verification, before pg-scribe) and BEFORE any migration touching the schema, RLS or the audit chain is written; it now also checks both write-scopes.
- **Fixed sequence** documented in CLAUDE.md BUILD METHOD, BOOTSTRAP-v5 §2/§3/§6 (→ 5.2, CHANGELOG-v4 §14), `.claude/commands/slice.md`, `docs/AGENT_WORKFLOW.md`: tester → backend → tester (verify) → reviewer → scribe → one `feat(<WBS>)` commit. No new agent. Enforcement is by agent definition plus pg-reviewer's write-scope check; no hook was added.
- Model: opus (session, Master direct — doc edits ≤ 30 lines each) · Delegated: none · Review: n/a · tokens: ≈ 25k

---

## X — Package docs pin pg-scribe to sonnet + archive of worktree resume-d2f142 — GM directive 2026-09-23, F (2026-09-23)

- **F1 (package docs):** `docs/package/BOOTSTRAP-v5.md` §3 line 69 (`pg-scribe     model: haiku` → `model: sonnet`) and §4 routing-table line 84 (`pg-scribe | haiku | ≤ 10k` → `pg-scribe | sonnet | ≤ 10k`); no other haiku mention in the file refers to pg-scribe (line 63 is the generic tier-alias list, left as-is) and no separate mix/percentage line exists in §4 to fold. Version 5.0 → 5.1, date → 23 September 2026. `docs/package/41-Cloud-and-AI-Efficiency.md` §1 line 93 (`pg-scribe` haiku → `pg-scribe` sonnet, inside the "القاعدة ٨" row); no other haiku mention in the file. Version 4.0 → 4.1, date → 23/09/2026. `docs/package/CHANGELOG-v4.md`: appended `## 13. إضافة (23/09/2026) — تثبيت نموذج pg-scribe على sonnet` (next in sequence after `## 12.`), one bullet listing both files, the lines changed, the version bumps, and citing GM directive 2026-09-23 (no haiku; pg-scribe = sonnet; `CLAUDE.md` and `docs/MODEL_ROUTING.md` already aligned in `dc3cc1b`).
- **F2 (archive resume-d2f142):** the unregistered directory `.claude/worktrees/resume-d2f142` (stale `.git` file, base tag `archive/claude/resume-d2f142` = `d8ccb7e`) held unmerged 0.13 contracts work not present at that tag. Copied its full content (excluding `.git`, `node_modules`, `dist`, `.turbo`, `coverage`, `*.tsbuildinfo`) into a temporary detached worktree off the tag, committed as `chore(0.13): archive untracked content of worktree resume-d2f142` (19 files changed, 1109 insertions(+), 34 deletions(-)), and tagged `archive/wt/resume-d2f142` → `fab7e5f68a175015d7bb33ba2d9a02e4eede4227` (verified the tag's tree contains `packages/contracts/_harness/contract-test.ts`). Its merge into main is assessed within WBS 0.13; never merged automatically. Temporary worktree and branch removed, `git worktree prune` run, then the now-unregistered directory `.claude/worktrees/resume-d2f142` itself deleted and confirmed gone (required a robocopy /MIR-over-empty-dir pass first — path exceeded `Remove-Item`'s handling under `node_modules/.pnpm/...`). `.claude/worktrees/upbeat-wu-e2c00c` (empty, OS-locked per prior entry) — one `Remove-Item` attempt made, still failed ("process cannot access the file"); left as-is, not fought further.
- Model: sonnet · Delegated: general-purpose (sonnet) · Review: n/a · tokens: ≈ 35k

---

## X — Archive stale branches + model pinning — GM directive 2026-09-23, E2 + E3 (2026-09-23)

- **E3 facts:** tags created `archive/claude/postgresql-16-254336` (`0217e85`, "wip(0.9): platform doc_no_probe + real G13/G8 guard proofs — paused mid-review"), `archive/claude/resume-4c8ecc` (`f127ab2`, "wip(0.9): checkpoint — doc_no_probe fixture, G13/G8 governing-form guard tests"), `archive/claude/project-setup-guide-a57502` (`2b5dbb2`, "docs: PROJECT-SETUP-GUIDE clarifies brief regeneration as its own commit"), `archive/claude/resume-d2f142` (`d8ccb7e`, "test(0.13): RED tests for packages/contracts registry/harness/openapi"); the four branches deleted after verifying each tag equals the tip; worktree `.claude/worktrees/resume-4c8ecc` removed; `git branch -a` now only `main`. Unregistered directory `.claude/worktrees/project-setup-guide-a57502` was identical to its tag (recursive content diff excluding `.git`/`node_modules`/`dist`/`.turbo`) → deleted. Unregistered directory `.claude/worktrees/resume-d2f142` is NOT identical → kept: modified vs tag: `docs/CHANGELOG.md`, `docs/PROJECT_STATE.md`, `packages/contracts/package.json`, `packages/contracts/tests/harness.test.ts`, `packages/contracts/tests/openapi-document.test.ts`, `packages/contracts/vitest.config.ts`, `pnpm-lock.yaml`, `tasks/LANE_LOCKS.md`, `tasks/MASTER_BACKLOG.md`; files not in the tag: `packages/contracts/_harness/contract-test.ts`, `_harness/index.ts`, `_shared/headers.ts`, `_shared/index.ts`, `_shared/problem.ts`, `_shared/registry.ts`, `openapi/openapi.json`, `scripts/generate-openapi.ts`, `index.ts`, `tests/generate-openapi.test.ts` (unmerged 0.13 work that predates the merged 0.13 at `335b5db` — GM to decide). Both unregistered dirs carried a stale `.git` file pointing at the pre-move path `Desktop/New sys/claude-kit`. `.claude/worktrees/upbeat-wu-e2c00c` remains an empty, OS-locked directory (B6).
- **E2 facts:** `.claude/agents/pg-scribe.md` and `.claude/commands/state.md` repinned `model: haiku` → `model: sonnet`; the other four agent files (`pg-backend`, `pg-frontend`, `pg-tester` = sonnet, `pg-reviewer` = opus) checked against the GM decision (sonnet by default; opus for the golden slice 2.9, ADRs and security/review) and left unchanged — already correct. `CLAUDE.md` MODEL ROUTING bullet and the Pre-commit (husky) bullet under SPEED AND QUALITY rewritten: the husky mention is replaced by a description of the versioned `.githooks/` mechanism (`core.hooksPath` set by `scripts/install-hooks.mjs`); grep confirms no other `husky` mention remains in `CLAUDE.md`. `docs/MODEL_ROUTING.md` updated: the tier cell for `State/backlog/CHANGELOG/i18n/renames` (pg-scribe) `haiku` → `sonnet`; the pinning sentence `pg-scribe=haiku` → folded into `pg-backend/pg-frontend/pg-tester/pg-scribe=sonnet`; the expected token-distribution mix `~75% sonnet (workers) · ~10% haiku (scribe) · ~10% opus (review) · ~5% session` → `~85% sonnet (workers + scribe) · ~10% opus (review) · ~5% session`. `tasks/LANE_LOCKS.md`: added lock row `wms | M | 2.8 | 2026-09-23 | (main — single lane, GM directive 2026-09-23)` — the Master claims 2.8, pg-tester starting. Note: `docs/package/BOOTSTRAP-v5.md` §4 (lines 69, 84) and `docs/package/41-Cloud-and-AI-Efficiency.md` §1 (line 93) still say pg-scribe = haiku — package documents, not edited under this task's scope; needs a package edit + CHANGELOG-v4 entry, reported to the GM.
- Model: sonnet (E2, E3 workers) · Delegated: general-purpose (sonnet) ×2 · Review: n/a · tokens: ≈ 80k

---

## 1.5 — ADR-0001 accepted: 1.5 as a proof slice ahead of 2.9 (2026-09-23)

- **Decision (ADR-0001, GM directive 2026-09-23 B1):** WBS 1.5 is built as a limited proof slice in the 2.1 pattern — data model + seed only, no UI, no workflow, no public API — to unblock 2.6 → 2.9; the full 1.5 slice is replicated later from the golden slice 2.9; 1.5 becomes `DONE (proof)` and is excluded from the completion ratio. The ADR records that the schema holds no sales seed (seed = labelled test fixtures; a persistent seed file needs an SCR under G-01) and the INV-C2-3 `≥ 0.85` vs `sales.possible_duplicates` `> 0.85` discrepancy the proof must surface.
- **`docs/adr/` created (GM directive 2026-09-23 E1):** `README.md` (purpose, opus authorship, `ADR-NNNN-<slug>.md` numbering, template Context · Decision · Alternatives rejected · Consequences · Status) and `ADR-0001-1.5-proof-slice.md`.
- **CLAUDE.md BUILD METHOD:** one bullet pointing to `docs/adr/` and to ADR-0001; it also names the standing substitute for pre-2.9 proof/mechanism slices (0.9, 0.17, 0.18, 2.1, 2.8 — CHANGELOG 0.16 / 2.1) so the pointer does not contradict the slices already built (opus review note).
- Model: opus (ADR review and acceptance) · Delegated: — · Review: PASS (ADR self-review, findings listed in the commit body) · tokens: ≈ 45k

---

## X — Branch and worktree cleanup — GM directive 2026-09-23, B6 (2026-09-23)

- **Rule applied:** no parallelism before 2.9 is accepted; delete only refs with zero commits unmerged into `main`; anything holding unmerged work stays and is listed.
- **Deleted (0 unique commits each):** worktree `.claude/worktrees/upbeat-wu-e2c00c` + branch `lane/2` (tip `bc0062a` = main); worktree `.claude/worktrees/whats-next-3659bf` (detached `a8f4899`, in main); branches `claude/document-abandoned-branches-verify-tests-79245f` (`3218aa6`), `claude/project-state-backlog-review-7d0a77` (`a8f4899`), `claude/upbeat-wu-e2c00c` (`d734508`), `claude/wbs-0-18-rls-client-isolation-839a72` (`f9a2bc6`), `claude/whats-next-3659bf` (`a8f4899`). `git worktree prune` run.
- **Kept (unmerged work, 1 unique commit each):** `claude/postgresql-16-254336` (`0217e85`, "wip(0.9): platform doc_no_probe + real G13/G8 guard proofs — paused mid-review") and `claude/resume-4c8ecc` (`f127ab2`, "wip(0.9): checkpoint — doc_no_probe fixture, G13/G8 governing-form guard tests") — the two 0.9 WIP branches of `docs/notes/0.9-abandoned-wip.md`, never merge, never delete; the registered worktree `.claude/worktrees/resume-4c8ecc` (detached at `f127ab2`) stays with it; `claude/project-setup-guide-a57502` (`2b5dbb2`, "docs: PROJECT-SETUP-GUIDE clarifies brief regeneration as its own commit"); `claude/resume-d2f142` (`d8ccb7e`, "test(0.13): RED tests for packages/contracts registry/harness/openapi").
- **Left on disk, not git worktrees (excluded via `.git/info/exclude`):** `.claude/worktrees/project-setup-guide-a57502` and `.claude/worktrees/resume-d2f142` — plain directories with no `.git` link, so their content cannot be verified as merged; not deleted. `.claude/worktrees/upbeat-wu-e2c00c` is now an **empty** directory that Windows reports as in use by another process — remove by hand.
- Model: fable (claude-fable-5-1) · Delegated: none · Review: n/a · tokens: ≈ 15k.

---

## X — Governance and environment restoration — GM directive 2026-09-23, phases A + B (2026-09-23)

- **Phase A:** environment restored — `pnpm install` (lockfile unchanged) recreated the missing `modules/wms/node_modules`. Build verification: `pnpm -w build --force` 9/9 green (domain-kit, wms, platform, db, events, identity-mechanisms, contracts, identity, documents packages); `pnpm -w test -- --force` 10/10 tasks green with per-package counts (domain-kit 87, wms 29, platform 5, db 11, events 8, identity-mechanisms 31, contracts 44, identity 15, documents 6 tests), no turbo cache used. PATH amended with pnpm executable location (user's `~/.bashrc` only, not repository). Session model deployed: fable (`claude-fable-5-1`, GM-specified).

- **Phase B:** governance — (B1) ADR for the 1.5 proof slice **BLOCKED**: no ADR folder exists in the repo; draft held outside the repo until the GM names the folder. (B2) `database/schema/13B-Schema-Reference-Consolidation.sql`: `tms.contact_log.cost` `numeric(10,4)` → `numeric(14,3)` (GM: every monetary amount is numeric(14,3), KWD); schema file only, the migration is created by the owning task (3.8); 13B header → 4.2; `apply.sh --recreate` re-run green (175 tables, migrations 0001–0003, G1–G13/G18 = 0, G13 100/100) and the live column now reads numeric(14,3). (B3) CLAUDE.md GIT rule rewritten: no standalone "record the verified hash" commit; the task ↔ code link is `git log` with `type(WBS):`; the previous task's hash is recorded in PROJECT_STATE inside the next task's commit; DEFINITION OF DONE wording aligned. (B4) `.githooks/commit-msg` (versioned) refuses any first line not matching `type(WBS): description` with a WBS ID from 38-WBS or `X`; activated by `scripts/install-hooks.mjs` from the root `prepare` script; verified: refuses `bad message`, `feat(9.9): …`, `Revert "x"`; accepts `docs(X): …`, `feat(2.8): …`, `docs(X.3): …`. (B5) pg-scribe: MASTER_BACKLOG 1.2 1.5 2.8 3.1 3.3 3.14 5.13 6.4 TODO → READY; 0.17 note (excluded from ratio, acceptance bound to 0.19); PROJECT_STATE refreshed (12/132, Next 3 without 0.19, external WAITING_GM blockers 2.2 / 0.2→0.3→0.5→0.6 / 0.8, Phase-0 gate open, 55 lines); CHANGELOG-v4 §11 (retroactive: 8d62747 SCR-I18N-01 and f03e160 D-002) and §12 (B2, B3, B4, B7); version bumps 19 · 28 · 30 · 38 · BOOTSTRAP-v4 · EXECUTION-MASTER-v4 → 4.1, 40 → 4.2 (D-blueprints 03/06/15 carry no version field). (B6) branch/worktree cleanup is its own commit `chore(X)`.

- **Phase C (deferred pending explicit GM approval):** WBS 2.8 — stock ledger + derived balance.

- **Phase D (after C):** WBS 1.5 — proof slice (unblocks 2.6 → 2.9) — once ADR folder exists.

- **B7 Confirmations:** `platform.test_probe` and `identity.test_probe` are not tables — string values (`aggregate_type` in `platform.outbox` seeded during test; permission code in `identity.permissions`), created and deleted within test boundaries (afterAll or per-test delete). **Accepted by GM.** `modules/wms` hand-scaffolded (documented WBS 0.16) — **accepted by GM.**

- Model: fable (claude-fable-5-1, session model, reported verbatim) · Delegated: pg-scribe (haiku) · Review: n/a (no code, no schema semantics changed beyond one column type) · tokens: ≈ 40k (estimate: reading context facts, editing 11 files, verifying line counts, draft commit message)

---

## 2.1 — Register WH1, its zones and its 8 space blocks — proof slice (2026-09-23)

- **Delivered (acceptance criterion verbatim).** Blocks are `P-A` 288 · `P-A1` 12 · `G-B` 906 · `G-C` 45 · `M-B` 906 · `M-C` 45 · `T-B` 906 · `T-C` 45; storage capacity 3,153; 3,301.641 m³ — proven via `modules/wms/tests/integration/wh1-setup.test.ts` (29/29 green tests, independent verification of eight space blocks, eleven zones with their location counts, totals, and wms.verify_wh1() 21/21 passed). The test suite `wh1-setup.feature` (Gherkin, written first) specifies the acceptance criterion in executable form. No schema change; read-only proof (SELECT only, pool closed in afterAll).
- **Scaffold decision (new-slice.sh no-op).** This is a proof-only slice, same shape as WBS 0.9 and 0.18 — the schema file `019-Warehouse-WH1-Setup.sql` already delivers the data; the slice is the permanent, re-runnable proof. No domain/application/api code to build; pg-backend not used. `modules/wms` scaffolded exactly like `modules/platform` and `modules/identity` (the standing substitute per CHANGELOG 0.16): package.json (`@pg-eos/wms`), tsconfig.json, tsconfig.test.json, vitest.config.ts, index.ts (empty M04 shell). `scripts/new-slice.sh` remains a no-op; the hexagonal tree is not created.
- **Build method.** Gherkin scenario written first, then test implementation — **GREEN immediately** against the live database (proof of pre-existing schema, doc 19 §4 and `019` already deliver the exact numbers). No RED phase; the acceptance criterion was verified by running the test suite (29 tests × 5 independent runs: 2 by pg-tester, 2 by the Master, 1 by pg-reviewer). Every number in the test is a doc-19 literal with its line cited (positions/m³: lines 127-135; zone/block_type and codes: lines 256-263; zones: §3-1 lines 111-119, §2-4 lines 93-102, lines 224-240; operational/structural totals: doc 40 line 239). Numeric comparisons in SQL as `= $n::numeric` with no display-format strings.
- **Review — FAIL(9) → PASS(0).** Round 1: pg-reviewer identified nine findings: (1) zones were proven only by verify_wh1's self-check, not against doc literals → fixed by adding eleven-zone it.each with doc-literal comparisons; (2) `'3153.000'` and `'.000'` display formats used as literals in assertions → fixed by SQL numeric equality, removing display formats; (3) zone/block_type citation pointed at wrong doc-19 lines → corrected to exact lines 256-263; (4) "exactly eight" checked only count = '8', not the exact codes → fixed by derived sorted-codes equality from the doc table, comparing [G-B, G-C, M-B, M-C, P-A, P-A1, T-B, T-C] (database collation identical under C and en_US); (5) feature file was narrower than the test scope → feature expanded to state whole-table check for WH1; (6) Gherkin continuation lines lacked keywords → every clause now its own step with explicit keyword; (7) brief's Read ONLY list was narrower than what was legitimately read → brief widened to doc 19 §3-1 / §2-4 / §3-4 and lines 224-240 / 256-263, doc 40 line 239, modules/identity/package.json and the three workspace-discovery files (eslint.config.mjs, pnpm-workspace.yaml, root package.json), and its test rules reconciled with finding 2; (8) work split across `wip(2.1)` checkpoints `deb75d5`, `7eddafa` (two transient network errors killed pg-tester mid-run — checkpoint strategy from a901a04) → squashed into single closing commit; (9) branch was on 0.18 instead of lane/2 → moved to `lane/2`. Reviewer also noted: the sorted-code comparisons rely on database collation; identical under C and en_US for today's eight codes.
- **Verification, independently re-run by Master and pg-reviewer.** `pnpm typecheck` root 11/11 (was 10 — `@pg-eos/wms` now included). `pnpm lint` clean. `pnpm lint:boundaries` BOUNDARIES ENFORCED (A–F). `pnpm --filter @pg-eos/wms test` — 29 tests, green, run twice by pg-tester, verified by Master and pg-reviewer (29/29 each run, 5 independent verification runs total). Guards G1–G14, G18, G-SEED all green (G15–G17 not yet in runner); no schema change so G-01/G-06 unchanged. Read-only suite: no rows written, pool closed in afterAll.
- Model: fable (claude-fable-5-1 — the session model the GM set; not one of the four listed tier aliases, reported verbatim per `docs/MODEL_ROUTING.md`, never silently mapped) · Delegated: pg-tester (sonnet, interrupted twice by ENOTFOUND, resumed + 1 fix round) · pg-reviewer (opus, 2 rounds — ≈ 80k, over MODEL_ROUTING's 30k-per-review budget, recorded at the reviewer's request) · pg-scribe (haiku, records + lock release) · Review: PASS(9 findings fixed) · tokens: ≈ 300k (estimate: pg-tester ≈ 55k + 55k + 75k, pg-reviewer ≈ 80k across 2 rounds, pg-scribe ≈ 10k, Master ≈ 40k).

---

## 0.18 (phase 2) — SCR-RLS-01 and SCR-RLS-02 applied: D-002 (2026-09-23)

- **GM approval and D-002 applied.** SCR-RLS-01 and SCR-RLS-02 both explicitly approved by the GM on 2026-09-23 ("نفذ الاصلاحات" — execute the fixes). **Migration `0003_M_rls-scr-01-02.sql` applies D-002:** SCR-RLS-01 Option B+C (entity_scope gating on seven tables; client users hold no user_entities via triggers), SCR-RLS-02 Option A+B+C (RLS on audit_log parent, G7 widened, auto-policy loop). **Migration 0003 is re-runnable:** precondition accepts the gated form and skips already-applied policies with a NOTICE.
- **Build method.** pg-tester rewrote the suite RED first against the un-migrated database — exactly the expected 8 red / 21 green (3× SCR-RLS-01 requirement, 2× Option C SQLSTATE 23514, 3× SCR-RLS-02), witnessed by the Master; G7 red via the real guard script (`G7|platform|audit_log`). Migration 0003 written, its preconditions checked read-only on the live database (7 predicates identical, 0 client users with entities, `audit_log` relkind p), applied → 29/29 green, G1–G14 green. Then `apply.sh --recreate` (schema 4/4, migrations 3/3, 175 tables, G7 = 0) exposed an order-dependent test: the SCR-RLS-02 vacuity guard relied on audit rows left behind by WBS 0.17's tests — nothing in the schema writes audit rows on its own (the only audit triggers are the hash-chain triggers). Fixed by seeding the block's own audited row through the parent (the shape `modules/platform/tests/integration/schema-invariants.test.ts` uses) with a tail-only, chain-safe delete; while verifying it pg-tester found `occurred_at` (timestamptz, microseconds) never matched after a JS `Date` round-trip (milliseconds) so fixture rows accumulated — fixed by round-tripping as `::text`. Fresh database → 29/29.
- **Acceptance met — isolation test 43 passed (43 total) on fresh database.** Tests run once on `apply.sh --recreate`, then twice more; identical results. Zero rows on direct ID substitution, no error, verified for both SCR-RLS-01 and SCR-RLS-02 scenarios including dual-role user. Residue cleared: sales.accounts 0 · pgeos_rls% roles 0 · _rls_isolation_fixture audit rows 0 · `platform.verify_audit_chain()` 0 rows. identity.users 0 (fresh database).
- **Migration properties.** Migration `0003_M_rls-scr-01-02.sql` re-applied six times on the live database — four by the Master, two by pg-reviewer — each time skipping the seven gated policies with a NOTICE; then once on recreated environment with zero previous state → green. Option B precondition refuses to overwrite a drifted predicate (no silent assumption of prior state). No new column. Option C applies two SECURITY DEFINER triggers (`user_entities_reject_client_user` / `users_reject_client_with_entities`), SQLSTATE 23514.
- **Guard and loop updates (doc 40 Part F rank-1, only `relkind in ('r','p')`).** G7 query widened: `relkind in ('r','p')` instead of `'r'` alone, catching partitioned parents. Auto-policy loop in 13B (Option C) uses the same widened filter. doc 40 Part F row G7 prose updated to reflect relkind change. Gated policies applied to seven tables (billing.invoices, tms.delivery_tasks, wms.outbound_orders, wms.occupancy_snapshots, wms.space_allocations, wms.space_reservations, wms.work_orders).
- **Tests — 43/43 on a freshly recreated database, twice.** The three former `it.fails` are now requirement tests; Option B is proven independently of Option C by constructing the dual-role user under `session_replication_role = replica` (disables origin triggers, cannot disable RLS); two Option C tests expect SQLSTATE 23514 on the front-door path; SCR-RLS-02 asserts the widened G7 = 0, zero audit rows with non-null `entity_id` through the parent in a portal context, `relrowsecurity` + `entity_scope` on the parent, and `verify_audit_chain()` = 0 after the fixture insert. Positive controls so the gate cannot silently over-deny: the dual-role user's own client-A rows still return 1 on all three tables; an internal user holding a real `identity.user_entities` row still sees another client's invoice via `entity_scope`. After review: seven `pg_policy` shape proofs (one per rewritten table — `entity_scope` permissive/`*`/exact Option B predicate, `client_portal_scope` unchanged) so the four tables without fixture rows are proven by shape (29 → 43).
- **Carried forward (eight items, none block 0.18 DONE; do not re-open here).** (1) `entity_scope` is `FOR ALL`/`USING`-only — governs INSERT — under non-superuser runtime a portal user's audited action is rejected, every internal reader/writer must pass `isInternal: true` → 0.5/0.6 role design (SCR-RLS-01 §6, SCR-RLS-02 §7, D-002). No legitimate caller affected today: client portal reads via `client_portal_scope`/`sku_client_scope`; 13B ق-9 gives housing/partners/hr no client-facing policy so drivers/partners/imile already `is_internal = true`. (2) Option B NOT extended to `wms.inbound_orders` / `cc.tickets` / 13-Schema-Additions.sql:702-708 (no client policy today; client policy added later re-opens silently). (3) No guard detects SCR-RLS-01 permissive-composition class; SCR §1 enumeration query is candidate; new guard requires G-01. (4) `identity.users.user_type` has no CHECK, so Option C only binds literal `'client'`. (5) Audit fixture row deleted only while chain tail; concurrent writer leaves permanent `_rls_isolation_fixture` row (G8-safe). (6) 0.17 `identity.users` fixture leftover — 0 on the current recreated database, recurs whenever the 0.17 suite runs. (7) Runtime connects as superuser (0.5/0.6 work). (8) Turbo `test` task caches DB-touching tests, unhashed on `PG*` (gates ②/③ replay stale green).
- **Review — pg-reviewer (opus) FAIL(12) → PASS.** Reviewed the 29/29 suite + migration + guard/loop/doc 40 edits. Fixed in this commit: (1) state/backlog/CHANGELOG absent from the change set; (2) stale "RED today / not yet applied / does not exist yet" prose across test and feature — the same finding class as phase 1 round 3, recurring; (3) the migration header claimed idempotency but the Option B precondition rejected the migration's own output, so 0003 was NOT re-runnable while `apply.sh` re-runs every migration under `ON_ERROR_STOP=1` with no applied-migrations table — disproved live by the reviewer; now accepts the gated form and skips (applied 4× on the live database, then once on a recreated one); (4) only 3 of the 7 rewritten tables asserted → seven `pg_policy` shape proofs; (9) trigger messages now name the permitted remedy; (10) stale SCR cross-references. Recorded as carried-forward, not fixed — each needs its own G-01 or belongs to the 0.5/0.6 role design: (5) Option B makes `is_internal` load-bearing for internal reads and writes; (6) Option B not extended to the non-vulnerable tables; (7) no guard for the permissive-composition class; (8) `user_type` has no CHECK; (12) the audit fixture row survives teardown if a concurrent writer appends after it. (11) the trailer below. Verdict on DONE: both halves of the doc 38 acceptance hold and were witnessed; the INSERT consequence does not force a partial close.
- Model: fable (claude-fable-5-1 — the session model the GM set; not one of the four listed tier aliases, reported verbatim per `docs/MODEL_ROUTING.md`, never silently mapped. MODEL_ROUTING §4 routes security design to opus: both reviews of this migration ran on opus) · Delegated: pg-tester (sonnet, RED-first suite + 2 fix rounds), pg-reviewer (opus, 2 rounds this phase), pg-scribe (haiku, records + brief) · Review: PASS(17 findings — round 1: 12 (7 fixed, 5 recorded as carried-forward); round 2: 5 fixed) · tokens: ≈ 700k this phase (estimate: pg-tester ≈ 95k + 80k + 105k, pg-reviewer ≈ 160k + 210k, pg-scribe ≈ 70k, Master ≈ 150k).

---

## 0.18 — RLS client-isolation test: suite delivered, schema gaps discovered (2026-09-23)

- **Delivered (doc 38/doc 40 Part F G14, partial).** New workspace `tests/isolation` (`@pg-eos/isolation-tests`): Gherkin `client-isolation.feature` written first, then `tests/client-isolation.test.ts` — 21/24 test scenarios pass (3 pinned FAILS deliberate, see below). Runner `pnpm test:isolation` wired into `scripts/guards-run.sh` as a blocking guard with positive-proof exit-code check. Turbo task `test:isolation` with `dependsOn: ["^build"]` and `globalPassThroughEnv: PGHOST/PGPORT/PGDATABASE/PGUSER/PGPASSWORD` (turbo's strict env was stripping PG*, caught in review and fixed). Root `package.json` script added. eslint.config.mjs: new `AGENT_CONSTRAINED` applied to `tests/**/*.{ts,tsx}` (withContext rule, no-console, ban-ts-comment all enforced over test code). No schema, no migration, no module, no endpoint, no UI.
- **RLS mechanism proven (G7 = 0, already; G14 suite run, new).** Verified against live database 2026-09-23: positive control on all five client-scoped tables (sales.accounts, wms.outbound_orders, tms.delivery_tasks, billing.invoices, wms.skus) — direct ID select by row owner returns exactly 1; same query with opposite client's ID returns 0 rows and no error (promise resolved). Symmetry tested on billing.invoices and wms.skus (roles swapped). Internal context (`isInternal: true`) sees both clients. Guard of the guard: current_user, usesuper, and rolbypassrls all asserted on first run. Suite: 21 passed | 3 expected fail (24 total), reproducible every run.
- **Mechanism (test fixture, non-schema).** The test provisions its own ephemeral NOBYPASSRLS role `pgeos_rls_isolation_test` (password `randomUUID()`, never committed) in `beforeAll`, connects through it via dynamic `import('@pg-eos/db')` and mutated PGUSER/PGPASSWORD env vars (ordering matters — the Pool is built at module load time). Session-level `pg_advisory_lock` serialises concurrent runs; prefix sweep in beforeAll cleans leftovers. Fixture data seeded via superuser client (need to bypass RLS), scoped by role in query, deleted in teardown via superuser (five tables, FK order: skus → invoices → delivery_tasks → outbound_orders → accounts).
- **Acceptance criterion DOES NOT hold in general — two G-01 schema-change requests discovered and reproduced.** (1) **SCR-RLS-01:** `entity_scope` (permissive, FOR ALL) OR-ed with `client_portal_scope` (permissive, FOR SELECT) lets a portal user holding an `identity.user_entities` row read another client's rows by direct ID substitution. Reproduced live: 1 row where criterion demands 0. **Seven tables** affected: billing.invoices, tms.delivery_tasks, wms.outbound_orders, wms.occupancy_snapshots, wms.space_allocations, wms.space_reservations, wms.work_orders. Pinned by `it.fails(...)` plus a companion assertion on the three that have fixture rows (billing.invoices, tms.delivery_tasks, wms.outbound_orders); fixing SCR-RLS-01 will make `pnpm guards:run` exit 1 until that block is removed — deliberate. Non-vacuity of the whole suite proven out-of-band: the same tampering query as a NOBYPASSRLS role → 0 rows, as a BYPASSRLS role → 1 row, as superuser `postgres` → 1 row. Gate proven end-to-end: with `billing.invoices`' `client_portal_scope` temporarily widened to `true` on the live database, G7 stayed green (it only checks `relrowsecurity`) while G14 went RED and `guards:run` exited 1; policy restored verbatim from `01-Data-Model.sql:1471-1474`. (2) **SCR-RLS-02:** `platform.audit_log` partitioned parent has `relkind='p'`, `relrowsecurity=false`, zero policies. Its five partitions all have RLS. A NOBYPASSRLS role with `allowed_entities()={}` and `app.is_internal=false` holding only SELECT on parent reads all 8 seeded rows (all non-null entity_id). G7's `relkind='r'` filter does not see the parent, so `G7=0` does not cover it. Not pinned by test — deliberate (a GM decision point, reasoned in section 6 of `docs/notes/SCR-RLS-02-audit-log-partitioned-parent-has-no-rls.md`).
- **Review history — three pg-reviewer rounds, 24 findings, all fixed; two regressions introduced by fixes and caught in review.** No pg-backend was involved: the schema is already delivered, so this slice is test-only (pg-tester) plus Master-direct edits. **Round 1** (pg-tester built the suite; 18/18 green): pg-reviewer **FAIL(11)** — the decisive one was that both portal contexts used `userId: null`, so the suite proved isolation only under the precondition `allowed_entities() = {}` and could not see the `entity_scope` OR-leg. The Master reproduced that leak live (1 row) and filed SCR-RLS-01. Other round-1 findings: superuser fixture client silently inherited the role's password; teardown not crash-safe (confirmed in practice — turbo killed the suite mid-`pnpm test` and leaked 2 `sales.accounts` rows); G14 not wired into `guards:run`; the suite enrolled in `turbo run test` and racing sibling packages on the same database; eslint comment overstated; brief not amended; env leak to a future sibling file; `firstRow` error message. **Round 2** (pg-tester fixed all eleven): pg-reviewer **FAIL(9)** — SCR-RLS-01 understated the blast radius (seven tables, not three — verified from live `pg_policy`); Option A's consequence stated in the flattering direction; `it.fails` records green for the wrong reasons too; renaming the package script `test` → `test:isolation` dropped `@pg-eos/db`'s `^build`; the sweep made concurrent runs mutually destructive; the SCR pin covered one table; the `platform.audit_log` partitioned parent was being dropped with a bullet — the Master then reproduced an actual read bypass (all 8 rows, all with non-null `entity_id`) and filed SCR-RLS-02; `apply.sh:207` now false (frozen path, carried forward); brief still unamended. **Round 3** (pg-tester: companion assertions, `describe.each` over three tables, advisory lock, turbo task with `^build`; Master: SCR rewrites, brief amendment): the `^build` fix routed G14 through turbo and **introduced two regressions**, one caught by the Master before review, one by pg-reviewer — (a) the task became cacheable: consecutive `pnpm test:isolation` runs replayed a green in ~55 ms with `cache hit` and no database connection → `"cache": false`; (b) turbo's default strict env-mode stripped `PG*`, so `PGPORT=59999 pnpm test:isolation` exited 0 against the local default database → top-level `globalPassThroughEnv`. Note this does **not** fix the pre-existing `test` task: `passThroughEnv` is deliberately excluded from turbo's cache hash, so `PGPORT=59999 pnpm test` still exits 0 via a 9/9 cache replay (`FULL TURBO`, 53 ms) and only `--force` exposes it. Carried forward. pg-reviewer **FAIL(3)**: the env-stripping above, the feature file still asserting the disproved claim, and a latent silent-green (`turbo run --filter` exits 0 when the package exists but the script is gone) → `guards-run.sh` now requires a vitest result line in the captured output. All three fixed and re-probed by the Master (`PGPORT=59999` → exit 1; script renamed → `G14 1 RED`). Final: **PASS(24 findings fixed)**. **Lesson:** a guard must be probed with a deliberately broken environment before it is trusted; exit 0 is not evidence that it ran.
- **Verification, independently re-run by Master and pg-reviewer.** `pnpm lint` clean. `pnpm typecheck` 10/10. `bash scripts/check-boundaries.sh` → BOUNDARIES ENFORCED (A–F). `pnpm guards:run` G1–G14/G18 GREEN with positive exit code check (G15–G17 not yet scheduled). `pnpm test:isolation` 21 passed | 3 expected fail | 24 total. Residue: pgeos_rls_isolation_test role dropped, advisory locks released, sales.accounts 0, billing.invoices 0, identity.users 1 (pre-existing 0.17 fixture leftover, noted).
- **Title "RLS enabled on every operational table" not yet literally true.** Row's own acceptance criterion requires `G7 = 0` (already true) AND the isolation test passing cleanly. Test passes functionally (21/24) but acceptance blocked by two reproduced schema gaps requiring GM decision. Per G-01 this slice changed no schema. `0.18 closes NOT DONE / BLOCKED`. Carried forward (do not fix here): SCR-RLS-01, SCR-RLS-02, apply.sh line 207 comment update (frozen path), identity.users 0.17 fixture, runtime superuser connection (0.5/0.6 work).
- Model: opus (Master — escalated per MODEL_ROUTING: the task failed review twice and produced two security SCRs) · Delegated: pg-tester (sonnet, build + 2 fix rounds), pg-reviewer (opus, 4 rounds), pg-scribe (haiku) · Review: PASS(24 findings fixed) · tokens: ≈ 900k (estimate: pg-tester ≈ 100k + 130k + 120k, pg-reviewer ≈ 95k + 125k + 100k + 35k, pg-scribe ≈ 70k, Master ≈ 230k).

---

## Housekeeping — WBS 0.9 abandoned-WIP catalogue + WBS 0.18 pre-flight verification (2026-09-23)

- **Not a WBS slice** — a GM-requested bookkeeping task. No code, no schema, no migration, no test changed.
- **`docs/notes/0.9-abandoned-wip.md` added.** Catalogues the two unmerged branches that attacked WBS 0.9 in parallel on 2026-09-22 before it was closed at `aa46787`: `claude/postgresql-16-254336` @ `0217e85` (3 pg-reviewer rounds, round-3 fix never re-verified) and `claude/resume-4c8ecc` @ `f127ab2` (pg-reviewer FAIL, 12 findings, 0 fixed). Both fork from `a8f4899`, which is on `main`; both verified **unmerged** via `git merge-base --is-ancestor`. The note records each branch's name, tip hash, authored time, review state, why it was abandoned, its exact `--name-status` file list, seven ranked reuse candidates, and five hazards. **Neither branch is merged or deleted; WBS 0.9 stays closed at `aa46787`.**
- **Gap the note makes explicit:** doc 40 Part F's *governing* form of G13 (`pgbench -c 100 -t 1 -f tests/guards/next_doc_no.sql` then `select count(distinct doc_no) from platform.doc_no_probe`) exists on neither branch's merged descendant. On `main` today `platform.doc_no_probe` does not exist in `01 · 13 · 13B · 019`, `tests/guards/` does not exist, and no `.feature` file exists for 0.9. `guards.sql` satisfies G13 in a pure-SQL single-connection form (its own header comment says so); `aa46787` proves real concurrency from Node. Whether to close the gap is a Master/GM call — the note only records that the work exists and where.
- **WBS 0.18 pre-flight verification (the second half of the request).** The 0.18 test **does not exist yet** — confirmed by `git ls-tree` across every local branch, `git worktree list` across all four worktrees, and a filesystem `find`; there is nothing to audit. What was verified instead, as preconditions: (a) `withContext()` is exported from `packages/db` via `packages/db/index.ts` and sets all three GUCs (`app.user_id`, `app.client_id`, `app.is_internal`) that `platform.current_user_id()` / `current_client_id()` / `is_internal()` read back, so 0.18 can and must use it; (b) all five client-scoped tables the isolation test needs (`wms.outbound_orders`, `tms.delivery_tasks`, `billing.invoices`, `sales.accounts` via `client_portal_scope`, and `wms.skus` via `sku_client_scope`) are defined in `database/schema/01-Data-Model.sql` — **nothing outside `01 · 13 · 13B · 019` is required**; (c) their RLS policies already exist and G7 already returns 0, so **no new migration is required** — the next free migration number stays `0003`.
- **Two real blockers found and recorded in `PROJECT_STATE.md` and `MASTER_BACKLOG.md` row 0.18.** (1) `packages/db/src/client.ts` defaults `PGUSER` to `postgres`, a Postgres **superuser, which bypasses RLS unconditionally regardless of any GUC** — and `grep -rin "create role"` over `database/schema/**` and `database/migrations/**` returns **nothing**, confirming the gap WBS 0.11 documented is still open. A non-superuser `NOBYPASSRLS` application role is therefore a **G-01 schema-change prerequisite** for 0.18; without it the isolation test would "pass" for the wrong reason. (2) G14's runner `pnpm test:isolation` is named in doc 40 Part F (line 644), `scripts/guards-run.sh` (lines 15, 84) and `database/schema/apply.sh` (line 207), but is **defined in no `package.json` in the repo**.
- **Correction to prior state:** `PROJECT_STATE.md` described 0.18 as "fully buildable now". That was wrong on blocker (1) and is corrected in this commit.
- Model: opus (Master, direct — no delegation; a documentation/verification task with no build step) · Delegated: none · Review: n/a (no code) · tokens: ≈ 60k (estimate).

---

## 0.16 — Column sensitivity classification: `identity.column_classification` + deploy guard (2026-09-22)

- **Delivered (doc 38 acceptance: "Unclassified column fails deploy and G6 returns 0 on the applied schema; every later migration classifies its own columns").** `database/migrations/0002_M_classify-columns.sql` — the SECOND forward-only migration ever merged in this repo (after WBS 0.11's `0001`). Classifies all 2,605 columns across the 14 business schemas (`platform, identity, catalog, sales, wms, tms, cc, billing, hr, partners, admin, housing, imile, governance`) into `identity.column_classification`'s five sensitivity categories (`public · personal · payroll · commercial · secret`) via a bulk, rule-based classifier: a priority-ordered regex `case` expression for generalizable patterns, plus a documented, table-qualified `values`-list override for the genuinely ambiguous cases a regex cannot safely generalize (e.g., person names under generic column names like `name_ar` that would otherwise sweep up company names; identity-document numbers under non-obvious names; jsonb snapshot columns embedding already-classified figures). Final distribution: `public: 2331 · commercial: 174 · personal: 64 · payroll: 33 · secret: 3` (2,605 total). Guard G6 special-case "WARNING, non-blocking until WBS 0.16" carve-out REMOVED from `scripts/guards-run.sh` and `database/schema/apply.sh` — G6 is now a normal blocking guard exactly like G1-G5/G7-G13. Verified live: a real column deleted from `identity.column_classification`, then `scripts/guards-run.sh` spawned as a genuine subprocess, exits with code 1.
- **Test infrastructure added:** `modules/identity` gained test scaffolding (previously had none — same starting state `modules/platform` was in before WBS 0.9) matching the established `modules/platform` precedent exactly, plus `modules/identity/tests/integration/column-classification.test.ts` (15 tests): completeness (G6 = 0), classification-correctness spot-checks via live pattern discovery, a destructive-but-reversible proof that the guard detects a real gap, the real subprocess-exit-code-1 proof, a hardcoded named allow-list proving specific known-sensitive columns are never `public`, and a "no invented sensitivity value" check.
- **A significant discrepancy resolved (record explicitly):** `database/schema/guards.sql`'s raw G6 query (doc-40 Part F, governing) counts 2,605 columns with no relation-kind filter (includes VIEW columns). `identity.unclassified_columns` helper view (added in 13B) additionally filters to base tables only (`relkind = 'r'`), returning 2,404 — 201 fewer. **Decision: classify against the stricter, doc-40-governing 2,605-row definition** — a superset that automatically satisfies the narrower 2,404-row view too, with no ambiguity either way.
- **Review history — three rounds, the second and third genuinely consequential (record this prominently):** Round 1: pg-tester wrote RED tests (the classification table starts empty, G6 = 2,605); pg-backend built the first classifier version. First pg-reviewer pass: **FAIL(11)**. Critically, **7 of those findings were REAL, LIVE-QUERIED compliance gaps in the classification VALUES themselves** — not code defects, but actual sensitivity misclassifications with audit-log exposure. Direct queries found: real invoice/ledger totals (`billing.invoices.total`, journal debits/credits) classified `public`; a partner's IBAN (`partners.partners.bank_iban`) classified `public`; national identity-document numbers under non-obvious column names (`hr.employee_documents.doc_no`, `tms.proof_of_delivery.receiver_id_no`) classified `public`; ~15 columns naming an actual employee/contact/driver by name (`hr.employees.name_ar`, `identity.users.full_name_ar`, etc.) classified `public` while `witness_name` on a different table was correctly `personal` — an internally self-contradictory result; jsonb "snapshot" columns embedding already-sensitive commission figures classified `public`, bypassing `sanitize_audit`'s per-key redaction entirely; an individual's compensation/liability outside the exact keyword list falling to the less-precise `commercial` bucket instead of `payroll`; geographic reference data (`imile.coverage_areas.governorate`) over-classified `personal`. **Impact:** `identity.column_classification.sensitivity` feeds directly into `packages/db`'s `sanitize_audit()` function (13B:297-305), which redacts a column's value in `platform.audit_log` based on this exact classification — a column wrongly left `public` means its real value gets written UNREDACTED into the audit log. Round 2: pg-backend rewrote the classifier substantially (broadened regex for genuinely generalizable patterns; new, explicitly-documented table-qualified override mechanism for cases a regex cannot safely generalize) — and, notably, CAUGHT AND FIXED three new false positives its own broadening would have introduced before finalizing (e.g., naive `total` substring match would have swept up unrelated columns like `total_beds`/`current_step`). pg-tester added subprocess-exit-code proof and the named regression-guard allow-list in parallel. Second pg-reviewer pass, after independently re-querying all ~41 originally-flagged columns plus an exhaustive sweep: **FAIL(2)** — two more real gaps found (`wms.outbound_orders.ship_to_phone` left `public` while siblings on the same row were already `personal`; `cc.calls.caller_number`/`.called_number`, external callers' phone numbers). Round 3 (Master, direct — two one-line table-qualified override additions, no regex change, explicitly confirmed by the reviewer as introducing no new risk): fixed. Final: **PASS**.
- **Verification, independently re-run by the Master (not just agents' word).** `pnpm --filter @pg-eos/identity test` — 15 tests, green, run repeatedly. `pnpm --filter @pg-eos/identity typecheck` — clean. `pnpm eslint modules/identity` — clean. `bash scripts/check-boundaries.sh` — BOUNDARIES ENFORCED (A-F). `bash scripts/guards-run.sh` — G1-G13, G18, G-SEED all green; **G6 now shown as a normal blocking guard, no WARNING text**. A full `apply.sh --recreate` (multiple times across the slice's rounds) confirmed the classification survives a clean recreate from scratch every time — 2,605/2,605 classified, G6 = 0. Re-ran every prior task's test suite after each round of changes (platform, db, events, documents) — all stayed green throughout, zero regression.
- Model: sonnet (Master) · Delegated: pg-tester (sonnet), pg-backend (sonnet), pg-reviewer (opus) · Review: PASS(11 findings fixed round 1-2 code/test, 2 findings fixed round 3) · tokens: ≈ 700k (estimate).

---

## 0.15 — Document engine: templates, bindings, Chromium PDF, bilingual RTL (2026-09-22)

- **Delivered (doc 38 acceptance: "Arabic/English PDF renders correctly; entity header auto-applied").** New package `packages/documents` — `renderDocument(pool, { templateId, entityId, data })` reads a `platform.document_templates` row and target `platform.entities` row, compiles the template's Handlebars `body_html` with a context merging the entity's header fields (`nameAr`, `nameEn`, `legalNameAr`, etc.) and caller-supplied `data`, composes a full HTML document with an auto-applied entity header/footer wrapper (RTL default, Cairo font embedded offline via `@fontsource/cairo` as base64 data URIs — no CDN/network dependency), and renders to PDF via a shared, lazily-launched headless Chromium instance (`puppeteer`). Rejects when an entity-scoped template (`document_templates.entity_id` set) is used for a different entity; validates `page_size`/`orientation` against a known set and throws clear error for invalid values (these columns are plain `text`, no DB CHECK constraint yet — application-level invariant).
- **Scope decision (recorded explicitly — this slice's deliverable vs. future binding/numbering/recording).** Built the RENDERING ENGINE ITSELF (template + entity + document-specific data → bilingual PDF with entity header/footer auto-composed), matching doc-38 criterion exactly. Deliberately did NOT build: `platform.document_bindings`' auto-generate-on-state-transition wiring, `platform.documents` row recording, doc-number allocation via `platform.next_doc_no`, checksum computation, or file storage. Those belong to a future, real business module (or the golden slice 2.9 itself) that actually consumes the engine — same class of scope boundary WBS 0.11 drew around `withContext` and WBS 0.12 drew around `relayOnce`. Task backlog row 0.15 title mentions "bindings" in its prose description; that's the full future scope, not this slice's delivered scope.
- **Slice history: two review rounds, one genuinely difficult plus a significant, separate finding.** Round 1: pg-tester wrote genuine RED tests against a non-existent package; pg-backend built it. pg-reviewer FAIL(13): (a) correctness defects — `getBrowser()` launch-failure had no retry path, wedging the package permanently; caller-supplied `data` could silently shadow the real entity object in Handlebars context; `page_size`/`orientation` invalid values silently misinterpreted instead of rejected; inaccurate Letter/Legal page dimensions; entity-scoped template mismatch guard's ALLOW direction completely untested; vacuous RTL-content assertion would pass even on broken render. Round 2: pg-backend + pg-tester in parallel fixed all defects: launch retry on failure, context merging isolation, page-geometry validation with clear error messages (inline all allowed values), corrected Letter (8.5×11″)/Legal (8.5×14″) dimensions in pts, comprehensive mismatch-guard test proving both ALLOW and REJECT paths, RTL assertion fixed to confirm the confirmed, provable single-run character-reversal pdf-parse actually produces. pg-reviewer re-review: **FAIL(5)** — five smaller findings (a `--help` regression Master had introduced with an earlier doc edit, two documentation notes folded into this entry, one new test-coverage gap for page-geometry validation, the close-out commit itself). Final: **PASS(16 findings fixed total across both rounds)**.
- **Separate, significant finding discovered mid-slice — record prominently, affects whole project.** While building 0.15's entity-header test, the Master discovered `database/schema/apply.sh` was silently corrupting multi-byte UTF-8 text on `apply.sh --recreate` — every seeded entity's Arabic name was affected (`platform.entities.name_ar` for `code='PCC'`, correctly `بريميوم CC` in the frozen source file, came out as `ط¨ط±ظٹظ…ظٹظˆظ… CC` after applying — a genuine byte-level UTF-8 double-encoding, confirmed via direct `pg`-client byte inspection, not a terminal-display artifact). **Root cause NOT fully isolated** — reproduced 7 times across the session on this machine; an independent pg-reviewer session on the same machine could not reproduce it; forcing console codepage to UTF-8 (65001) did not fix it either, ruling out the Master's initial codepage theory. **Leading working hypothesis (unconfirmed):** this machine has two PostgreSQL installations (16 and 17) and which `psql.exe` gets resolved may differ per session/PATH state. The fix: every `psql ... -f <largefile>` invocation (3 sites in `apply.sh`, 1 in `scripts/guards-run.sh`) changed to use stdin redirection (`psql < file`) instead. **Defensive, verified-safe measure:** zero functional regression — all of WBS 0.9-0.12's test suites and 0.15's own re-ran and stayed green; all guards stayed green. Documented honestly in `apply.sh`'s header comment (explicitly states the unresolved uncertainty and the contradicting review finding) — do not overstate certainty about the mechanism in the CHANGELOG. Trade-off accepted: `psql` error output under stdin redirection loses the file name and line number it would show under `-f`; `apply.sh` header comment now documents this and suggests running `psql -f <file>` by hand for diagnosis only if a specific migration/schema file's exact failing line needs to be found. All five seeded entities' Arabic names verified correct post-fix via direct `pg`-client queries (PCC/PDL/PGH/POR/PST).
- **Two CHANGELOG-only documentation notes from review (record as bullets, no code needed).** (1) Puppeteer's Chromium binary (~200MB, downloaded to `~/.cache/puppeteer` on `pnpm install`) has real deployment-time implications for the Tier 0 Docker image (doc 42) — headless Chromium typically needs host shared libraries (`libnss3`, `libatk-1.0`, `libgbm1`, fonts, etc.) that neither `docker-compose.yml` nor any Dockerfile currently provides. This is an infra-track concern for whoever builds the production image, not something this rendering-engine slice needed to solve — noted so it isn't silently discovered later. (2) `platform.document_templates.page_size`/`orientation` have no DB CHECK constraint; the invariant is enforced only in `packages/documents/src/render.ts`'s `assertValidPageGeometry()`. A future schema-change request could add a CHECK constraint at the database layer too (belt-and-suspenders), but that touches the frozen 01-Data-Model.sql baseline and is out of scope here.
- **Verification, independently re-run by the Master (not just agents' word).** `pnpm --filter @pg-eos/documents test` — 6 tests, green, run multiple times. `pnpm --filter @pg-eos/documents typecheck` — clean. `pnpm eslint packages/documents` — clean. `bash scripts/check-boundaries.sh` — BOUNDARIES ENFORCED (A–F). `bash scripts/guards-run.sh` — G1–G13, G18, G-SEED all green (G6 = 2,605 unclassified columns, pre-existing, non-blocking per WBS 0.16 — this slice adds zero columns). Re-ran WBS 0.9/0.10/0.11/0.12's own test suites after the apply.sh UTF-8 fix — all still green (5/5, unchanged; 11/11; 8/8) — no regression. A full `apply.sh --recreate` with the fix confirmed all five seeded entities' Arabic names now correct (PCC/PDL/PGH/POR/PST), verified via direct `pg`-client queries.
- Model: sonnet (Master) · Delegated: pg-tester (sonnet), pg-backend (sonnet), pg-reviewer (opus) · Review: PASS(16 findings fixed) · tokens: ≈ 673k (estimate — pg-tester RED ≈92k, pg-backend build ≈115k, pg-reviewer pass 1 ≈95k, pg-backend round 2 ≈43k, pg-tester round 2 ≈33k, pg-reviewer pass 2 ≈76k, pg-tester round 3 ≈29k, Master direct work UTF-8 investigation+fix ≈80k, Master session ≈110k).

---

## 0.12 — `packages/events`: outbox relay + subscriber registry (2026-09-22)

- **Delivered (doc 38 acceptance: "Event written in same tx as state; relay at-least-once; subscriber idempotent").** New package `packages/events` — (1) `writeOutboxEvent(tx, input)` writes a `platform.outbox` row using the ambient transaction scoped to the same `drizzle` handle `withContext` (from 0.11) passes to its callback, so rows commit or roll back with the caller's work, never a third case. (2) `registerSubscriber(name, handler)` + `clearSubscribers()` (test-only, not exported) + `listSubscribersWithNames()` — an in-memory registry. (3) `relayOnce(pool, opts?)` — polls unpublished `platform.outbox` rows (optionally filtered by `opts.eventType`), invokes every registered subscriber for each row, marks a row published only if ALL subscribers succeed; a row whose subscriber throws stays unpublished so the next `relayOnce` call redelivers it — this redelivery IS the at-least-once mechanism. (4) `packages/events/catalog.ts` — the event-name registry (CLAUDE.md's PARALLEL LANES section references it as frozen for cross-module event consumption); starts empty by design (no module has shipped real domain events yet), grows as future slices ship their domain-event publishers.
- **Slice history: exceptionally deep, three full review passes, mirroring 0.11's depth.** Round 1: pg-tester wrote genuine RED tests against a non-existent package; pg-backend built it to green. pg-reviewer FAIL(16): (a) correctness blocker — `relayOnce` with zero registered subscribers silently marked every pending row published without delivering it (permanent, silent event loss); (b) correctness blocker — the SELECT was completely unscoped, a "limit respected" test swept up pre-existing shared-database rows and destructively marked them published (data loss); plus 14 more (process-global `pg.types.setTypeParser` mutating on test-local int8-string problem; false architectural parity claim with 0.11's `withContext` exemption; `writeOutboxEvent` imposing undocumented G9 obligation; dangerous test-only export on public barrel; missing `last_error` subscriber attribution; no attempt ceiling/dead-letter documentation; mid-batch subscriber re-read; missing `catalog.ts`; concurrency-safety gap). Round 2: pg-backend + pg-tester in parallel — fixed the two blocking bugs (early return on zero subscribers; added `opts.eventType` filter to scope `relayOnce`), replaced global type-parser mutation with local `id::text` casts, documented the RLS/superuser opposite-direction gap explicitly, documented G9 obligation, removed test-only export from public barrel, added subscriber-name attribution to `last_error`, documented dead-letter/attempt-ceiling limitation, fixed mid-batch re-read, created `catalog.ts`, documented concurrency-safety gap. Master fixed one latent regression (one-line `Number()` cast in `outbox-write.test.ts`) and re-enabled `fileParallelism` in test config. pg-reviewer re-review: **FAIL(5)** — genuinely new finding: test suite's `event_type` strings were hardcoded (not unique per RUN), live exploit reproduced — a concurrent worktree's pre-seeded row with the same string was destructively deleted by cleanup, causing false assertion failure in another test. Plus 4 minor documentation-accuracy findings. Round 3: Master, direct — suffixed all 6 hardcoded `event_type` constants with `randomUUID()` per test run (session-unique); changed one cleanup from delete-by-event_type to delete-by-id (stricter idiom); removed the now-dead `listSubscribers()` export; corrected two inaccurate code comments. pg-reviewer's final pass: **PASS** — reproduced the original exploit against the fixed code (seeded 6 foreign rows with the old unsuffixed strings, ran suite 20 times: 1 warm-up + 10 sequential + 9 concurrent simulating the 3-lane maximum, all 8/8 green every time, all 6 foreign rows untouched afterward).
- **Deliberately deferred, documented, not fixed — record explicitly.** (1) `relayOnce` has no locking (`FOR UPDATE SKIP LOCKED`) and is not safe against concurrent instances of itself — documented in `relay.ts`'s header. (2) The RLS/superuser gap: `relayOnce` sets no GUCs and "works" today only because the local dev connection is superuser bypassing RLS entirely (same underlying gap 0.11 documented) — but for `platform.outbox` specifically, closing that gap will make `relayOnce` silently stop relaying business (non-`platform.`/`identity.`) events entirely, since a background process has no natural `current_user_id()` to satisfy `entity_scope` RLS. A background-worker/system-role RLS access strategy is a future G-01 schema-change request, not invented here. (3) No attempt ceiling or dead-letter policy — `attempts` is tracked but never capped; a permanently-failing row can head-of-line-block a batch (sorted first by id) — documented as a known limitation. (4) `packages/events/catalog.ts` starts empty by design — grows as future slices ship real domain events.
- **Verification, independently re-run by the Master (not just agents' word).** `pnpm --filter @pg-eos/events test` — 3 files, 8 tests, green, run repeatedly (Master ran 3 times directly; pg-reviewer's final pass ran it 20 times total including 9 concurrent instances simulating the 3-lane maximum, all green, zero residue). `pnpm --filter @pg-eos/events typecheck` — clean. `pnpm eslint packages/events` — clean. `bash scripts/check-boundaries.sh` — BOUNDARIES ENFORCED (A–F). `bash scripts/guards-run.sh` — G1–G13, G18, G-SEED all green (G6 = 2,605 unclassified columns, pre-existing, non-blocking per WBS 0.16 — this slice adds zero columns).
- Model: sonnet (Master) · Delegated: pg-tester (sonnet), pg-backend (sonnet), pg-reviewer (opus) · Review: PASS(20 findings fixed; 4 deliberately deferred — FOR UPDATE SKIP LOCKED, background-worker RLS role, dead-letter policy, catalog growth) · tokens: ≈ 725k (estimate — three full review rounds, two substantively reworked rounds, two correctness blockers, concurrent/determinism deep-fixes).

---

## 0.11 — `packages/db`: Drizzle + `withContext()` + lint rule (2026-09-22)

- **Delivered (doc 38 acceptance: "`db.` outside `withContext()` fails build").** New package `packages/db` — `withContext(ctx, fn)` opens a transaction on a pooled connection, sets three RLS session GUCs via parameterized `set_config()` calls (SQL-injection-safe per two explicit tests), runs the callback against a drizzle-orm node-postgres handle, commits/rolls back and releases correctly. Companion ESLint rule `packages/db/eslint-rules/no-db-outside-with-context.js` flags any direct call on `db` outside a `withContext()` callback — wired into root `eslint.config.mjs` and enforced by the build.
- **Slice history: three rounds across two review passes, plus a critical bugfix.** (Round 1) pg-tester wrote genuine RED tests (test-driven, unlike 0.9/0.10 which proved pre-existing schema); pg-backend built the package from scratch to green. pg-reviewer FAIL(11): 4 blocking code defects (security false-negative in lint rule callback exemption — removed; no test proved commit worked — added; "same pool" leak-check testing different pool — fixed; unguarded raw `pool` export — removed) plus 7 robustness findings (rule tracking gaps — scoped to AST; rollback-error masking — nested try/catch; default `PGUSER` superuser bypass — documented as known/deferred; missing pool error handler — added). (Round 2) pg-backend + pg-tester fixed all 11; pg-reviewer re-review: **PASS(0 findings)**. Committed as WIP checkpoint at `a192a72` (9 of 11 resolved, explicitly not-yet-DONE per user request).
- **Critical post-commit discovery: `is_internal()` RLS bug + blast radius.** Re-running full suite after `a192a72` surfaced NEW bug: `platform.is_internal()` threw `invalid input syntax for type boolean: ""` on pooled connections reused after `withContext` transaction. Root cause: Postgres custom GUCs set via `set_config(..., true)` revert to EMPTY STRING `''` on commit, not `NULL`. `platform.current_user_id()`/`platform.current_client_id()` already guarded against this with `nullif(..., '')` — `is_internal()` was missing it. **Blast radius: every RLS policy in 13B calling `is_internal()` (reference_read/reference_write/internal_only families) affected any reused pooled connection — fail-closed, never returned wrong true, but real availability bug.** Fix: NEW forward-only migration `0001_B_fix-is-internal-empty-guc.sql` adding the guard. **Second gap found alongside: `database/schema/apply.sh` only knew four baseline files, so migrations would never run on fresh environment/CI recreate.** Fixed by extending `apply.sh` to apply every `database/migrations/*.sql` (sorted numerically) after baseline. Escalated to opus per CLAUDE.md security/RLS escalation. (Round 2 on this fix) pg-reviewer FAIL(4) on test ordering accident and missing reused-connection scenario — both fixed (order-independent tests via Pool's 'connect' event for physical-connection verification; explicit `withContext` call matched by `pg_backend_pid`). pg-reviewer re-review: **PASS(0 findings)**.
- **Deferred known gap, documented explicitly.** Default `PGUSER` (`postgres`) is Postgres superuser and unconditionally bypasses RLS regardless of the GUCs `withContext` sets. No non-superuser application role exists in schema (verified by grep — zero `create role` in `database/schema/`). Creating one is future G-01 schema-change request, not invented in this slice. Code comment in `packages/db/src/client.ts` documents this.
- **Migration issued:** `0001` (lane B) — `database/migrations/0001_B_fix-is-internal-empty-guc.sql` (applied 2026-09-22). FIRST migration ever in repo. Applied automatically by extended `apply.sh`.
- **Process note: WIP checkpoint.** Commit `a192a72` was explicit user-directed mid-rework checkpoint (user interrupted delegation, asked to commit as-is). This close-out commit completes it.
- **Verification.** `pnpm --filter @pg-eos/db test` — 2 files/11 tests green twice. `pnpm eslint packages/db` clean. Boundaries ENFORCED (A–F). `bash scripts/guards-run.sh` — G1–G13, G18, G-SEED green (G6 = 2,605 unclassified, non-blocking per WBS 0.16). `apply.sh --recreate` succeeded live; migration 0001 applies automatically; `platform.is_internal()` is fixed version afterward.
- Model: sonnet (Master) · Delegated: pg-tester (sonnet), pg-backend (sonnet), pg-reviewer (opus), Master-on-opus (security/RLS escalation) · Review: PASS(11 findings fixed round 1, 4 findings fixed round 2 on is_internal/apply.sh bugs) · tokens: ≈ 763k (estimate — unusually large: full RED→GREEN slice + two review cycles + post-commit critical bugfix).

---

## 0.10 — `platform`: thresholds, feature flags, automation rules, decisions table (2026-09-22)

- **Delivered (doc 38 acceptance: "Threshold change takes effect without redeploy").** Two new integration tests prove the acceptance criterion against the live, already-applied `platform` schema (no new schema, no migration, no application code — `platform.thresholds`/`platform.automation_rules`/`platform.feature_flags`/`platform.decisions` are pre-existing/frozen in `database/schema/13B-Schema-Reference-Consolidation.sql`): (1) `modules/platform/tests/integration/thresholds-live-read.test.ts` (new) — proves a `platform.thresholds` value changed on one physical connection (`pool.connect()`) is immediately visible to an independent read on a second, distinct physical connection. Restores `value`, `unit`, `changed_by`, `changed_at` exactly, in try/finally, and verifies the restore. (2) `modules/platform/tests/integration/automation-rules-threshold-integrity.test.ts` (new) — read-only regression test proving every non-null `platform.automation_rules.threshold_key` names a real `platform.thresholds.key` (documented convention, not a DB foreign key — 13B:321), plus a negative-control test that inserts a throwaway row with bogus `threshold_key`, proves the query flags it, and deletes it in finally. Schema itself is pre-existing/frozen.
- **Two rounds: FAIL(9) → rework → PASS(0 open).** Round 1: pg-reviewer (opus) FAIL(9): findings 1/3/4/5 were code defects (vacuous negative-free integrity test; mis-cited doc source; incomplete restore missing `changed_by`/`changed_at`; false "separate client" claim when sequential `pool.query()` calls reuse one pooled connection). Round 2: pg-tester (sonnet) fixed all four (added genuine negative control; fixed citation to 13B:321; restored all four columns; switched to two explicit `pool.connect()` clients held simultaneously to force distinct connections). pg-reviewer's re-review: **PASS(0 open findings)**. Suite idempotent (2/2 both runs). `pnpm test` · `pnpm typecheck` · `pnpm eslint` clean. `bash scripts/check-boundaries.sh` → BOUNDARIES ENFORCED (A–F). Guards G1–G13 all green; G18 report-only.
- **Disposition of five non-code round-1 findings (recorded, not re-fixed).** Finding 2 (no DB-level FK enforcing `automation_rules.threshold_key` → `thresholds.key`) — **accepted, recorded, deferred.** Schema is frozen; adding a constraint is a G-01 schema-change request (out of scope). Interim mitigation: new negative-control regression test. Finding 9 (`platform.feature_flags`/`platform.decisions` not proven) — **accepted, recorded.** Single doc-38 acceptance criterion governs DEFINITION OF DONE (CLAUDE.md); both tables pre-existing and covered by guards G1–G13 (all green); no stated acceptance criterion requiring further proof — same precedent WBS 0.9 set. Findings 6 (guards unverified) — resolved: Master ran `bash scripts/guards-run.sh` — G1–G13, G18, G-SEED all green. Finding 7 (no commit yet) — resolved by this close-out. Finding 8 (`tasks/LANE_LOCKS.md` modified) — withdrawn by reviewer (Master's own lock-release step, not worker scope).
- **Three non-blocking observations (not blockers, not re-opened).** (1) A `changed_at` timestamptz/string round-trip could theoretically lose sub-millisecond precision (self-converging; no guard depends on it). (2) Two `.release()` calls in the shared `finally` could leak if the restore query rejects (only reachable if DB unreachable). (3) Stale code comment mentions a "restore fallback" path not actually implemented; the Arabic 13B:321 comment is paraphrased in English in a file citation (faithful, not verbatim).
- Model: sonnet (Master) · Delegated: pg-tester (sonnet), pg-reviewer (opus), pg-scribe (haiku) · Review: PASS(5 findings fixed) · tokens: ≈ 196k (estimate).

---

## 0.9 — `platform` schema atomic allocator + audit chain proof (2026-09-22)

- **Delivered (doc 38 acceptance: 100 concurrent `next_doc_no` calls → 100 unique numbers; `platform.verify_audit_chain()` returns zero rows and detects a deliberately tampered row).** `modules/platform/tests/integration/schema-invariants.test.ts` (new) proves WBS 0.9's acceptance criterion against the live, already-applied schema via (1) real `pg.Pool` (max 20) and `Promise.all` against the live `platform.next_doc_no()` allocator for the PCC/DOC/ALL counter — 100 concurrent calls yield 100 distinct, correctly formatted results (`PCC-DC-NNNNN`); (2) `platform.verify_audit_chain()` asserts whole-table zero rows before tampering, a deliberately tampered `row_hash` is detected and flagged, then the original hash is restored in a try/finally exception handler (proven via induced-failure probe). Also: `modules/platform/tsconfig.test.json`, `vitest.config.ts` (new); `modules/platform/package.json` (additive: `pg` dependency, `@types/pg`/`vitest`/`@types/node` devDependencies, `test` script); `pnpm-lock.yaml` updated. Schema itself is pre-existing/frozen in `database/schema/01` and `13B` — this slice's entire deliverable is the proof.
- **Three rounds: FAIL(2) → rework → FAIL(1) → rework → PASS(0).** Round 1: pg-tester (sonnet) wrote 2/2 green immediately (no RED phase — proving pre-existing schema). pg-reviewer (opus) FAIL(2): (1) tamper never reverted, permanently red-lining guard G8; (2) pre-tamper assertion scoped to fixture IDs instead of whole-table. Round 2: pg-tester added capture-restore logic with try/finally. pg-reviewer FAIL(1): restore wasn't exception-safe (no try/finally). Round 3: pg-tester wrapped tamper/detect in try/finally, proved via induced-failure probe the row was restored even on detection failure. pg-reviewer's final re-review: **PASS(0 findings)**. Suite ran twice (2/2 both runs). Guard G8 verified at five separate checkpoints (all 0). Counter advances exactly +100 per run. Typecheck/lint/boundaries clean.
- **Three non-blocking items noted (out of scope for this slice).** (1) Latent schema bug: `platform.next_doc_no()` uses `lpad(v_val::text, 5, '0')` — truncates silently once counter passes 99999, colliding future doc numbers (PCC/DOC/ALL counter now ~1300 post-testing, rises +100 per suite run; worth flagging before reachable). (2) `modules/platform/package.json` lists `pg` under `dependencies` not `devDependencies` — currently test-only; revisit when WBS 0.11 makes `packages/db` the real DB-access owner. (3) Whole-table `verify_audit_chain()` means concurrent suite runs against the same DB collide on fixture rows (not a defect, constraint of local/CI DB sharing).
- Model: sonnet (Master) · Delegated: pg-tester (sonnet), pg-reviewer (opus), pg-scribe (haiku) · Review: PASS(3 findings fixed) · tokens: ≈ 95k (estimate).

---

## ENV — psql 16 installed, local Postgres verified, briefs regenerated (2026-09-22, Master, no WBS ID)

- **Unblocks WBS 0.9.** Installed PostgreSQL client tools via `winget install PostgreSQL.PostgreSQL.17` with `--override "--mode unattended --unattendedmodeui minimal --disable-components server,pgAdmin,stackbuilder"` (command-line tools only, no server/pgAdmin) — discovered mid-install that PostgreSQL 16 (`psql 16.15`) was already present at `C:\Program Files\PostgreSQL\16\bin` and already on the machine's User PATH (not visible to the Bash tool's shell snapshot; worked around per-command with an explicit `PATH` export). The 17 install is redundant but harmless; 16 is the one actually used (matches the `postgres:16` Docker image exactly).
- **Started `infra/docker/docker-compose.yml`'s `postgres` service** (Docker Desktop's engine needed ~10s to come up after its processes were already running but the API pipe wasn't ready yet). Found and worked around a pre-existing bug: the `pgadmin` service is `profiles: ["tools"]`-gated and not started by `up -d postgres`, but Compose still validates its required `PGADMIN_PASSWORD` env var at interpolation time regardless of profile — worked around with a placeholder value in the shell, not fixed in the file (small, out of scope for this note).
- **Ran `database/schema/apply.sh --recreate`** — the first successful run on this machine. Zero errors, 175 tables / 14 schemas, `wms.verify_wh1()` 21/21 checks pass (3,330 locations exactly matching the seed's declared count), guards G1–G13 all return zero rows (G6 is non-blocking per CLAUDE.md until WBS 0.16 — 2,404 unclassified columns, expected), G13 100/100 unique `next_doc_no` calls, G18/G-SEED (report-only) zero. `PROJECT-SETUP-GUIDE §3`'s "175 tables" refers to the 15 module-owned logical tables (`gen-briefs.py`'s own count); raw `information_schema.tables` reports 191 because it counts each of `audit_log`'s 5 monthly/default partitions separately — not a discrepancy.
- **Regenerated all 15 `.claude/briefs/*.md`** via `python scripts/gen-briefs.py` against this machine's now-live schema (they had never been generated from a locally-applied database before). Found and recorded, not fixed: `platform.brief.md`'s `audit_log` row says "RLS OFF — G7 red", which is a generator false-positive — `audit_log` is a declaratively partitioned parent (`relkind='p'`) with no rows of its own; G7's guard filters `relkind='r'` and correctly checks only the 5 partitions, which all have RLS on. Worth a small `gen-briefs.py` fix later (exclude/annotate partitioned parents), not blocking.
- No code, schema, or test changes. Files touched: `docs/PROJECT_STATE.md`, `.claude/briefs/*.md` (15 files, regenerated).
- Model: sonnet (Master, direct — no delegation, this is environment/tooling verification, not a WBS slice) · tokens: ≈ 35k (estimate).

## 0.14 — `packages/domain-kit`: Money, Quantity, Clock, IdGenerator (2026-09-22)

- **Delivered (doc 38 acceptance: Domain tests deterministic across 1,000 runs).** Four domain modules: (1) `Money` and `Quantity` — fixed-point decimals backed by BigInt with scale 1000 (numeric(14,3): 11 integer + 3 decimal digits), all arithmetic operations range-checked on every result not just construction; Money currency locked to KWD, Quantity unit-agnostic; both implement add/subtract/multiply(scalar)/negate/compare/equals/isZero/isNegative/isPositive/toString. (2) `Clock` — interface `Clock.now(): Date` with `SystemClock` (the sole sanctioned caller of `new Date()`) and `FixedClock` test double (now()/advance(ms)/set(date)). (3) `IdGenerator` — interface `IdGenerator.next(): string` returning RFC4122 v4 UUID strings matching Postgres `gen_random_uuid()` format, with `UuidGenerator` (crypto.randomUUID()) and `SequentialIdGenerator` (deterministic seeded via crypto.createHash('sha256'), strictly increasing, test double). Acceptance verified: fast-check property tests with fixed seed (1_000_003) and `numRuns: 1000` on every invariant (commutativity, associativity, round-trip exactness, additive identity, negate involution, compare reflexivity/antisymmetry, closure under numeric(14,3) range, SequentialIdGenerator uniqueness/determinism/RFC4122 well-formedness); pg-reviewer ran suite twice with identical results.
- **Three rounds: RED → implementation → FAIL(4 findings) → rework → re-review → PASS.** Round 1: pg-tester (sonnet) wrote 86 RED property tests and enum tests. pg-backend (sonnet) implemented, 71/71 green. pg-reviewer (opus) FAIL(4): (1) rounding/malformed-scalar branches untested; (2) in-range operands could combine into out-of-range results — add/subtract/multiply/negate re-validated range via shared `fromResult` factory enforcing MAX_MAGNITUDE=99999999999999n; (3) RangeError messages omitted the allowed input shape — all rewritten to name numeric(14,3) constraints; (4) missing Model/Delegated trailers (escalated to Master). Round 2: pg-tester added 4 new RED tests for rounding/malformed paths. pg-backend implemented range checks. Six pre-existing law-testing properties hit genuine numeric(14,3) overflow at the fixed seed, now correctly rejected. Round 3: pg-tester added a second generator (5-digit integer cap) for those 6 law-testing bodies only, preserving the full-range generator for range-invariant tests. pg-reviewer re-review: **PASS(4 findings fixed)**. Suite 87/87 both runs; `tsc` clean; `eslint` clean; `bash scripts/check-boundaries.sh` → BOUNDARIES ENFORCED.
- **One pre-existing repo bug noted (out of scope).** `eslint.config.mjs` resolves `readdirSync('modules')` relative to cwd; fails when eslint invoked via `pnpm --filter <pkg> lint` from inside a package (reproducible identically for already-DONE 0.13, `packages/contracts`). Root-relative path would fix it; worth its own task later; `/` invocation unaffected.
- **Non-blocking nit (not a finding).** stale comment headers in money.test.ts:4 and quantity.test.ts:4 still describe tests as "expected to fail on import" (true only during round 1's RED phase). Cosmetic; left as-is.
- Model: sonnet (Master) · Delegated: pg-tester (sonnet), pg-backend (sonnet), pg-reviewer (opus), pg-scribe (haiku) · Review: PASS(4 findings fixed) · tokens: ≈ 240k (estimate).

## 0.13 — `packages/contracts`: Zod → OpenAPI generation (2026-09-21)

- **Delivered (doc 38 acceptance: OpenAPI spec generated; contract test harness runs).** `packages/contracts` is now the single source of truth for the HTTP surface. Exports (1) `ContractRegistry` to register Zod schemas and route contracts with invariant checks (D1–D3); (2) a minimal `Problem` error envelope (type, title, status, detail, instance) per RFC 9457 (D4) with the three mandated status codes (400, 409, 422); (3) `Idempotency-Key` header + shared parameter definitions; (4) `contract-test` harness for modules to import; (5) `openapi/openapi.json` committed with a drift test ensuring contracts never become stale (D5). Files sit at `packages/contracts/<area>/`, no `src/` (D6). Two rounds: pg-backend (sonnet) + pg-tester (sonnet) built to spec; pg-reviewer (opus) round-1 FAIL (24 findings) → rework + round-2 re-review PASS.
- **Master decisions D1–D6 recorded (doc 40 §A4 governing rules encoded).** (D1) `zod@^4.6.5` + `zod-openapi@^6.0.2` — purpose-built for Zod 4, emits OpenAPI 3.1, uses `.meta({id})` not prototype patching. (D2) `vitest@^4.1.11` not 5.x — vitest@5 requires Node ^22.12.0 | ^24.0.0 | >=26.0.0; this machine runs 25.2.1; vitest@4 accepts ^20 | ^22 | >=24 (same pattern as TS 5.9.3 ceiling, revisit on Node 26 LTS). (D3) `@seriousme/openapi-schema-validator@^2.10.0` (dev) — makes "OpenAPI spec generated" a real assertion against official 3.1 meta-schema. (D4) Error body = minimal Problem object (RFC 9457 shape) — shared envelope every module inherits. (D5) `openapi/openapi.json` committed with drift test — emitted document is published contract; committing enables review and consumer diffing; drift test stops staleness. (D6) No `src/` directory — files at `packages/contracts/<area>/` per doc 36 §1-2 layout.
- **Two additional Master decisions from round-2 re-review.** (a) `@types/node@^24.13.6` added to `packages/contracts/package.json` — the package's first `node:` import consumer; gap in Master's own 0.4 scaffold, not either worker's fault. (b) `fast-check@^4.10.2` added as a real property-testing dependency to close finding 19 — the Idempotency-Key invariant is genuinely property-tested, not just enumerated.
- **Finding 2 (module-contract wiring) recorded as a Master deferral, out of scope.** The brief's own text states "module contracts arrive with their own WBS tasks from 0.9 on"; no mechanism exists yet to test wiring against, so completeness/discoverability wiring is deferred to when the first module (0.9+) registers real routes and a test harness becomes available.
- **Findings 21 and 23 scoped out by Master decision.** Finding 21: `.gitattributes` LF pinning — no rule requires it; recommended for the day CI runs on Windows or a fresh clone, not before. Finding 23: the brief's own "Read ONLY" list template should include the package's own `package.json`/`tsconfig*` and dependency type declarations — recorded as a process note for future briefs, not a 0.13 defect.
- **Mid-slice data-loss-and-recovery incident (process risk escalated to PROJECT_STATE).** While pg-reviewer's round-2 re-review was in progress, a concurrent process (same git author identity, an unrelated "cleanup" run outside this session) committed `a901a04` directly to `main`, deleting tracked WBS 0.4 scaffold and every uncommitted 0.13 file. Recovered without loss: (1) `a901a04` reverted at `1bc09f7` to restore tracked files; (2) untracked files had been moved rather than deleted, to a sibling `_TO_DELETE/` folder outside this repo, from which round-2's source was restored byte-identical; (3) re-verified green and checkpointed at `50055f8`. **Flagged as a process risk for PROJECT_STATE's Blockers**: other concurrent sessions/processes sharing this working directory can destroy uncommitted work; commit more frequently on long slices as mitigation until root cause is found.
- **Reviewer's minor residual observations (not blockers, not re-opened).** (1) `registerSchema` writes to Zod's process-global registry rather than being truly instance-scoped — harmless today, `ProblemSchema` already self-tags via `.meta()`. (2) The contract-object freeze is one level deep — nested `responses[status].description` etc. remain mutable. (3) Three `.toThrow()` assertions in the tests lack a message-pattern matcher — recommendation only, not required for acceptance.
- **Tests:** `pnpm --filter @pg-eos/contracts test` → 5 files / 44 passing. `pnpm --filter @pg-eos/contracts typecheck` clean. `pnpm --filter @pg-eos/contracts build` clean. `pnpm --filter @pg-eos/contracts generate:openapi` succeeds with no stray output. `pnpm lint` clean. `bash scripts/check-boundaries.sh` → BOUNDARIES ENFORCED (A–F).
- Model: sonnet (pg-backend, pg-tester) · opus (pg-reviewer) · haiku (pg-scribe) · Delegated: pg-backend (sonnet) · pg-tester (sonnet) · pg-reviewer (opus) · pg-scribe (haiku) · Review: PASS(24 findings fixed: 21 fixed in code, finding 2 recorded as Master deferral, findings 21/23 scoped out by Master decision with reasons) · tokens: ≈ 280k (estimate).

## SCR-I18N-01 — Amharic (`am`) added as a sixth field-app language, in the governing package (2026-09-21)

- **Rule change, not a correction.** `42c72ba` added `am` to `CLAUDE.md`, the five `.claude/agents/`
  files and `docs/DECISION_LOG.md` — all of them **downstream** of the package — while every governing
  source still read `ar, en, hi, ur, bn`. The language list originates in the **doc 40 closing note**,
  rank **1** on the R-01 ladder; under R-01 the higher document governs and the lower one is the one
  that gets corrected, so a sixth language cannot be introduced by editing the files that cite it.
  The GM confirmed the directive is real, so it was raised as **SCR-I18N-01** under
  EXECUTION-MASTER-v4 §1.11 (G-01), recorded as **D-001**, and applied top-down.
  Full request and reasoning: `docs/notes/SCR-I18N-01-amharic.md`.
- **Seven findings from `42c72ba`, all closed.** (1) the citation did not support the change — doc 40
  said five; (2) `docs/DECISION_LOG.md:106` sits under `## Seed — EXECUTION-MASTER-v4 PART 1 (verbatim)`
  and was edited while its source `EXECUTION-MASTER-v4.md:168` was not, desynchronising a section whose
  contract is that it is verbatim; (3) the row then read "5 languages" beside a six-item list;
  (4) ten package locations were left at five; (5) no WBS ID and none of the `Model:` / `Delegated:` /
  `Review:` trailers; (6) it changed the frozen `CLAUDE.md` and `.claude/*` outside a single-lane Master
  task — this commit is that task; (7) `DEC-002` was a prose block with a prefix the log does not use,
  numbered after a `DEC-001` that never existed — replaced by the conforming row **D-001**.
- **16 lines across 12 files.** Governing: doc 40:714 (rank 1) · EXEC-v4:168 and :585 (rank 3) ·
  doc 38:131, task 3.22 (rank 5) · BOOTSTRAP-v4:211 (rank 8). Reference: 19:506 · 28:62,204 ·
  30:37,323 · D-03:720,873 · D-06:675 · D-15:890. Derived: `DECISION_LOG.md:106` resynced to its
  EXEC-v4 source · `tasks/MASTER_BACKLOG.md:122` (mirror of doc 38). The admin console is untouched
  and stays **ar / en** — the directive concerns the workforce, not back-office users.
- **Three things deliberately left alone.** `AUDIT-REPORT-v4.md:215` is the closed record of finding
  OPS-46 (four languages → five) and is history, not a live statement — rewriting it would falsify the
  audit trail. **Doc 29 carries no language statement at all**; its "§6" citation supports the *admin*
  `ar / en` half, and inventing a six-language line there would have repeated the fault being fixed.
  Doc 40 §A5 has no list either, so docs 28 and 15 were re-pointed at the closing note, where the list
  actually lives.
- **Filesystem freeze respected.** Docs 19, 30, 38 and 40 carried a read-only attribute; it was cleared
  for the edit and restored — 10 read-only package files before, 10 after. The attribute covers only
  10 of 57 package files and does not track the R-01 ladder (rank-3 EXEC-v4 is writable, rank-5 doc 38
  is not), so it reads as a copy artifact rather than a control; flagged, not "fixed".
- **No code impact.** `packages/` holds only `.gitkeep`; `packages/i18n` does not exist and no `.ts`,
  `.tsx` or `.json` file referenced `am` before this change or does now. The rule lands ahead of the
  implementation, which is the right order.
- **Open items carried into `docs/notes/SCR-I18N-01-amharic.md` §5** — Amharic is **LTR in Ge'ez script**,
  so the `packages/i18n` direction map must group it with `en`/`hi`/`bn` and `pg-frontend`'s
  "RTL is the default" needs it in the LTR exception list; Ge'ez font coverage on the industrial PDA is
  unverified against doc 30 §W6 and is worth settling before WBS 2.16; `D-15` decision **D-2** stays
  ⏳ GM; and Amharic translation sourcing has no named owner.
- Also filled the placeholder hash for 0.4 in `docs/PROJECT_STATE.md` (`05674b5`), which the file had
  left pending for the next commit to write.
- Model: opus (Master direct — single-lane governance task; frozen paths) · Delegated: none · Review: PASS(7 findings fixed) · tokens: ≈ 140k.

## 0.4 — monorepo initialised: pnpm workspace · Turborepo · TypeScript strict · ESLint boundaries (2026-09-21)

- **Acceptance (doc 38 row 0.4): `pnpm build` green · cross-module import fails lint — both green**, and
  `bash scripts/check-boundaries.sh` is the executable form of it. It asserts six things, because the
  criterion's words are satisfiable without its intent: (A) `pnpm build`, `pnpm typecheck` and
  `pnpm lint` green on the committed tree; (B) cross-module import **by package name** fails;
  (C) **by resolvable relative path** fails; (D) **by unresolvable relative path** fails; (E) an
  `eslint-disable` comment does **not** rescue it; (F) a file at `modules/<m>/domain/` **is**
  type-checked. Each of B–F was proved by a negative control — reverting the fix makes that
  assertion fail again.
- **Layout** (doc 36 §8): `apps/` · `modules/` · `packages/` workspaces, plus `tests/{scenarios,load,guards}/`
  and `database/{migrations,seeds}/`. Two module shells only — `modules/platform`, `modules/identity`
  (`package.json` + `tsconfig.json` + `index.ts` = `export {}`). They are the pnpm workspace containers
  `scripts/new-slice.sh` presupposes, not a replicated slice: the generator only ever creates
  `modules/<m>/{domain,application,infrastructure,api,tests}/<use-case>` and stays a no-op until
  `.golden-slice-accepted` exists. Module tsconfigs are scoped to **that** layout (doc 36 §1-2), never
  to `src/` — a tsconfig scoped elsewhere makes `pnpm build` green over an empty file set from WBS 2.9 on.
- **Boundary enforcement is three-layered**, because a module border is crossed three ways and each
  resolves differently: `boundaries/dependencies` (v7 policies) classifies resolvable paths;
  per-module `no-restricted-imports` `paths`/`group` catches package names; a `regex` entry catches
  relative escapes that **do not resolve** — boundaries cannot classify what it cannot resolve and
  would allow them silently. `linterOptions.noInlineConfig` + `reportUnusedDisableDirectives` make
  `eslint-disable` itself an error (CLAUDE.md · AGENT CONSTRAINTS).
- **New libraries — written justification (doc 36 §5-4 point 8).** All seven are devDependencies; no
  runtime dependency was added (NestJS, Drizzle, Zod, XState, pg-boss arrive with their own WBS tasks).

  | library | why | named by |
  |---|---|---|
  | `turbo` ^2 | the monorepo task runner the task calls for | doc 38 row 0.4 · doc 36 §7 ("pnpm + Turborepo") |
  | `typescript` **>=5.7.0 <6.1.0** | "TypeScript strict" | doc 38 row 0.4 |
  | `eslint` ^10 | gate ① STATIC | doc 36 §4-3 |
  | `@eslint/js` ^10 | ESLint's own recommended set, shipped separately | — |
  | `typescript-eslint` ^8 | parser + rules; without it `.ts` cannot be linted at all | — |
  | `eslint-plugin-boundaries` ^7 | named verbatim as the boundary mechanism | CLAUDE.md · ARCHITECTURE |
  | `eslint-import-resolver-typescript` ^4 | resolves NodeNext `.js` specifiers back to `.ts`, so boundaries can classify relative imports | — |

- **TypeScript is held at 5.9.3 deliberately.** `7.0.2` is the published `latest`, but
  `typescript-eslint@8.70.0` declares peer `typescript >=4.8.4 <6.1.0`, and that stack *is* gate ①.
  The range is written as `>=5.7.0 <6.1.0` rather than `^5.7` so the ceiling is a stated constraint
  instead of an accident of caret semantics. Revisit when typescript-eslint supports TS 7.
  ESLint went to 10 and `eslint-plugin-boundaries` to 7 (ESLint 9 is deprecated upstream); the v5-era
  `rules`/`element-types`/`${...}` config forms were migrated to v7 `policies`/`dependencies`/`{{...}}`
  — the plugin had been warning that it could not parse one of the old policies.
- **Review: FAIL (9 findings) → all 9 closed.** 1 `eslint-disable` opt-out · 2 tsconfig scoped to
  `src/` instead of the doc 36 §1-2 layout · 3 libraries undocumented + TS ceiling unexpressed ·
  4 boundaries config in unparseable v5 syntax · 5 three files written outside the brief's
  "Write ONLY" · 6 tier deviation (below) · 7 the script tested only the lint half of its own
  scenario · 8 unresolvable relative imports silently allowed · 9 fixture could be left in the tree
  and committed (now `__check_*.ts` is git-ignored, `trap` covers INT/TERM, and a stale fixture is
  cleared on entry).
- **Process deviation, recorded not hidden:** built **Master-direct on opus**, not pg-backend (sonnet)
  as `docs/MODEL_ROUTING.md` §4 and PROJECT_STATE require. The session opened on `Desktop/New sys`,
  one level above the repo root, so Claude Code never registered `.claude/agents`, `.claude/commands`
  or the lane-guard hook and no worker agent existed. Not retro-fixable in-session. **Every later
  session starts inside `claude-kit/`** — the check is that `/resume` and `/slice` are offered.
- **Guards G1–G18 NOT RUN** — psql 16 still absent and no SQL in the diff, so none can have regressed;
  not recorded as green. `scripts/check-boundaries.sh` is wired to `pnpm lint:boundaries` only; WBS 0.6
  must call it from gate ①.
- Model: opus (Master direct — see deviation) · Delegated: pg-reviewer role on opus · Review: PASS(9 findings fixed) · tokens: ≈ 190k.

## BOOTSTRAP-001 — kit verified and completed per BOOTSTRAP-v5 §9 (2026-09-21)

- Verified, nothing re-derived: 5 agents (`opus` ×1 · `sonnet` ×3 · `haiku` ×1; `inherit` = 0; each has `tools:`, the AGENT CONSTRAINTS block and the REPORT format) · 6 commands (13–30 lines, all ≤ 40) · 15 module briefs + `_TEMPLATE` (max 94 lines, all ≤ 120 — pre-generated; **not regenerated from the live schema: psql absent**) · `settings.json` valid JSON · hooks and scripts pass `bash -n` · `scripts/new-slice.sh` prints "golden slice not accepted yet" · `.gitignore` carries the 7 v4 §9 patterns and does NOT ignore `.golden-slice-accepted` · `DECISION REQUIRED` = 0 · MASTER_BACKLOG has 2.9 with its doc-38 criterion · LANE_LOCKS empty · 4 tasks in `tasks/proposed/` WAITING_GM · MODEL_ROUTING carries the §4 table · no `.env`, no credentials · `scripts/check-setup.sh` → FILES READY.
- Recorded: SETUP-000 = `bab005a` in PROJECT_STATE (exactly one DONE with a hash).
- **NOT RUN (environment, not a code blocker):** `database/schema/apply.sh --recreate` — Docker Desktop and psql 16 absent on the GM machine; expected on first run: zero errors · 175 tables · G7 = 0 · G-SEED = 0. `pnpm lint` cross-module-import FAIL test — pnpm absent; it is the acceptance criterion of 0.4 (doc 38) and is delivered there, not duplicated here (default taken, per BOOTSTRAP-v5 §2 "state one default, record it, proceed").
- Unsupported aliases: none reported (aliases are declared in the agent files; they are exercised on first delegation).
- Model: session (claude-fable-5-1, Master direct — state files only) · Delegated: none · Review: n/a (no code) · tokens: ≈ 45k.

## SETUP-000 — repository initialised from package v4 (2026-09-21)

- `docs/package/` ← PG-EOS-v4: A-governing (9 md) · B-reference (26 md) · C-tools → `tools/` · D-blueprints (16 md + `diagrams/` 172 mmd + 153 svg + `tools/`) · root docs (00-README-v4 · AUDIT-REPORT-v4 · CHANGELOG-v4). PNG renders omitted (svg kept) to keep the repository light.
- `database/schema/` ← `01 · 13 · 13B · 019 · guards.sql · apply.sh` — the ONLY permitted schema (175 tables · 16 views · 14 schemas; expected `apply.sh --recreate`: zero errors · G1–G13 = 0 · G7 = 0 · G-SEED = 0).
- Kit at root (CLAUDE.md · `.claude/` · `scripts/` · `tasks/` · `docs/` · `infra/` · `.gitignore`) as shipped in `claude-kit/`.
- **Added by setup (were reported missing by the `/resume` pre-check):** `tasks/MASTER_BACKLOG.md` (132 rows generated from doc 38 by `scripts/gen-backlog.py`, statuses seeded: 0.1 DONE pre-build · lane A WAITING_GM · 0.4 READY · rest TODO) · `docs/DECISION_LOG.md` (seeded from EXECUTION-MASTER-v4 Part 1, zero open markers) · this file · `tasks/{backlog,active,completed,blocked}/` · `docs/notes/` · `scripts/check-setup.sh` · `scripts/gen-backlog.py`.
- Model: n/a (no agent session) · Delegated: none · Review: n/a · tokens: 0 (done outside Claude Code).
- Remaining before BOOTSTRAP-001: install pnpm / Docker Desktop / psql 16 on the GM machine (PROJECT-SETUP-GUIDE §1); run `bash scripts/check-setup.sh` → `docker compose … up -d postgres` → `database/schema/apply.sh --recreate` (§3) → the bootstrap prompt (§4).
