Task: 3.1 part 2 — AssertVehicleAssignable, the real INV-C4-1 hard gate (doc 38 acceptance "Expired-doc vehicle cannot be assigned", still NOT met by part 1)
Lane: 3          Lock: fleet (existing whole-module lock, no new claim)
Read ONLY:
  - CLAUDE.md
  - .claude/briefs/fleet.brief.md
  - docs/notes/slice-briefs/_slice-3.1.brief.md (the original design brief — part 2's architecture is already decided there: fail-closed on `ctx.isInternal`, `expiredDocumentsOf` as the single comparison implementation, i18nKey/params surfaced, "(Allowed: ...)" clause — this fix round does NOT re-litigate the design, only the 6 remaining hygiene/coverage findings below)
  - modules/wms/domain/process-outbound/errors.ts lines 75-91 (the `OutboundCheckError`/`i18nKey` precedent finding 6 already cites — now legitimately in scope)
  - modules/wms/api/process-outbound/handlers.ts lines 79-121 (the `ProblemBody`/`i18n` params precedent finding 6 already cites)
  - modules/fleet/tests/register-vehicle/register-vehicle.test.ts lines 200-240 (the non-internal-actor fixture pattern — gives the actor an `identity.user_entities` row for the vehicle's entity, the exact fixture shape finding 5's test needs)

P7 budget: 6 files, well under the 8-file/1,000-line cap enforced by `scripts/brief-check.sh`. **P7 review-round rule (effective now): two rounds max. If round 2 is FAIL, commit only the PASS subset and open part 3 — no round 3, no session-tier Master fix.** Commit trailer must read `Review: PASS(<n> findings, <r> rounds)` (the commit-msg hook now refuses a feat commit without a PASS trailer in that or the old `PASS(<n> findings fixed)` form).

Write ONLY: modules/fleet/{domain,application,infrastructure,api,tests}/assert-vehicle-assignable/** · modules/fleet/domain/register-vehicle/errors.ts (finding 4 only — the `VehicleDocumentAccessDeniedError` docstring, one comment, no logic change) · modules/fleet/application/register-vehicle/register-vehicle.ts (Master-authored only, one-line `CatalogedEventType` typing — see "Also apply" below; **round-1 finding 1: this was missing from Write ONLY, added here**) · modules/fleet/index.ts · packages/contracts/fleet/assert-vehicle-assignable.ts · packages/contracts/package.json (its export entry) · tests/…

Six findings from round 2 of the original review (all already scoped/diagnosed, this round is pure fix, not new design):

1. **Fabricated brief citation.** `application/assert-vehicle-assignable/assert-vehicle-assignable.ts`, `api/assert-vehicle-assignable/handlers.ts`, `tests/assert-vehicle-assignable/handlers.test.ts` all quote "the brief says ... same class as a query, not a command" — the actual brief (docs/notes/slice-briefs/_slice-3.1.brief.md) only says "No Idempotency-Key — this is a pure read-and-assert, no write, no side effect." Fix every such comment to quote the brief accurately or drop the false attribution and state the design decision plainly.
2. **Stale docstring, domain.** `domain/assert-vehicle-assignable/errors.ts`'s file header still names `hasExpiredDocument` as the one register-vehicle reuse; the command actually imports `expiredDocumentsOf` only (register-vehicle's `invariants.ts`, already fixed in round 2's production fix). Correct the header, and it should also note the (legitimate, already-authorized) reuse of register-vehicle's `VehicleDocumentAccessDeniedError` for the fail-closed check.
3. **Stale docstring, test.** `tests/assert-vehicle-assignable/assert-vehicle-assignable.test.ts`'s header has the same stale `hasExpiredDocument` reference — correct it the same way.
4. **Stale docstring, register-vehicle's own file.** `domain/register-vehicle/errors.ts`'s `VehicleDocumentAccessDeniedError` docstring currently claims it "adds no new application-level 'is internal' check" and is only ever a translation of a genuine RLS SQLSTATE-42501 rejection on a WRITE — that description stops being complete once `assert-vehicle-assignable` reuses the same class to refuse a READ path proactively (checking `ctx.isInternal` before even attempting the read, since RLS silently empties a SELECT rather than erroring it). Update the docstring to describe BOTH uses accurately: the original write-path translation, and the new read-path proactive refusal, without contradicting itself.
5. **Security regression test doesn't test the vulnerability.** `tests/assert-vehicle-assignable/assert-vehicle-assignable.test.ts`'s "non-internal caller with an expired document" test uses a non-internal actor with NO `identity.user_entities` row for the vehicle's entity — so `platform.allowed_entities()` returns `'{}'`, `tms.vehicles`' own `entity_scope` RLS hides the vehicle itself, and the command throws `VehicleNotFoundError` — never reaching the real fail-open path (`tms.vehicle_documents`' `internal_only` RLS silently emptying a SELECT while the vehicle itself stays visible). Fix: give the non-internal actor a real `identity.user_entities` row for the vehicle's own entity (same fixture pattern as `register-vehicle.test.ts` lines 200-240, now in Read ONLY) so the vehicle IS visible and only the documents are hidden — THEN the test proves the fail-closed `ctx.isInternal` check is what's actually protecting the gate, not an unrelated 404.
6. **Out-of-scope citations, now resolved.** The two precedent files (`modules/wms/domain/process-outbound/errors.ts`, `modules/wms/api/process-outbound/handlers.ts`) that were read without being in Read ONLY are now explicitly listed above with line ranges — no code change needed for this finding itself, it's closed by the brief correction; just don't introduce a NEW out-of-Read-ONLY citation while fixing findings 1-5.

Also apply (small, already-decided, not a new design item): `modules/fleet/application/register-vehicle/register-vehicle.ts`'s `VEHICLE_REGISTERED_EVENT_TYPE` is now typed `CatalogedEventType` (imported from `@pg-eos/events`, same convention as `modules/wms/application/process-outbound/allocate.ts`'s own event constants) — `fleet.vehicle.registered` landed in `packages/events/catalog.ts` after part 1 merged (P7). This edit is ALREADY DONE by the Master directly (one-line type annotation + import, `packages/events`'s own `dist/` rebuilt to pick up the new catalog entry — a stale build artifact, not a source change) — no action needed from the worker, mentioned here only so the brief's own Write ONLY correctly covers `modules/fleet/application/register-vehicle/register-vehicle.ts` if pg-reviewer asks why it's in the diff.

Stop-and-ask if: any table/column/rule not in 01 / 13 / 13B / 019 / 40, or if any of the 6 findings turns out to need a genuine design change beyond a comment/test fix (that would be a REAL BLOCKER, report it, don't invent a workaround).
